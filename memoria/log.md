---
capa: wiki
tipo: log
estado: vigente
fecha: 2026-08-02
areas: []
tags: [generado]
fuente: sesion
resumen: "Bitácora cronológica de la wiki: qué se ingirió, qué se consultó y qué se verificó"
---
# Bitácora

Append-only. Una línea por operación, la más reciente abajo. Formato:

```
- YYYY-MM-DD · operación · asunto · páginas tocadas
```

Las operaciones son `ingest`, `query` y `lint`, descritas en [[index]]. Se escribe al final de
cada una, no antes.

---

- 2026-08-02 · ingest · Se funda la wiki con el patrón LLM Wiki de Karpathy · [[index]], [[log]]
- 2026-08-02 · ingest · Migradas 31 memorias privadas del asistente desde `~/.claude`, conservando el cuerpo verbatim y acortando los wikilinks · 23 páginas en `trampas/`, 6 en `decisiones/`, 2 en `referencias/`
- 2026-08-02 · ingest · Leídos los 16 `goal.md` para fijar el estado real de cada goal · [[estado]]
- 2026-08-02 · ingest · Escritos los siete mapas de área a partir de la documentación existente · [[arquitectura]], [[design-system]], [[pdc]], [[lps-dominio]], [[rbac-y-rutas]], [[entorno-y-despliegue]], [[qa-y-gates]]
- 2026-08-02 · ingest · Enganche de la wiki en [[CLAUDE]] (sección «Memoria del proyecto», con el esquema y las tres operaciones) y en [[AGENTS]] (línea de precedencia) · `CLAUDE.md`, `AGENTS.md`
- 2026-08-02 · ingest · Las 31 memorias privadas de `~/.claude` reducidas a punteros; su `MEMORY.md` pasa a avisar de la mudanza
- 2026-08-02 · lint · Apareció un primer tramo de la wiki ya en disco (`Inicio.md` y dos páginas de `decisiones/`), sin autoría identificable; se asume trabajo previo propio no registrado. Se retiraron dos páginas duplicadas entre `trampas/` y `decisiones/`, se adoptó su campo `origen:` en las 31 páginas migradas y se fusionó `Inicio.md` en [[index]]. Una versión previa del spec, no versionada, se perdió al sobrescribirla · [[index]], [[compras-migrado-shell-sidebar]], [[dev-door-acceso-local]]
- 2026-08-02 · lint · Filtrados `.agents/` y `.github/` del vault: espejaban la documentación de skills y creaban 44 nombres duplicados. Quedan 8 duplicados estructurales (`goal.md`, `plan.md`, `facts.md` por goal) que no afectan a ningún enlace · `.obsidian/app.json`
- 2026-08-02 · lint · Verificación: 186 wikilinks resueltos, 0 rotos y 0 ambiguos; 0 rotos también en un clon fresco; 31 páginas migradas frente a 31 punteros; toda página aparece en [[index]]; ningún `.md` de `docs/` modificado
- 2026-08-02 · ingest · Escrita la lección del episodio de atribución: no deducir autoría de un archivo por la hora de última actividad de una sesión · [[autoria-por-coincidencia-de-hora]], [[index]]
- 2026-08-02 · ingest · Repasada la lista blanca de `goals/` en `.gitignore`: `shell-layout-design-system` y `sidebar-todos-modulos` no tenían ningún archivo en git, faltaba `cierre-dark-mode-y-tablas/specs/diseno.md`, y `bi-control-tower-gemini` viajaba solo por accidente histórico. Añadidas sus excepciones: los 16 goals viajan ahora, 97 `.md` en un clon fresco. Corregida la sección que los daba por perdidos · [[estado]], `.gitignore`
- 2026-08-02 · ingest · Convertidas a wikilinks las menciones que los mapas y [[estado]] ya hacían por ruta: 9 documentos de `docs/` y los 16 goals. La conectividad del grafo pasa de 50 a 75 nodos (de 265). Se dejó en código `docs/ROUTES.md`, que no viaja en git y quedaría roto en un clon · 7 mapas, [[estado]]
- 2026-08-02 · ingest · Tejidos los goals a sus áreas: [[design-system]] y [[pdc]] enlazan ahora los goals que trabajaron cada zona, así que cada `goal.md` tiene dos entrantes en vez de colgar solo de [[estado]]
- 2026-08-02 · ingest · Por decisión del usuario se abre una excepción a la inmutabilidad de las fuentes: cada `goals/<slug>/goal.md` recibe al pie una sección «Archivos de este goal» que enlaza a sus hermanos versionados y a [[estado]]. `goals/` pasa de 16 a 97 nodos conectados de 99; el grafo completo, de 75 a 156 de 265. `docs/` no se toca · 16 `goal.md`, [[index]], [[CLAUDE]]
- 2026-08-03 · lint · Primera pasada con `scripts/wiki-lint.mjs`: tres afirmaciones corregidas contra medición del repositorio, una nota de diez hechos repartida en siete páginas, vocabulario de `areas` cerrado en trece valores (reincorporado `arquitectura`) y catálogo del índice generado con Bases · [[index]], 7 páginas nuevas, `memoria/paginas.base` verificada visualmente en Obsidian
- 2026-08-03 · ingest · `CLAUDE.md` actualizado tras la pasada: la operación lint pasa a nombrar `scripts/wiki-lint.mjs`, se distingue lo que el script comprueba (forma) de lo que no (veracidad, que sigue siendo manual), y se documenta la lista cerrada de trece áreas y el deber de corregir el `resumen` junto al cuerpo · `CLAUDE.md`
- 2026-08-03 · ingest · Saneamiento del goal [[goals/cierre-dark-mode-y-tablas/goal|cierre-dark-mode-y-tablas]], cerrado el 2026-07-31 con tres de sus ocho condiciones sin cumplir como estaban escritas: la escala `--ds-cell-state-*` ancló cinco colores propios junto a `--ds-color-state-*` (ahora deriva de ella y conserva un solo ancla), el gate no estaba enrutado (ahora corre en estático y en runtime) y era estático pese a exigir medición con filas. El gate de runtime nuevo encontró en su primera corrida que `--ds-table-empty-fg` apuntaba a `--ds-active-text-tertiary`, inexistente · nueva trampa [[gate-estatico-no-ve-tokens-rotos]], `public/css/tokens.css`, `package.json`, `tests/browser/design-system-table-contract.runtime.mjs`
- 2026-08-03 · ingest · Arquitectura por módulo generada desde el código: 23 páginas en `memoria/arquitectura/` con zona generada entre marcadores (222 rutas repartidas, cero huérfanas), dos páginas de flujo, y `docs/ROUTES.md` retirado tras comprobar ruta por ruta que su matriz de navegación quedaba rescatada como prosa. RBAC contrastado contra `src/Security/RbacManager.php` en tres módulos (programa-general, programación semanal, contratos) y en runtime por la puerta de servicio en viewport 1180×820 dark: `test.R` ve los controles de semana (crear/eliminar) en `/programa-general`, `test.V` no, consola limpia en ambos — comprobación de navegador completada por el coordinador tras el cierre de esta tarea · [[index]], [[flujo-lps]], [[flujo-pdc]]
- 2026-08-03 · ingest · `DESIGN.md` puesto al día tras el goal [[goals/cierre-dark-mode-y-tablas/goal|cierre-dark-mode-y-tablas]]: el contrato de consumo no mencionaba el sistema de tablas construido el 30-31 de julio (cero apariciones de `--ds-table-*`, `--ds-cell-state-*`, `.aia-grid-shell`, los tres adaptadores y los dos gates) y su frontmatter declaraba los estados semánticos en los valores CLAROS pese a que la app es dark-only. Corregido a la paleta invertida a oscuro real de `tokens.css`, con las variantes `-light` marcadas como reserva de impresos/XLSX —hoy declaradas y sin ningún consumidor— y nueva subsección «Tablas y grillas» con las cinco reglas de uso · `DESIGN.md`, `.impeccable/design.json`
- 2026-08-03 · ingest · Corrección al pase anterior de `DESIGN.md`, avisada por otra sesión y verificada aquí contra `tokens.css:234-295` y `docs/design-system/state-semantics.json`: la paleta de estado no es una lista plana sino **dos canales ortogonales** —nivel (4 peldaños, se pinta en el acento) y matiz (8 anclas nominales, se pinta en el fondo)—, con la regla contractual de que ningún módulo repite matiz. El frontmatter representaba fielmente el canal de nivel pero aparentaba cobertura total: cuatro de las ocho anclas son bit-idénticas al `-bg` de su nivel y las otras cuatro (`blue`, `violet`, `orange`, `neutral`) no tienen token de nivel. Añadidos los ocho `state-tint-*` con los dos canales rotulados y la regla nombrada. Detectado de paso que `--ds-cell-state-bloqueado-*` ancla un color fuera de ambas escalas · `DESIGN.md`, `.impeccable/design.json`
- 2026-08-03 · ingest · Cierre del intercambio con la sesión de dark mode sobre `DESIGN.md`. Resuelto que `--ds-cell-state-bloqueado-*` es **deuda, no diseño**: su razonamiento («el nivel no tiene peldaño de detenido por otro») se tomó sin conocer el canal de matiz, y medido en OKLCH `--ds-state-tint-blue` (`#17334f`, L 0,314 · C 0,061 · H 250,4°) queda a 0,036 de luminosidad y 9,6° del `oklch(0.35 0.05 260)` de `bloqueado`. No se corrige: la paleta tiene aprobación visual explícita del usuario de hoy y cambiarla de oficio la invalidaría en silencio. Enunciado de la deuda escrito en `DESIGN.md`. Retirado un hallazgo propio por caducado: el gate de runtime de tablas **sí** está versionado desde `fff71ad`; mi observación salía del snapshot de git del arranque de sesión, anterior a ese commit · nueva trampa [[comentario-de-token-afirma-uso-inexistente]], `DESIGN.md`, [[design-system]]
- 2026-08-03 · ingest · Decisión del usuario sobre los ocho `--ds-color-state-*-light`: **se quedan y el comentario dice la verdad**. Al grillearlo apareció el dato que faltaba: el consumidor que el comentario invocaba sí existe —`src/Controllers/Gestion/ReportController.php` exporta con PhpSpreadsheet y pinta el estado de cada fila— pero nunca se cableó y lleva su propia paleta ARGB escrita a mano, con valores distintos. Dos paletas claras para lo mismo. Unificarlas cambiaría el aspecto de los Excel ya repartidos, así que queda como decisión aparte. Comentario de `tokens.css` reescrito (sin hex, para no romper el presupuesto del audit) · `public/css/tokens.css`, `DESIGN.md`, [[comentario-de-token-afirma-uso-inexistente]]
- 2026-08-03 · ingest · Corregida [[semanal-auto-dispara-mutaciones]] contra el código: afirmaba que abrir /programacion-semanal dispara `save` y `auto-program` «en cada carga, sin interacción», y ambas son condicionales — `save` exige `canManageToolbarActions()` (`hot.js:2095-2106`) y `auto-program` exige `semana > 0` (`changeMonitor.js:35-47`). Lo destapó una sesión que interceptó POST y no observó ninguno de los dos; el fondo sigue vigente y queda como línea F-bis del reparto · [[semanal-auto-dispara-mutaciones]], `docs/superpowers/specs/2026-08-03-reparto-trabajo-pendiente-design.md`
- 2026-08-03 · ingest · Corregida en `DESIGN.md` la «Regla de Un Matiz por Estado», que documenté como cumplida por `/programa-general` fiándome de `state-semantics.json`: **el contrato la cumple, la implementación no**. Verificado aquí: el contrato declara «Actividad Futura» → `green` y «En Curso» → `blue`, y `public/css/styles.css:3516` y `:3541` pintan las dos con `--ds-cell-state-ok-*` (8,88:1 ambas, mismo color); la leyenda sí las distingue (`--pg-dot-future` de success, `--pg-dot-progress` de info), así que promete una diferencia que la grilla incumple. Tercer sitio del mismo fallo, no señalado antes: `cell-state-vocabulary.mjs` manda los dos alias a `CELL_STATE.OK`, y es el traductor que el propio `DESIGN.md` recomienda usar. La regla queda documentada con el aviso de que está vigilada en el papel, no en el píxel · nueva trampa [[guard-valida-declaracion-contra-si-misma]], `DESIGN.md`, [[design-system]]
- 2026-08-03 · ingest · Nueva trampa [[axe-incomplete-cuenta-como-violacion]]: el arnés de a11y aplana `incomplete` junto a `violations`, y como las superficies del repo son translúcidas por diseño axe no puede medir contraste y devuelve `incomplete` siempre. Los nueve de `/programa-general` se midieron con la sonda que sí compone alfa: 55 nodos entre 13,82 y 15,27:1, cero bajo AA. Registrados como excepción con la cifra dentro; queda pendiente separar las dos categorías en el arnés · `memoria/trampas/axe-incomplete-cuenta-como-violacion.md`, `docs/design-system/a11y-exceptions.json`
- 2026-08-03 · ingest · Falsa alarma cerrada: el rojo permanente de `test:design-system:static` por un `ops-state-chip.css` inexistente ya no existe. El reporte se apoyaba en `ad14fc1` (que sí adelantó el test), pero la hoja llegó en `8b586b3` y está versionada en `main`; el test del chip pasa. Lo que sí es real y despistó al diagnóstico: ese suite es mitad Node y mitad PHP-en-Docker, y desde un worktree cada mitad lee un árbol distinto, así que «stylesheet versions follow nested CSS changes» da un rojo garantizado por mtimes. Verificado: 28/28 en `foundation.test.mjs` desde el árbol principal, 364/365 desde el worktree. Los 3 rojos que quedan en el principal son de trabajo sin commitear de otra sesión (`docs/design-system/a11y-exceptions.json`, +108 líneas), no de este goal · nueva trampa [[suite-estatico-mide-dos-arboles]], [[branch-preexisting-red-gates]]
- 2026-08-03 · ingest · Task 12-bis rediseñó el ancla de `state-token-exceptions.json` de número de línea a selector+token (con `occurrence` sólo para el único duplicado literal, `.ps-dropdown-item.is-active`). Revisión encontró un residuo: `occurrence` cuenta posición relativa entre copias del mismo selector+token, así que insertar una regla nueva ENTRE las dos copias existentes corre el `occurrence` declarado; el gate sigue rompiendo, pero el mensaje no señala la causa. Documentado con nota propia y un campo `occurrenceCaveat` junto a las dos entradas afectadas del JSON · nueva trampa [[occurrence-no-resiste-insercion-entre-duplicados]], [[design-system]], `docs/design-system/state-token-exceptions.json`
- 2026-08-03 · ingest · Cerrados los tres pendientes de la wiki que dejó el diagnóstico. Nace la cuarta operación `veracidad` y su alarma: `scripts/wiki-veracidad.mjs` (funciones puras, git inyectado, 12 pruebas con `node --test`) cuenta los commits de código posteriores al último pase y `wiki-lint.mjs` sale en rojo por encima de 40. Se mide en commits y no en días porque el repo hace 100-181 al día en sprint y ninguno en fin de semana; `memoria/` no cuenta, para que la wiki no dispare su propia alarma. El esquema sale de la prosa de `CLAUDE.md` a `docs/wiki-operacion.md`, en la capa de fuentes (CLAUDE.md: 38 líneas fuera, 9 dentro) · `scripts/wiki-veracidad.mjs`, `tests/wiki/veracidad.test.mjs`, `docs/wiki-operacion.md`, [[index]], [[CLAUDE]]
- 2026-08-03 · ingest · Grafo tejido con criterio comprobable tras descartar una clasificación delegada que decidía por carpeta y fecha: marcaba 113 de 124 como vigentes con justificaciones genéricas. Se rehízo a mano en tres reglas medibles (citado por documento vivo · contrato o referencia del design system, cabecera leída · operativo de infraestructura): 25 tejidos, sueltos de 124 a 99, y la caída cuadra exactamente con lo tejido. Los 99 restantes son trabajo fechado y quedan sueltos a propósito, declarado en el índice. De paso apareció que `memoria/arquitectura/integracion.md` estaba aislada siendo página de la wiki · `docs/superpowers/specs/evidencia/2026-08-03-nodos-sueltos.md`, 4 mapas, [[index]]
- 2026-08-03 · veracidad · Primer pase, áreas revisadas: design-system, pdc · 27 páginas · 22 confirmadas, 5 corregidas, 10 no comprobables en lectura (exigen navegador, Docker o historial de CI). Las cinco correcciones son citas de `archivo:línea` que el código movió, verificadas una a una antes de aplicarlas. La de [[compras-migrado-shell-sidebar]] resultó más honda que la reportada: el harness lista 23 rutas pero solo cuenta 21 en `MIGRATED`, y las dos que excluye —`/contratos` y `/listado-actividades`— son justo las que la nota daba por migradas · [[hot-container-height-ownership]], [[compras-migrado-shell-sidebar]], [[lab-desktop-layout-suite]], [[comentario-de-token-afirma-uso-inexistente]]
- 2026-08-03 · ingest · El mojibake de `/programa-general` y `admin/usuarios` no era un bug de encoding del código: verificado byte a byte, el pipeline PHP → PDO `utf8mb4` → `json_encode` está limpio y el byte doble-codificado ya viene del `fetch()`. Eran dos filas de la base local (`programa_consolidado` 990100/1 y `general_usuarios` id=1), corregidas con respaldo `HEX()` previo. Dos lecciones de método: comparar dos caminos exige igualar primero la fila que cada uno lee —se diagnosticó contra `project_id=27` sano mientras la sesión real leía el 990100 corrupto— y el respaldo salvó el arreglo, porque el primer `CONVERT` fue en dirección contraria y añadió una tercera capa · nueva trampa [[mojibake-es-dato-no-codigo]], [[qa-y-gates]]
- 2026-08-03 · ingest · Segunda vuelta sobre los 99 huérfanos, en dos mitades. Archivado: de los 66 documentos de julio se midieron las referencias entrantes una a una y se movieron con `git mv` solo los **30 que nadie citaba**, a `docs/archive/superpowers/`; los 36 referenciados y los 5 citados por contratos vivos (entre ellos la spec de la puerta de servicio, que cita [[AGENTS]]) se quedaron donde estaban. Verificado después: cero referencias rotas. Registro: `scripts/wiki-registro.mjs` genera [[IMPLEMENTATION_PLAN_INVENTORY|Registro de trabajo]] emparejando cada spec con su plan por slug —60 trabajos, 23 con las dos mitades, 14 archivados—, que es la relación real y no un enlace de relleno desde los mapas. Sueltos: 124 → 99 → **25** · `scripts/wiki-registro.mjs`, [[IMPLEMENTATION_PLAN_INVENTORY|Registro de trabajo]], [[index]], `docs/wiki-operacion.md`
- 2026-08-03 · ingest · Premisa de diseño de alta prioridad fijada por el usuario: la interfaz queda **inspirada en Apple pero expresada con el design system de AIA en dark** — se adoptan los principios (deferencia al contenido, jerarquía por espaciado y tipografía en vez de cromo, un solo acento por vista, controles discretos que ganan presencia al necesitarse) y se rechaza copiar el aspecto de macOS/iOS: siguen mandando los tokens `--ds-*`, las primitivas `aia-*` y el alcance desktop-dark de [[AGENTS]]. Su límite duro es que la libertad es de forma y nunca de funcionalidad: comportamientos, datos, reglas de negocio, rutas, permisos y el texto que comunica una regla de dominio quedan intactos. Enlazada desde [[design-system]] como lo primero que manda · nueva decisión [[inspiracion-apple-en-dark-aia]], [[design-system]]
- 2026-08-04 · veracidad · Segundo pase, disparado por la alarma en su primer uso real: 158 commits desde el del 2026-08-03, cuatro veces el umbral. Áreas revisadas: qa, admin, proceso, rbac, arquitectura, lps, datos, docker, worktrees, deploy, bi, y una repesca de design-system porque `public/css` recibió 76 commits en un día · 46 páginas · 1 corregida, 27 confirmadas con cita, 19 con afirmaciones no comprobables en lectura (exigen Playwright, Docker, base de datos o SSH; declaradas, no dadas por buenas). La corregida es [[drawer-en-handsontable-module]]: la geometría de `.lps-drawer` ya no empieza en `handsontable-module.css:377` sino en `:399` —377 es hoy una sección de navbar—, y de paso se precisó que el agregador la importa en `layer(vendor)` y `core.css` no la menciona. Muestreé cuatro confirmaciones de los frentes delegados contra el código y las cuatro coincidían literalmente; se cerró además un hueco que un frente declaró sin abrir (`tests/test_pdc_v2_vencimientos.php:138` dice lo que la nota afirma) · [[drawer-en-handsontable-module]], [[powerbi-indicadores]], [[qa-y-gates]], [[rbac-y-rutas]], [[entorno-y-despliegue]]
- 2026-08-04 · ingest · Sembrados los conceptos del design system: el área tenía 21 trampas y cero páginas de concepto, así que la wiki decía dónde tropezar pero no cómo funciona el sistema. Siete fichas nuevas en `memoria/conceptos/` —tokens en dos capas, madurez y API estable, baselines y presupuestos, excepciones registradas, homologación por familias, manifiesto de módulo, los cuatro inventarios—, cada afirmación con cita `archivo:línea` verificada contra el consumidor real (dos recolecciones delegadas, muestreadas antes de usar). Estrenan el tipo `concepto`, que el lint admitía y nadie usaba; `paginas.base` gana su vista. Las 18 fuentes de `docs/design-system/` que la wiki no mencionaba quedan en 0. Hallazgo en el camino: `lab-performance-baseline.json` existe pero ningún script lo lee — mismo patrón que [[comentario-de-token-afirma-uso-inexistente]], anotado en [[baselines-y-presupuestos]] · 7 conceptos, [[design-system]], [[index]], `memoria/paginas.base`
- 2026-08-04 · ingest · Cerrada la divergencia de versión del design system: `version.json` dice 1.0.0/stable desde `58b850e7` (16 de julio) y el `CHANGELOG.md` siguió titulando «0.3.6 - En construcción» durante 19 días. Registrada la trampa [[changelog-ds-encabeza-version-vieja]], corregido el encabezado del changelog por decisión del usuario (citando el commit de activación, sin tocar el cuerpo) y corregido el mapa, que desde ayer mandaba a leer justo la fuente que engañaba · [[changelog-ds-encabeza-version-vieja]], `docs/design-system/CHANGELOG.md`, [[design-system]]
- 2026-08-04 · ingest · Verificada la preocupación del usuario: el CHANGELOG del design system llevaba sin escribirse desde el 25 de julio mientras entraban ~30 commits al sistema, cinco de ellos contractuales (densidad compacta `67f35c4f`, botones/chips compactos `201dd5ec`, control de toolbar `03434f64`, gatillo de filtro `a6a664f9`, paridad del chip `51ccd5ca`). Se anotaron bajo «Sin publicar (candidato a 1.1.0)» porque el intento de subir `version.json` a 1.1.0 puso la suite en rojo: 38 excepciones de `exceptions.json` vencen en esa versión, ~10 manifiestos exigen `designSystemVersion` sincronizado y dos gates comprueban `1.0.0` literal. Bump revertido, suite estática en verde (8/8) · nueva trampa [[subir-la-version-del-ds-cobra-deudas]], `docs/design-system/CHANGELOG.md`, [[design-system]]
- 2026-08-04 · ingest · Diseñada la biblia de flujos (`docs/flujos/`, cláusula «si biblia y código divergen es un bug de uno de los dos»), escritos los cinco planes de tanda y sus goals, y cerrada la fase 1 de improve-app con `docs/CUSTOMER.md`: tres jobs por rol, nueve dimensiones y subentrega declarada en las cuatro áreas. El hallazgo del mapa de jobs es que las tres cadenas comparten origen —si el residente no registra bien, el director no ve patrones y la gerencia no sostiene cifras—, lo que convierte la cascada PG→PI→PS en cuello de botella de los tres a la vez · `docs/CUSTOMER.md`, `docs/EXPERIMENTS.md`, 5 planes, 5 goals, [[estado]]
- 2026-08-04 · ingest · Dos hallazgos aparecidos **escribiendo los planes**, antes de ejecutar ninguna tanda, ambos verificados contra el código antes de escribirlos: [[semi-auto-solo-lo-usa-pdc]] ([[AGENTS]] `:23` da por compartidos los contratos `auto/*` entre Listado, Contratos y PDC, pero las 13 rutas son todas `/api/pdc/` y los 12 métodos `*Listado` no tienen ninguna) y [[indicadores-oculta-en-cliente-bi-en-servidor]] (`/indicadores` esconde el informe con JavaScript y su controlador no mira el rol, mientras `/bi/*` responde 403). Ninguno se arregla: la regla de la biblia es registrar y seguir, y los dos admiten dos conductas correctas distintas · `docs/EXPERIMENTS.md`, 2 trampas nuevas
- 2026-08-04 · ingest · T1 de la biblia ejecutada: `docs/flujos/` nace con su README (cláusula «si biblia y código divergen es un bug de uno de los dos») y tres documentos —autenticación, proyecto y RBAC— con 10 escenarios `AUTH`, 7 `PROY` y las 17 capacidades volcadas del código. 7 pruebas ejecutables en `e2e/tests/biblia/transversal.spec.mjs`, tituladas con su `id`, **en verde**. 10 hallazgos al backlog, ninguno arreglado. Los tres que más pesan: la sesión caduca en una grilla y el usuario pierde el trabajo sin error entendible porque solo 2 archivos mandan `X-AIA-Expect-Json` y ningún módulo de grilla lo hace; el selector normaliza roles con un método propio en vez del `cleanCargo()` que exige [[AGENTS]], así que un «Director de Obra» escrito en texto entra como Visualizador; y la barra de progreso del selector es `rand(0,100)`. La prueba de RBAC falló en su primera corrida por defecto **mío** —los controles viven dentro del flyout de semanas, que no abría—, se diagnosticó con sonda y se corrigió con identificadores estables · `docs/flujos/`, `docs/EXPERIMENTS.md`, [[rbac-y-rutas]]
- 2026-08-04 · ingest · T2 de la biblia ejecutada: cinco documentos en `docs/flujos/` (cascada, programa general, intermedia, semanal, aprendizaje) con 26 escenarios verificados por lectura y 5 pruebas ejecutables en verde. Lo aprendido de fondo: **la aplicación autoriza de cuatro formas distintas** —`authorizePermission` en PG e Intermedia, guardias propias en Semanal, `rbac_guard` legado en CNP/CNC/CIC, y permisos `lps.pdc.*` en el PDC—, y las capacidades booleanas de `RbacManager` que la interfaz usa para pintar botones **no son las que el servidor comprueba**. Hallazgos nuevos: `sanear` muta sin CSRF (confirmado en ejecución), la restricción de editar el pasado solo existe en el navegador, `canDeleteRows` no la consulta nadie, y las dos piezas del candado fallan hacia lados opuestos. Corregida de paso [[flujo-lps]], que describía el CIC como «cumplimiento medido» cuando es la calificación integral del contratista en cuatro dimensiones · `docs/flujos/`, `docs/EXPERIMENTS.md`, [[flujo-lps]]
- 2026-08-04 · ingest · T3 de la biblia, primera pasada (la puerta): `docs/flujos/pdc-plan-compras.md` con 8 escenarios de autorización y 3 pruebas en verde, **cero hallazgos**. Dos resultados que valen por sí mismos: la deuda que `docs/pdc-v2.md` marcaba como «el borrado más peligroso de PlanFechasService» **está atendida** —las seis escrituras sobre `pdc_plan_paso`, `pdc_plan_paquete` y `pdc_paquete_frente` se acotan por `project_id`+`paquete_id`+`subpaquete_id`, y los borrados respetan `fecha_real`—; y el PDC previó lo que a Semanal se le escapó: su lista de operaciones que exigen CSRF lleva la red `|| isset($_POST['columna'])`, así que ninguna edición de celda se cuela. Dentro del propio PDC conviven los dos mecanismos de autorización, legado y moderno · `docs/flujos/pdc-plan-compras.md`, [[pdc]]
- 2026-08-04 · ingest · T4 de la biblia, primera pasada: `docs/flujos/soporte.md` con 3 escenarios y 2 pruebas en verde. El hallazgo es el de mayor alcance de todo el proyecto y cierra el pendiente que T2 dejó abierto: **seis módulos mutan datos sin validar CSRF** —CIC (10 sentencias), Profesionales (5), Subcontratistas (4), Control de Cambios (3), CNP (2), CNC (1)—, porque `rbac_guard_require_permission()` autoriza pero **no valida token**, y el helper que sí lo hace, `legacy_require_csrf()`, vive en el mismo archivo (`src/Legacy/rbac_guard.php:83-89`) y solo lo usan dos scripts. No falta la pieza: falta llamarla. Aclarado de paso que «Contratos» y «Listado de Actividades» no tienen backend propio —sus rutas son del PDC por un reparto de criterio declarado en `scripts/wiki-arquitectura.modulos.mjs:116-124`—, comprobado antes de escribirlo para no registrar una alarma falsa · `docs/flujos/soporte.md`, `docs/EXPERIMENTS.md`
- 2026-08-04 · ingest · T5 de la biblia, primera pasada, y con ella las cinco tandas tienen su primera vuelta: `docs/flujos/lectura-bi.md` con 6 escenarios. El aislamiento de portafolio de BI está bien hecho —`BiProjectScope::resolve()` rechaza la petición entera si algún proyecto pedido no está autorizado, en vez de filtrar en silencio, y exige `lps.indicadores.ver` además de la membresía—. Hallazgo: el filtro «proyecto cerrado pero visible para la jefatura» está escrito en tres sitios con tres criterios distintos, y solo BI incluye el alias legado `'P'`, así que una cuenta con ese rol ve en la Torre de Control un proyecto que el selector le esconde · `docs/flujos/lectura-bi.md`, `docs/EXPERIMENTS.md`
- 2026-08-04 · ingest · El piso tactil de 44px estaba caido en el rail colapsado del sidebar y nadie lo habia reportado: los 13 items median `43,5 x 44`, y el medio pixel no era redondeo sino min-content (icono 17,5 + padding del estado normal), con el `nav` dejando 38px utiles dentro de un carril de 4rem — el piso era inalcanzable por geometria. Arreglado en `navigation.css` con `padding-inline` de `--ds-space-2` y `min-inline-size: var(--ds-target-min)`. Al ponerse verde esa familia, la suite avanzo y destapo dos cosas mas en `data-display`: `lab-entrypoint.css` no importaba `table-filter-trigger.css` (el laboratorio retrataba un boton desnudo de 4x4) y el gatillo medía 13px, por debajo del suelo de 24x24 que la propia excepcion de [[DESIGN]] §5 bis declara innegociable. El test aplicaba 44 plano a todas las familias sin conocer esa excepcion; ahora la codifica acotada al gatillo canonico, y se comprobo mutando el token a 23px que el piso nuevo muerde. Goldens de PG, PI y `data-display` regenerados con autorizacion explicita: la cabecera crecio ~10px · [[rail-colapsado-no-llega-a-44]], [[piso-44-no-aplica-a-tablas-densas]], [[lab-desktop-layout-suite]]
- 2026-08-04 · ingest · El usuario deprecó el PDC v1 (`/pdc`, `/api/pdc/*`, y los módulos «Listado de Actividades» y «Contratos» que cuelgan de esas rutas), y la biblia se rehízo en consecuencia: **la T3 escrita horas antes describía v1 entera**, así que se retiró y se escribió desde cero `docs/flujos/compras-v2.md` sobre lo vivo —`/plan-compras`, 70 rutas, nueve `PlanCompras*Controller`, SPA en `pdc-app/`—. La distinción no era obvia: v2 no usa ni una ruta `/api/pdc/*`. Dos datos que salieron al mapearlo: **los nueve controladores de v2 validan CSRF**, justo lo contrario de los seis del hallazgo de soporte, y v2 autoriza con `RbacService::can()`, un **quinto** mecanismo. El hallazgo de `AGENTS.md:23` se cierra como «no se arregla» por deprecación, pero queda anotado que la frase de `AGENTS.md` seguirá siendo falsa aunque el código desaparezca. Lo que **no** se cierra: la acotación por `subpaquete_id` de `PlanFechasService`, que es de v2 y sobrevive · `docs/flujos/compras-v2.md`, `docs/flujos/soporte.md`, `docs/EXPERIMENTS.md`
- 2026-08-04 · ingest · **El PDC v1 se eliminó, no solo se deprecó**: por decisión explícita del usuario se borraron código *y* datos. Se fueron 5 controladores (`PdcController`, `PdcApiController`, `PdcAutoGenerateController`, `PdcPlantillaController`, `SemiAutoController`), 2 servicios (`SemiAutoService` de 3.282 líneas, `SemiAutoAssistantService`), 4 helpers de `Support/`, `_pdc_functions.php`, la vista, `hot.js`, `pdc.css`, `semi_auto_review.js`, `info_general_nav.js`, 25 rutas y ~45 pruebas; y **18 tablas** (`pdc`, `papelera_pdc`, `actividades`, `actividad_programa_fuentes`, `auto_contrato_log`, `contratos_trazabilidad`, las 9 `semi_auto_*` y 3 `general_pdc_*`), con respaldo previo verificado de 95 MB. Lo que costó tiempo y conviene recordar: **el nombre no dice la versión**. `src/Services/Pdc/` es v2, no v1; `lps.pdc.ver` y `lps.pdc.editar` los usa v2; y seis tablas `general_pdc_*` del catálogo de familias las consume el **panel Admin** (`FamilyCatalogController`), más `general_informe_pdc`/`general_curvas_pdc` que son del motor de informes y `bi_pdc_general` que es de la Torre de Control — por eso sobreviven. La clasificación se hizo leyendo quién consulta cada tabla, no por el prefijo. `AGENTS.md:23`, que la sesión anterior anotó como «seguirá siendo falsa», ya se corrigió · [[listado-de-actividades]], [[contratos]], [[plan-de-compras]], `AGENTS.md`
- 2026-08-04 · ingest · Limpieza del rastro muerto que dejó el borrado del PDC v1. Lo que se aprendió al medirlo, y no era obvio: de los **23 ids** que las cuatro cadenas de selectores de `maestroPermisos` ocultaban por rol en `cargarDatosGeneralesPagina2.js`, **solo dos siguen teniendo productor** (`btn_cargarCronogramaExcel` y `btn_eliminarActualizacion`, ambos en Actualizar Cronograma); los otros 21 los creaba el nav legado —la barra superior borrada en 42ba76c e `info_general_nav.js` borrado con el v1—. Se comprobó en runtime, no solo por grep: los cinco ids centrales devuelven `null` en la página servida. Dos trampas evitadas: `btn_Actividades` **sigue vivo** en Programación Semanal (el v1 usaba ese mismo id, así que un borrado por nombre lo habría roto), y `applyProjectTypeVisibility` parecía enteramente muerta pero su asignación de `window.__PROJECT_AREA__` la consumen `programacion_semanal/hot.js` y `legacyCards.js` — se conservó esa línea y se retiró el resto. **Lo que NO se pudo hacer:** retirar el adaptador `semi-auto-review.css` y su especimen, porque `operational-fixtures.php` se renderiza *dentro* de cada `<section data-family>` y el golden de la familia `actions` retrata el panel completo: quitarlo mueve dos goldens y eso exige aprobación visual explícita según [[AGENTS]] · `public/js/cargarDatosGeneralesPagina2.js`, `public/css/styles.css`, `public/css/design-system/adapters/legacy-bridge.css`
- 2026-08-04 · ingest · Cerrado el último resto del PDC v1 con **aprobación visual explícita del usuario**: se retiró el adaptador `semi-auto-review.css` (995 líneas, todas muertas — sus clases `.sar-*` solo las emitía el JS borrado) junto con su contrato operativo del laboratorio, y se regeneraron los dos goldens de la familia `actions`. Hashes registrados: `1180x820` 7af07d9d→6ab179f4, `1440x900` 26598032→3c1d9ea7; ningún otro golden se movió y las 20 pruebas visuales del laboratorio pasan. El dato que obligaba a la aprobación: `operational-fixtures.php` se renderiza **dentro** de cada `<section data-family>`, así que el golden retrata el panel entero, y `semi-auto-review` era el **único** contrato operativo de `actions` — al quitarlo desaparece la sección completa, no solo un espécimen. Trampa aparte, cazada solo al correr la suite runtime (la estática no la ve): `design-system-lab.mjs:350` fijaba 13 destinos en el shell del laboratorio «espejo del shell real»; al salir del rail «Familias de Actividades» y «Paquetes de Contratación», el espejo real son 11. Correr únicamente `test:design-system:static` no basta cuando se toca un fixture del laboratorio · `public/css/design-system/adapters/`, `views/design-system/operational-fixtures.php`, `docs/design-system/manifests/laboratory.json`
- 2026-08-04 · veracidad · áreas revisadas: pdc, arquitectura, design-system, qa · 95 páginas barridas por `fuente:` inexistente, 2 derogadas. **Lo que el pase destapó y no era una nota, era código roto:** [[compras-migrado-shell-sidebar]] describía el matiz «23 rutas listadas, 21 migradas» de `tests/browser/shell-sidebar-rollout.mjs`; al ir a verificarlo, el harness seguía listando `/listado-actividades`, `/contratos` y `/pdc`, con `/pdc` aún dentro de `MIGRATED`, y **reventaba la ejecución entera** con un timeout esperando `[data-shell-pattern="sidebar"]` sobre un 404. Podado → 20 listadas, 20 migradas, **123/123 checks**. La lección: verificar una nota contra el código es también una prueba del código, y aquí el que estaba mal era el código. Derogada también [[semi-auto-solo-lo-usa-pdc]], cuya duda abierta —doce métodos `*Listado` sin ruta— se resolvió por retiro. Aparte, causa raíz del gate de diseño en rojo desde `ddfea348`: ese commit regeneró cinco goldens y **no actualizó los `sha256` de los manifiestos**; se sincronizaron y `design-system-contracts.mjs` volvió a PASS · [[compras-migrado-shell-sidebar]], [[semi-auto-solo-lo-usa-pdc]], [[pdc]], [[qa-y-gates]]
- 2026-08-05 · ingest · Revivido `tests/browser/preconstruccion-full-cycle.mjs`: de 0/21 a **21/21**. Llevaba roto porque `PC_PROJECT = 'Aeropuerto Regional PC'` no estaba sembrado en la base local, y el fixture agotaba 120 s esperando la tarjeta. La trampa de fondo: **existía `database/seeds/preconstruccion_global_seed.php` escrita a propósito para ese proyecto** (id 75, prefijo `da_aeropuerto_pc`, con las tres restricciones PC configurables), y `tests/browser/fixtures/projects.mjs` lo declara como proyecto distinto de «Preconstrucción Da Porto». Reapuntar el test al otro proyecto habría dejado la semilla huérfana y cambiado en silencio la cobertura: antes de tocar un fixture que no encuentra sus datos, buscar si hay semilla que nadie corrió. Tres pruebas seguían rojas tras sembrar, y las tres eran deuda ajena al PDC: dos miraban el nav legado (`#programa_general`, `.navbar-brand`) que ya no existe —reescritas contra el shell, verificadas en vivo— y una fijaba 5 opciones de `restriccion_pc_1` cuando el commit `41720ad7` del 2026-07-05 las bajó a 4 a propósito en backend, `programacion_intermedia/hot.js` y el texto de ayuda; el test nunca lo registró porque ya estaba muerto. De paso se migró el montaje de `/login` con contraseña a la puerta de servicio, como exige AGENTS.md · `tests/browser/preconstruccion-full-cycle.mjs`, `database/seeds/preconstruccion_global_seed.php`
- 2026-08-06 · veracidad · Cuarto pase, disparado por la alarma (87 commits desde el del 2026-08-04). Áreas revisadas: design-system, qa, lps, bi, proceso, más la repesca por antigüedad de docker, worktrees, deploy y datos · 64 páginas · 11 corregidas, 39 confirmadas con cita, 14 con afirmaciones no comprobables en lectura (exigen Playwright, Docker, base de datos o SSH; declaradas, no dadas por buenas). El grueso de las correcciones es deriva de inventario tras el borrado del PDC v1: [[hot-container-height-ownership]] hablaba de 6 módulos HOT y quedan 4, [[reset-legacy-pisa-adaptadores]] citaba como patrón un `semi-auto-review.css` que ya no existe, y [[pdc-legend-item-clase-compartida]] fijaba un conteo de líneas de `styles.css`. Tres cifras movidas: 38→39 excepciones que vencen en 1.1.0 ([[subir-la-version-del-ds-cobra-deudas]], también en su `resumen`), `.gitignore:121`→`:237` ([[tests-browser-allowlist]]) y la tolerancia visual 0.03→**0.002**, que el propio `playwright.config.mjs:25` justifica citando el hallazgo de [[gate-visual-tolerancia-enganosa]] — la nota describía como vigente una causa que ella misma ya había hecho corregir. [[pdc-e2e-sandbox]] exigía exportar `E2E_BASE_URL` a mano y `base-url.mjs` ya autodetecta el puerto del stack del worktree. **Lo que salvó el muestreo:** un frente delegado reportó que `/bi/*` no restringe a `G,S,SG,C` «ni en cliente ni en servidor», leyendo que `BiViewController` no menciona roles; verificado a mano, el corte sí existe pero a dos saltos — `BiProjectScope:89` descarta los proyectos cuyo rol no tenga `lps.indicadores.ver` y `RbacCatalog:302-338` no se lo da a esos cuatro, así que `resolve()` lanza y responde 403. Se precisó la cita en vez de derogar una protección real. Regenerado [[IMPLEMENTATION_PLAN_INVENTORY|Registro de trabajo]] (60→70 trabajos) y recontado [[estado]]: faltaban tres goals enteros y las cinco tandas de la biblia siguen diciendo «ABIERTO» con el trabajo hecho · [[design-system]], [[qa-y-gates]], [[rbac-y-rutas]], [[estado]]
- 2026-08-06 · ingest · Cierre de la sesión que destapó el changelog dormido del design system. Brainstorming con 4 decisiones del usuario (D1–D4): migrar `/proyectos` a primitivas `aia-*` pagando sus 15 excepciones verbatim; gates de activación a «al menos 1.0.0» (el hito 1.0.0 fue único); revisión una a una de las 23 excepciones legacy (pagar las huérfanas, re-vencer a 1.2.0 las que el puente móvil/vendor aún obliga); y arrancar solo después de la campaña dark mode. Quedaron commiteados spec, plan (7 tasks) y goal (`c2f2327e`) — el goal exigió su allowlist en `.gitignore`, como todos —, y la campaña recibió en su Task 31 un Step 6 de traspaso (`70640860`): al terminar, ella misma deja el chip de arranque, relevo en vez de temporizador. Verificado de paso que la deprecación del PDC V1 (aviso entre sesiones del 2026-08-04) no toca este goal: los nombres `pdc-*` de las excepciones del selector son clases compartidas que la migración sustituye · [[goals/cierre-version-1-1-0-design-system/goal|cierre-version-1-1-0-design-system]], [[subir-la-version-del-ds-cobra-deudas]], [[estado]], [[design-system]]
- 2026-08-06 · ingest · Cerrado DS-006 en `views/auth/`. AdminLTE 3.2 entraba desde jsDelivr con un `<link>` **sin capa** y por tanto ganaba a todas las capas en declaraciones normales: su `button { border-radius: 0 }` dejaba en radio 0 el botón del diálogo de sesión caducada. Se sirve ahora local y capado desde `entrypoints/attach-adminlte.css`, lo que obligó a moverlo de `VIEW_OWNED_VENDORS` a `VENDOR_ATTACHMENTS` + `STANDALONE_ATTACHMENTS` —las dos cosas son mutuamente excluyentes por el candado `view-owned-with-attachment`— y dejó a `adapters/sweetalert2.css` **sin un solo `!important`**. Segunda tanda: fuera también Google Fonts, el CDN de Font Awesome y el bundle de Bootstrap (carga muerta, cero `data-toggle`/modal/tooltip en las tres vistas), y jQuery a la copia local, que es la misma 3.6.0; auth queda con **cero peticiones externas**. **La lección que costó el rodeo, y no era de la app:** el diálogo parecía no cerrarse al pulsar OK, se reportó como fallo preexistente y no lo era — el panel Browser integrado tiene el reloj de animaciones congelado (`document.timeline.currentTime` en `0` tras 1778 ms reales), SweetAlert2 desmonta en `animationend` y ese evento no llega jamás. Descartar Bootstrap no bastaba; lo que cierra el caso es una animación de sonda propia que tampoco corre. Con Playwright cierra bien. Corregida de paso [[admin-adminlte-adaptador]], que daba el candado como veto al patrón · [[auth-capado-y-sin-red-externa]], [[panel-browser-no-anima]], [[admin-adminlte-adaptador]], [[design-system]]
- 2026-08-06 · ingest · Rehecho el golden `auth/login-dark-1180x820.png` con **aprobación visual explícita del usuario**: `d932f02b`→`0d58ad67` en `scenarios[0].sha256` de `auth.json`. Llevaba desactualizado desde `4437fcfa` —retrataba el logo viejo y el botón «Modo linen»— y el capado de AdminLTE no lo movió; son dos deudas distintas y se pagaron por separado. Se regeneró con el **mismo mecanismo que lo fijó** en 2026-07-22 (`DRYRUN_SURFACE=auth npx playwright test tests/browser/entrypoint-segmentation-dryrun.mjs`), no con una captura ad hoc: es lo único que garantiza mismo viewport, mismo `fullPage: false` y mismo estado de carga que el golden sustituido. La fase se nombró `golden-2026-08-06` para no pisar la evidencia del goal. Dato que la corrida regaló: las tres rutas dan ahora **cero violaciones de axe**, donde el dry-run original registraba `landmark-one-main` y `region`; y las 33 peticiones CSS son todas locales · [[auth-capado-y-sin-red-externa]], [[design-system]], [[qa-y-gates]]
- 2026-08-06 · ingest · Cierre formal de las cinco tandas de la biblia de flujos (`goals/biblia-t1..t5`): sus `goal.md` seguían en cabecera «ABIERTO» aunque el trabajo de la primera pasada llevaba dos días hecho y verde (12 documentos en `docs/flujos/`, pruebas E2E en verde por tanda, hallazgos registrados en `docs/EXPERIMENTS.md` sin arreglar). Verificado contra el código antes de cerrar: los documentos, las suites de `e2e/tests/biblia/` y las líneas de este mismo log del 2026-08-04 confirman las siete/ocho condiciones de hecho de cada goal. Se añadió sección «Cierre formal» a los cinco siguiendo el patrón de `goals/pdc-preparar-b1/goal.md`, y se movieron sus cinco filas de «Abiertos» a «Cerrados» en [[estado]] · [[goals/biblia-t1-transversal/goal|biblia-t1-transversal]], [[goals/biblia-t2-cascada-lps/goal|biblia-t2-cascada-lps]], [[goals/biblia-t3-pdc/goal|biblia-t3-pdc]], [[goals/biblia-t4-soporte/goal|biblia-t4-soporte]], [[goals/biblia-t5-lectura/goal|biblia-t5-lectura]], [[estado]]
- 2026-08-06 · ingest · Dos aprendizajes de la campaña de cierre de hallazgos de seguridad de la biblia (spec y plan del 2026-08-06). Primero, **una regla que solo vive en el cliente no es una regla**: cerrada la familia completa del patrón —CSRF ausente en 6 módulos (`88ba6e0d`+`ca642189`), `sanear` sin CSRF (`32cccddf`), el candado de semanas pasadas del Programa General que solo existía en JS (`23d27bb7`) y `/indicadores` que ocultaba el informe solo en el navegador mientras la URL de Power BI viajaba en el HTML (`4b1a2be0`, deroga [[indicadores-oculta-en-cliente-bi-en-servidor]]). La trampa reutilizable queda en [[regla-solo-en-cliente-no-es-regla]]: una capacidad declarada en `RbacManager` y en `rbac_capabilities.js` puede no tener ningún consumidor en PHP. Segundo, en [[test-nuevo-rompe-en-silencio-suites-viejas]]: al blindar CSRF, `tests/browser/support/session.mjs` solo adjuntaba el token a dos prefijos de API y dos tests comparaban el mensaje de error contra una subcadena que el servidor nunca decía (`'CSRF'` vs `'Token de seguridad inválido'`; `'pasado'` vs `'pasadas'`), así que aprobaban siempre sin cubrir nada; corregido en `ca411b65` · [[regla-solo-en-cliente-no-es-regla]], [[test-nuevo-rompe-en-silencio-suites-viejas]], [[indicadores-oculta-en-cliente-bi-en-servidor]], [[rbac-y-rutas]], [[qa-y-gates]]
- 2026-08-06 · ingest · Cerrado [[goals/pg-chip-de-estado/goal|pg-chip-de-estado]] **sin ejecución propia**: el goal se abrió el 2026-08-03 con diseño aprobado porque *Actividad Futura* y *En Curso* se veían idénticas en la grilla, y lo resolvió por otra vía `51ccd5ca` («paridad del chip de estado en PG, PI y PS, medida en píxeles») de la campaña de cierre de dark mode. Verificado antes de cerrar, no asumido: `public/js/modules/programa_general/hot.js:1658` pinta el `<span class="ops-state-chip">` con `data-aia-hue` (línea 804), `npx playwright test tests/browser/programa-general-state-hue.mjs` pasa («cada matiz declarado se pinta distinto y legible»), y en pantalla los siete estados se distinguen. Caso de un goal que envejece abierto mientras otra campaña cumple su objetivo: el registro del goal no se entera solo · [[estado]], [[design-system]]
- 2026-08-06 · ingest · Medida la tolerancia de los goldens visuales, que figuraba como pendiente («0,002 sigue demasiado holgado en el resto de la app»). El resultado **contradice el comentario del propio `playwright.config.mjs:22-24`**, que afirma que con el piso en `0` tres corridas seguidas no producen ni un píxel de diferencia: bajándolo a `0` y corriendo dos veces `design-system-lab.visual.mjs` + `lps-drawer-design-system.mjs`, dos escenarios difieren sin tocar nada — `data-display-dark-1180x820` con 141 px (ratio 0,000146) y `shell-navigation-dark-1440x900` con 1720 px (ratio 0,001327). El piso vigente deja 1,5× de holgura sobre ese peor caso, así que apretarlo produce falsos rojos en vez de rigor: el pendiente **no era accionable como estaba escrito**, y la deuda real es el no determinismo de esos dos escenarios. Se dejó `0,002` intacto y se corrigió [[gate-visual-tolerancia-enganosa]] con las cifras · [[gate-visual-tolerancia-enganosa]], [[qa-y-gates]], [[design-system]]
- 2026-08-07 · ingest · **Cerrada la campaña de cierre de dark mode** (Steps 5 y 6 de su Task 31, lo único que le faltaba). El trabajo estaba hecho desde el 2026-08-05; lo que faltaba era declararlo. Disposición verificada una a una contra `git log`, no contra el plan: **36 tareas ejecutadas, 2 retiradas por la deprecación del PDC V1 (17 y 25), 0 pendientes** de 38 —las 37 numeradas más la 15b, de donde sale el denominador—. **La lección de proceso, que ya costó dos informes equivocados:** las casillas `- [ ]` del plan **nunca se marcaron**, mientras la prosa del mismo documento declaraba tareas «HECHA»; el informe `docs/reportes/estado-desarrollo.html` leyó las casillas y publicó 34/38 cuando la cifra real era 38/38. Un plan largo tiene dos relatos y sólo uno es cierto: **el commit**. La disposición se escribió como sección de cierre al final del plan en vez de marcar 200 casillas retroactivamente, porque marcarlas sin haber presenciado cada paso es fabricar evidencia. **Trampa nueva, y muerde a cualquier sesión en worktree:** `npm run test:design-system:static` daba 7/8 con `node-tests` en rojo por mtimes, y no era regresión — `docker-compose.yml:1` fija `name: last-planner-aia`, así que `runPhpInApp()` de `foundation.test.mjs` hace `compose exec` **al contenedor del worktree principal**, que monta otro árbol, y el PHP compara mtimes ajenos. Con `COMPOSE_PROJECT_NAME` propio: **8/8**. Traspaso dejado explícito y verificable en `goals/cierre-version-1-1-0-design-system/goal.md`, cuya precondición dura D4 era justamente este cierre · [[suite-estatica-miente-en-worktree-secundario]], [[goals/cierre-version-1-1-0-design-system/goal|cierre-version-1-1-0-design-system]], [[estado]], [[design-system]], [[qa-y-gates]]
- 2026-08-07 · ingest · Resueltos los cuatro cabos que dejó el cierre de la campaña dark mode, y el hallazgo de fondo no es ninguno de ellos: **una lista de pendientes que no se relee es ella misma una forma de deuda**. De las **once** entradas de `docs/DESIGN-AUDIT.md` §Pendiente de decisión, **cinco ya estaban resueltas** y seguían pidiendo criterio del usuario: las 9 excepciones de a11y de `.pdc-header` estaban retiradas (cero entradas con prefijo `programa-general`, `accessibility.test.mjs` 15/15 afirmando la ausencia), los dos goldens «ciegos» estaban sembrados desde `1a4c0e9f`, el gate de emparejamiento estaba en verde por `cc1b03aa`, el agujero de `applySharedConstraints` lo cerró `02670bfb` en servidor, y el rojo de `foundation.test.mjs` nunca fue del código. **Dos de esas cinco habrían generado trabajo repetido:** el usuario autorizó sembrar los goldens y mover sus líneas base sin saber que ya estaban sembrados — mirar antes de ejecutar ahorró rehacer dos retratos aprobados. Otras dos se ejecutaron (`221cae22`): la evidencia huérfana de `programa-general-actualizar` y el hex `#007bff` del toast, que **no se tokenizó sino que se borró entero** — al ir a comprobar si la regla seguía viva resultó que **nadie emite `.pdc-toast`** desde que `a3fee1fb` se llevó los dos archivos del PDC v1 que la duplicaban sin capa, así que no había color que decidir sobre un toast que no se pinta; `check:frontend` idéntico antes y después (859/2610/397). Dos se decidieron sin código (la frase provisional de C-33 se queda; las tres auditorías de la Task 27 entran por el cierre de la 1.1.0 en vez de aplicarse sueltas sobre CSS compartido con otra sesión en marcha). Quedan dos, ninguna deuda de la campaña. Registrada además la **Task 38**, ejecutada en `7ff39b54`/`02670bfb` y **nunca dada de alta en el plan**: sin cabecera `### Task N` no entraba en ninguna cuenta · [[design-system]], [[qa-y-gates]], [[regla-solo-en-cliente-no-es-regla]]
- 2026-08-07 · ingest · Publicada la **versión 1.1.0 del design system** (`a5223a0c`), cerrando [[goals/cierre-version-1-1-0-design-system/goal|cierre-version-1-1-0-design-system]]. De las 39 excepciones que vencían en ella: **7 pagadas y 32 re-vencidas a `1.2.0`**, cada una con su medición. **El spec estaba equivocado en su decisión D1:** describía las 15 excepciones de `theme-overrides.css` como «copias verbatim del selector de proyecto» y ordenaba pagarlas migrando `/proyectos`; medido, son normalizaciones **globales** de `border-radius` del propio DS —idénticas byte a byte a los bloques del agregador `aia-design-system.css`, que es a lo que se refería el «verbatim»— y `/proyectos` ni siquiera carga la mayoría. Migrar esa pantalla habría pagado **cero**, así que el grupo se trató con la regla de D3 (medir el opresor) en vez de con una migración especulativa. Método que decidió cada veredicto: degradar en vivo cada `!important` a normal y comparar el `border-radius` computado de todo el DOM. Pagadas las que no movieron un píxel (2 reglas duplicadas del buscador, el hex de `--primary` → `--ds-color-accent-legacy-dark`, y las capas `states`/`responsive` fuera de la escalera canónica); re-vencidas las que hacían caer el radio a valores heredados (4px / 11.52px / 15.2px). **Trampa de medición que casi cuesta un pago falso:** varios adaptadores llegan **duplicados** al documento, así que degradar una copia deja la otra en `!important` y el «sin efecto» no prueba nada — las 21 de Handsontable se re-vencieron con evidencia estática (226 `!important` vivos en el puente y la piel, y `#hot-container td` ganando por especificidad al selector del adaptador aunque nadie usara `!important`). Ampliada [[subir-la-version-del-ds-cobra-deudas]]: los frentes no eran tres sino **seis** · [[subir-la-version-del-ds-cobra-deudas]], [[excepciones-registradas]], [[madurez-y-api-estable]], [[design-system]], [[estado]]
- 2026-08-07 · veracidad · Quinto pase. Áreas revisadas: design-system y qa (las que movieron los commits desde el pase del 2026-08-06) · 12 páginas verificadas afirmación por afirmación, 6 corregidas. **La premisa con la que se pidió el pase era falsa y verificarla fue lo primero:** no se publicó la 1.1.0 — `docs/design-system/version.json` sigue en `1.0.0 / stable`, no hay commit sobre ese archivo desde `58b850e7` (16 de julio), y ninguna página de la wiki la daba por viva; las que la nombran hablan de deudas que *vencerían* en ella. La trampa `version-escrita-a-mano-rompe-el-bump` no existe y no se creó: no hay hecho que la sostenga. Lo que sí cambió y la wiki no registraba: **la suite estática corre verde en sus ocho gates** y ya no es la cadena `&&` que cortaba en el primer rojo (`scripts/design-system-static-suite.mjs:2-4,26-36`), así que [[branch-preexisting-red-gates]] daba por tolerados dos rojos que no existen y mandaba correr a mano tres gates que ya corren solos; biome, en cambio, empeoró de 879 a **859 errores + 2.610 avisos** —sigue sin ser un gate verde—. La asimetría de anclas que describía [[excepciones-registradas]] se cerró: `state-tint-exceptions.json` va por la `version` 2.0.0 y su guard importa el mismo `state-token-locator.mjs` que el hermano. Y [[guard-valida-declaracion-contra-si-misma]] ilustraba su lección con una divergencia ya pagada por `51ccd5ca` (el chip emite `data-aia-hue` y `legacy-bridge.css:114-127` da fondos distintos a `blue` y `green`) — se conserva la trampa, que es del guard, y se corrige el ejemplo; matiz nuevo: la clase `ds-cell-ok` que produce `cell-state-vocabulary.mjs` no la declara ninguna hoja. Registrado en el mapa **DS-032** (2026-08-07): `390x844` vuelve a ser viewport soportado y no requerido, `SUPPORTED_VIEWPORTS`/`REQUIRED_VIEWPORTS` en `design-system-contracts.mjs:158-159`, sin evidencia móvil todavía. Corregido que `docs/qa/workflows.md` «no viaja en git»: sí viaja. Regenerado [[IMPLEMENTATION_PLAN_INVENTORY|Registro de trabajo]] (70→75). **Dos defectos de las fuentes, anotados sin tocarlas:** `CHANGELOG.md:6` dice 38 excepciones donde `exceptions.json` tiene 39, y `state-token-exceptions.json:3` sigue justificando `out-of-scope-mobile` con una prohibición de `AGENTS.md` retirada el 2026-08-07 · [[design-system]], [[qa-y-gates]], [[branch-preexisting-red-gates]], [[visual-baselines-estado-real]], [[excepciones-registradas]], [[baselines-y-presupuestos]], [[guard-valida-declaracion-contra-si-misma]], [[IMPLEMENTATION_PLAN_INVENTORY|Registro de trabajo]]
- 2026-08-07 · ingest · Arreglado el caso «mobile critical navigation and render» de `tests/browser/full-app-flow.spec.mjs` (`a8b8b3e0`): buscaba `#drawerToggle` y `#aiaNavbar`, del navbar superior legacy que retiró el shell de sidebar y que no existen en ninguna vista —el único resto vivo es `public/js/modules/aia_ui/nav_drawer.js`, que se carga desde `linksComunesHead2.js:61` y sale en falso por falta de `.navbar-collapse-drawer`. Verificado antes de tocar el test que **no falta un cajón**: a viewport estrecho el rail colapsado ES la navegación (4rem fijos, 11 destinos visibles con caja de 44 px, body desplazado ese mismo ancho, `scrollWidth == innerWidth`), así que el caso pasa a afirmar eso. 13/13 en verde en el stack CI aislado. El rodeo del día engordó [[panel-browser-no-anima]] con su segunda cara: el rail «no se ensanchaba» al expandir y se reportó como defecto del shell, pero era la misma trampa sin ningún callback de por medio — una transición congelada devuelve el valor **inicial** de la propiedad y, por ganar sobre el estilo inline, hace buscar un `!important` que no existe · [[panel-browser-no-anima]], [[qa-y-gates]]
- 2026-08-07 · ingest · Los tres hallazgos más graves de la biblia estaban ya arreglados por otra sesión, así que la biblia se pone al día en vez de duplicar el trabajo: `sanear` entró en la lista CSRF de Programación Semanal (`32cccddf`), los seis módulos de soporte y aprendizaje validan token (`88ba6e0d`, `ca642189`) y el candado de semanas pasadas del Programa General existe ahora en el servidor (`23d27bb7`). Verificado antes de escribirlo que las defensas **se llaman** y no solo se declaran —`assertNotPastWeekOrPrivileged()` está cableado en los tres endpoints que mutan (`:165`, `:403`, `:1200`)—, que es justo el fallo que tenían antes. Suite de la biblia 17/17 en verde contra el código corregido. Nace el escenario [[docs/flujos/lps-programa-general|PG-006]] con la regla ya real, y las notas de PS-002 y SOP-002 conservan la lección en vez de borrarse: dos listas paralelas que deben decir lo mismo sin que nada lo obligue, y una defensa que existía en el mismo archivo sin que nadie la llamara. Sigue abierto `canDeleteRows`, sin consumidor y protegiendo algo destructivo · `docs/flujos/`, `docs/EXPERIMENTS.md`
- 2026-08-10 · veracidad · Sexto pase, cuatro verificadores en paralelo por área (design-system 44 páginas, qa+proceso 34, pdc/datos/bi/docker/worktrees/deploy/admin 35, arquitectura+rbac+lps 52), disparado por el Frente 0 de higiene y decisiones · 165 páginas revisadas, **14 hallazgos, 2 graves**. Los dos graves: [[madurez-y-api-estable]] y [[manifiesto-de-modulo]] prometían que el gate de gobernanza era «verificable, no declarativa» cuando `release-governance.test.mjs:68-74` solo comprueba `evidence.length > 0` sin mirar el contenido — los 14 recibos de release medidos horas antes en la Task 6 eran stubs; y [[baselines-y-presupuestos]] citaba «cinco legacy» en el baseline de PHPStan del design system cuando hoy tolera cero, retiradas ese mismo día en `9011c99c`. El resto: citas corridas por commits posteriores ([[homologacion-y-familias]], [[semanal-auto-dispara-mutaciones]], [[pdc-legend-item-clase-compartida]], `indicadores.md`), una regla que DS-032 volvió parcial ([[inspiracion-apple-en-dark-aia]]), una narrativa dos versiones atrás ([[changelog-ds-encabeza-version-vieja]]), una afirmación falsa heredada de `CLAUDE.md` ya corregida ([[qa-y-gates]] sobre `phpstan.neon`), dos cifras que el retiro del PDC v1 movió un 24 % ([[suite-php-rojos-preexistentes]], [[tests-browser-allowlist]]), cuentas de `.env` que cambiaron ([[dev-door-acceso-local]], y `AGENTS.md` con ella, por precedencia), y el mapa de PDC dando por vivas rutas que ya no existen ([[pdc]]). El hallazgo más caro: los bloques `<!-- generado:inicio -->` de [[contratos]] y [[listado-de-actividades]] llevaban desde el 2026-08-04 listando rutas, controladores y tablas del PDC v1 borrado, con el banner de derogación en rojo justo encima — `wiki-arquitectura.mjs --escribir` no los toca porque el módulo ya no está declarado en `scripts/wiki-arquitectura.modulos.mjs`, así que se vaciaron a mano siguiendo el patrón de `panel-admin.md`. Nada de esto se preguntó por sospecha: los cuatro verificadores leyeron código, no wiki, y citaron archivo:línea de cada corrección · 15 páginas corregidas, 2 vaciadas a mano
- 2026-08-10 · ingest · Cierre de la Task 8 del Frente 0. Tres trampas nuevas: [[gate-solo-cuenta-elementos-no-los-lee]] (el gate de gobernanza cuenta elementos de un array en vez de inspeccionar su contenido — misma familia que [[guard-valida-declaracion-contra-si-misma]]), [[condicion-de-hecho-caduca-sin-aviso]] (dos goals, `bi-control-tower-gemini` y `design-system-nucleo-gobernanza`, llevaban semanas bloqueados por una condición que ya no se podía cumplir — un tema retirado y evidencia anterior a lo que avalaba, medido el mismo día en las Tasks 5 y 6) y [[reabrir-semana-asimetria-cliente-servidor]] (el cliente esconde el botón de reabrir salvo al rol `A`, el servidor solo exige una capacidad de edición genérica, y el log siempre escribe «reabierta por Admin» — no estaba documentada en ningún sitio de la wiki, medido en la Task 3). [[estado|estado.md]] puesto al día: de 24 a **26 carpetas** de goal (sumadas `adopcion-logo-construccion`, cerrada por evidencia de commit pero sin excepción en `.gitignore`, y `reapertura-movil-y-tema-claro`, abierta con tres fases cerradas), corregidas las causas de bloqueo de los dos goals colgando y cerrado formalmente `cierre-version-1-1-0-design-system` desde el pase anterior. `docs/reportes/estado-desarrollo.html` **no se tocó**: su generación no está automatizada (sin script ni entrada en `package.json`) y editarlo a mano habría sido peor que dejarlo con su corte del 2026-08-07 declarado · [[estado]], [[design-system]], [[qa-y-gates]], [[rbac-y-rutas]], [[lps-dominio]], `AGENTS.md`, 3 trampas nuevas
- 2026-08-10 · ingest · Publicado el gate de paridad RBAC (`4ba845dc`, `npm run test:rbac-parity`) **a sabiendas de que sale en rojo**, por decisión explícita del usuario al cerrar el día: el gate es nuevo y su cometido es justo destapar la divergencia, que existía en el código desde antes — el gate no la creó, la hizo visible. Reporta dos capacidades del rol **R** con el servidor concediendo y el cliente escondiendo: `canManageContracts` y su alias `canManagePdC` (`RbacManager.php:28,48`). Verificado antes de publicar que **no son vestigios** del PDC v1 borrado, que era la lectura cómoda: `/plan-compras` (PDC v2) está vivo en `public/index.php:141`, así que la divergencia es sobre un módulo en uso. Tercer caso de la misma familia tras los cuatro del 2026-08-06 y [[reabrir-semana-asimetria-cliente-servidor]]; su cierre es del Frente 1A. Se descartó la opción de declararlas en `docs/rbac-parity-exceptions.json` para dejar el gate verde, porque eso convierte un hallazgo vivo en excepción documentada — el aviso queda escrito en la propia página para quien lo encuentre rojo y lo tome por regresión propia. **Del cierre en sí, la parte que importa es el paso 5:** re-verificado *después* de integrar `fc6ad8ca` de otra sesión, no antes — mismas dos divergencias, ninguna nueva, y `npm run test:wiki` en verde con 104 páginas y 0 hallazgos; solo entonces se publicó (`fce730c7`). Añadido también `openspec/` al `.gitignore` (`231bfd5b`): OpenSpec 1.8.0 + esquema Superspec quedan instalados en el proyecto, pero sus artefactos son de flujo local, no contrato del repo · [[regla-solo-en-cliente-no-es-regla]], [[reabrir-semana-asimetria-cliente-servidor]], [[rbac-y-rutas]], [[qa-y-gates]]
- 2026-08-10 · ingest · Cerradas en `80e25c35` las dos divergencias que el gate de paridad RBAC destapó horas antes, y **el hallazgo no es la divergencia sino lo que apareció al medirla**: `canManageContracts` y su alias `canManagePdC` **no tienen un solo consumidor** — ni en `src`, ni en `admin/src`, ni en `public/js`, ni en `pdc-app/src`, ni en `views`, solo sus dos declaraciones—, y `/plan-compras` no comprueba ninguna capacidad: exige `project_id` y pasa el rol al bootstrap de la SPA. Así que corrijo lo que yo mismo escribí en la línea anterior: la forma era la de [[reabrir-semana-asimetria-cliente-servidor]], pero la consecuencia no — allí el endpoint concedía de más y aquí no había nada explotable, solo una mina para el primero que cableara el permiso. La pregunta real estaba escrita sin resolver en el propio cliente desde su origen (`['A','D','OT']; // Asumiendo que… Ajustar si R necesita`) y **era de negocio, no de código**: la respondió el usuario —en obra el Residente también gestiona el Plan de Compras—, así que el servidor estaba bien y se alineó el cliente, en vez de la lectura cómoda de quitarle la `R` al servidor. Verificado: gate en verde, matrices idénticas para `R` y `OT` (permitidos) y `V` y `C` (denegados) medidas a la vez en PHP dentro del contenedor y en el JS real, y biome idéntico a HEAD (1 error / 19 avisos, el error es preexistente de la línea 27). Corregida la propia página, que ya contradecía al repo por decir «publicado en rojo a propósito» · [[regla-solo-en-cliente-no-es-regla]], [[reabrir-semana-asimetria-cliente-servidor]], [[rbac-y-rutas]], [[qa-y-gates]]
- 2026-08-10 · ingest · Retirada `canAutoDefineContracts` en `513449a1`, y el valor está en lo que **no** se borró. El encargo era «borra las capacidades muertas, nadie las usa»; el barrido completo del repo mostró que la premisa era cierta para el permiso y falsa para las herramientas: `scripts/wiki-arquitectura.modulos.mjs:103,123` nombra `canManageContracts` y `canManagePdC` y es el generador vivo de `memoria/arquitectura/`, así que borrarlas desincronizaba la wiki. Además ya existía decisión aprobada y contraria al borrado —RBAC-A en `docs/EXPERIMENTS.md:33`, ICE 200: «colapsar los cuatro alias a un solo nombre»— con su regla escrita en el plan del Frente 1A publicado ese mismo día (`:236`, gana el nombre con más consumidores). De las tres candidatas solo una estaba muerta de verdad: `canAutoDefineContracts`, cero referencias fuera de su declaración, ni siquiera declarada en el cliente. Se retiró solo esa y se corrigió el escenario RBAC-015 de `docs/flujos/transversal-rbac.md`, que el propio borrado dejaba describiendo algo inexistente. **La trampa reutilizable: «nadie lo usa» se verifica contra el repo entero, no contra el código de producción** — un alias sin consumidores en runtime puede tener consumidores en generadores, gates y documentos de contrato, y esos rompen igual. Verificado: `phpstan analyse src admin/src` sin errores, `test:rbac-parity` en verde, la capacidad ausente de la matriz medida en PHP dentro del contenedor, y `canManagePdC` intacta (`true` para R, `false` para V) · [[regla-solo-en-cliente-no-es-regla]], [[rbac-y-rutas]], [[qa-y-gates]]
- 2026-08-10 · ingest · Ejecutado RBAC-A (`docs/EXPERIMENTS.md:33`, ICE 200): los cuatro pares de capacidades que eran alias exactos se colapsaron a un solo nombre — `canManageGeneralProgram`, `canManageWeeklyProgram`, `canManageMediumTermProgram` y `canManagePdC` —, y `RbacManager` pasa de diecisiete capacidades a doce sin ningún alias. **Lo que importa no es el colapso sino cómo se eligió el superviviente.** La regla aprobada («gana el nombre con más consumidores») no bastaba: los cuatro pares quedaban en empate casi exacto —2 contra 2, 4 contra 2, 1 contra 2, 3 contra 2— y aplicarla al pie de la letra habría conservado `canEditWeeklyProgram`, que **no consume nadie en runtime**. Hubo que **pesar** los consumidores en vez de contarlos: un acierto en `scripts/wiki-arquitectura.modulos.mjs` es un generador de documentación y no vale lo que uno en `views/indicadores/indicadores.view.php`, que es HTML servido al usuario. Con ese criterio los tres pares de programación los gana `canManage*` por ser el único nombre que la vista de indicadores consulta; el cuarto no tiene consumidor de runtime en ningún lado y lo decide la semántica: «Contratos» es un módulo eliminado con el PDC v1 el 2026-08-04 y `/plan-compras` está vivo. Los cuatro convergen en `canManage*`, así que el vocabulario queda uniforme por accidente afortunado, no por diseño. Complemento del 2026-08-10 a [[regla-solo-en-cliente-no-es-regla]]: aquella página avisa de que declarar no es aplicar; esta añade que **al retirar un nombre duplicado hay que mirar quién lo consume y desde dónde, no cuántos lo consumen**. Verificado: `test:rbac-parity` en verde, `phpstan analyse src admin/src` sin errores, matrices de PHP y del JS real medidas a la vez para `R` (permitido, `true` en las cuatro) y `V` (denegado, `false` en las cuatro) y los cuatro nombres viejos ausentes de ambas, `test:wiki` sin hallazgos con 104 páginas, biome idéntico a HEAD (1 error preexistente de la línea 27), y `/indicadores` cargado en el navegador con `test.A` y `test.V` sin errores de consola — el único 403 es del informe externo de Power BI, ajeno al cambio · [[regla-solo-en-cliente-no-es-regla]], [[rbac-y-rutas]], [[qa-y-gates]]
- 2026-08-10 · ingest · La suite PHP ya tiene runner (`scripts/run-php-tests.php`) y el CI corre 71 tests donde antes corría **tres** listados a mano en `design-system.yml`; los otros ~96 no los ejecutaba nadie. Cada test declara `// @requiere: <nivel>` (`puro` 21, `db` 47, `http` 4, `datos-proyecto` 30) y un test **sin etiqueta rompe el runner**, que es lo que impide que uno nuevo vuelva a nacer fuera del CI. **El hallazgo que cambió el diseño no era el del encargo.** Se pedía tapar cinco tests sin `exit()` que «pasarían siempre»: al medirlos, cuatro señalan el fallo con `throw` y una excepción no capturada ya devuelve 255, así que el ciego era **uno solo** (`test_pi_shared_payload_smoke`, un volcado sin aserciones, ya convertido y comprobado que sabe fallar). Lo gordo estaba en otro sitio: cruzando dos pasadas por código de salida —sin entorno y con el stack de `docker-compose.ci.yml`— aparecen **26 tests que salen 0 cuando NO hay base de datos** y fallan con ella, porque capturan el fallo de conexión ([[test-sin-base-sale-verde]]). Por eso el runner comprueba el entorno **antes** de ejecutar y sale 2 sin correr nada si falta: la ausencia de entorno no es un verde. Etiquetar destapó tres más que abren la base sin nombrar `Database::getInstance` ni `new PDO` en su fuente, invisibles leyendo el código y cazados corriendo el nivel `puro` sin red. Los 28 que fallan con el fixture **no son código roto**: 20 piden datos que el CI no tiene (14 del PDC v2), 4 tablas que el fixture no crea y 4 evidencia de `docs/qa/evidence/` que no viaja en git; se declaran en su nivel en vez de silenciarse o de enriquecer el fixture. Verificado con salida real: `puro` 22/22 y `http` 71/71 en verde, 8 s frente a los <1 s de los tres de antes, y el guardarraíl comprobado creando un test sin etiqueta (sale 2 y lo nombra). El paso del job estático fija `-f docker-compose.yml` a propósito: sin eso el override monta el checkout y tapa el `vendor/` de la imagen, y el job falla — se vio reproduciendo el mount, no leyendo el YAML. Queda anotado un test inestable, `test_pdc_v2_torre_control_rbac`, que en lote falla unas veces y otras no por un `SELECT … LIMIT 1` sin `ORDER BY` · [[test-sin-base-sale-verde]], [[qa-y-gates]], [[suite-php-rojos-preexistentes]]
- 2026-08-10 · ingest · Auditoría cruzada entre la coordinadora y la sesión del Frente 1: dos gates dieron verde y rojo a la vez sobre el mismo commit, y las dos medidas eran correctas sobre su propio árbol. El lint de la wiki resolvía un enlace a una carpeta ignorada por git que solo existe en el disco del coordinador; la suite estática lee fechas con Node en el worktree y ejecuta PHP en el contenedor del árbol principal, porque `docker-compose.yml:1` fija el nombre de proyecto. Escrita la lección · [[un-verde-solo-vale-para-el-arbol-donde-se-midio]], `docs/EXPERIMENTS.md`
- 2026-08-10 · ingest · Dos sesiones leyeron mal un codigo de salida el mismo dia y por la misma causa: el coordinador miro `$?` despues de una tuberia con `tail`, y la sesion de CI encadeno un `git push` detras de un `echo` que no puede fallar, asi que publico pese a una verificacion en 2. El arbol resulto sano, pero el paso de verificacion no goberno la publicacion, que es su unico trabajo · [[el-codigo-de-salida-se-pierde-en-la-tuberia]]
- 2026-08-10 · ingest · El frente del runner de tests PHP **publicó con la suite estática en rojo**, y lo destapó la sesión coordinadora auditando `main`, no quien hizo el trabajo. `tests/design-system/visual-ci-contract.test.mjs:156` exige que `.github/workflows/design-system.yml` contenga literalmente `php tests/test_global_table_safety.php`; al sustituir los tres tests listados a mano por el runner esa cadena desapareció, y el gate `node-tests` se puso rojo **por un cambio que subió la cobertura de 3 pruebas a 71**. Verificado el rojo antes de tocar nada, en vez de fiarse del aviso. Arreglado conservando el paso explícito además del runner (corre dos veces, <1 s) y **sin tocar el contrato**: cambiar una aserción ajena para que el trabajo propio dé verde es lo que `AGENTS.md` prohíbe, así que la mejora —que la aserción compruebe que el CI *ejercita* el test en vez de que lo *nombra*— queda encolada como `D-CI-1`. La trampa reutilizable está en [[el-archivo-que-tocas-puede-tener-un-contrato]]: quien hizo el cambio verificó lo suyo a conciencia —runner, los dos comandos del CI, códigos de salida— y aun así publicó en rojo, porque **el contrato que vigilaba el archivo editado no estaba en su lista**; la regla que faltaba no es correr todos los gates por si acaso, sino averiguar con un `grep` si algo aserta el archivo antes de editarlo. Corolario del mismo día: el primer intento de arreglo salió rojo en `node-tests` **y** en `contracts`, ambos con `activation: worktree and index must be clean` — no eran dos fallos sino uno, el arreglo sin commitear, disfrazado de dos. Re-verificado después de integrar `113a964e`: los **8 gates en verde**, runner `--nivel=http` 71/71, paso explícito rc=0 · [[el-archivo-que-tocas-puede-tener-un-contrato]], [[qa-y-gates]], [[test-sin-base-sale-verde]]
- 2026-08-10 · ingest · Segundo caso del mismo dia y de la misma familia, visto por el coordinador: `docs/design-system/manifests/goal-provenance.json` fija por `sha256` el `goal.md` y el `facts.md` del goal de gobernanza, y una tarea los edito con autorizacion del usuario sin actualizarlo. La suite cayo a 6/8 y **ningun subagente lo detecto** — lo encontro la verificacion final. Confirma que la regla del `grep` previo no es del CI, sino general · [[el-archivo-que-tocas-puede-tener-un-contrato]]
- 2026-08-11 · veracidad · Séptimo pase, acotado a las áreas que movieron los 97 commits desde el sexto: **rbac+lps (27 páginas) y qa+proceso (34)**, con dos verificadores de solo lectura en paralelo sobre `123a8bff`. Disparado por la alarma del lint, no por acordarse. **4 hallazgos, ninguno grave.** rbac/lps salió limpia —el pase del 2026-08-10 ya la había cubierto— salvo una caducidad que el verificador olió y no cerró: [[reabrir-semana-asimetria-cliente-servidor]] seguía en `estado: vigente` describiendo un agujero que `6dcec299` cerró el mismo día que se escribió la página; queda **derogada** conservando la lección, que no depende del caso —el cliente puede esconder, solo el servidor puede impedir—. Los otros tres son cifras corridas: la tabla de niveles de [[qa-y-gates]] decía 21/47/4/30 y suma 102 cuando el reparto real es **22/45/4/30 = 101**, y las cifras base de [[test-sin-base-sale-verde]] (99) y [[suite-php-rojos-preexistentes]] (96) quedaron viejas. El universo de tests se movió **tres veces en 24 h** —126 → 96 → 99 → 101— y cada cifra fue correcta al medirla, así que las páginas pasan a citar sha además de fecha y a traer el comando para re-medir en vez del número copiado · [[qa-y-gates]], [[reabrir-semana-asimetria-cliente-servidor]], [[test-sin-base-sale-verde]], [[suite-php-rojos-preexistentes]]
- 2026-08-11 · ingest · **Fase 2 del frente de testing: PHPUnit 12.5.33 entra como dependencia de desarrollo y convive con los 101 scripts, sin migrar ninguno.** Los tests nuevos van a `tests/unit/*Test.php` y declaran su entorno con `#[Group('<nivel>')]`; los viejos siguen con `// @requiere:`. El runner sigue siendo **la puerta única**: comprueba el entorno, corre los scripts del nivel y además invoca PHPUnit con los grupos que ese nivel selecciona, agregando ambos códigos de salida — se descartó el diseño de dos comandos porque obligaría a cada job del CI a acordarse de invocar los dos, que es el defecto que el runner vino a cerrar. **Lo que condicionó el diseño fue algo medido antes de escribir nada:** `docker/php/Dockerfile:36` instala con `--no-dev` y solo `docker-compose.ci.yml` lo sobrescribe, así que **la imagen del job estático no traía PHPUnit**; la respuesta a esa ausencia no podía ser «se salta» —sería el mismo falso verde de [[test-sin-base-sale-verde]]—, así que el runner sale 2 y el job estático pasa a construir con `--build-arg COMPOSER_INSTALL_FLAGS=""`, sin tocar las tres líneas que fija `visual-ci-contract.test.mjs:143-145`. Esta vez el `grep` de contratos se hizo **antes** de editar, que es la lección de [[el-archivo-que-tocas-puede-tener-un-contrato]], y por eso el workflow no volvió a romper nada. PHPUnit 11 no resuelve: `sebastian/diff` está fijado en 7.0.0 por el resto del ecosistema. El primer test cubre `RbacService::normalizeRole()` y sobre todo la regla de [[AGENTS]] «conserva solo lectura como fallback seguro»: **no comprueba que `DEFAULT_ROLE` valga una constante concreta —eso no demuestra nada— sino que ese rol no concede ni una capacidad de escritura**, medido antes de escribirlo (`C` tiene todo en `false` e `isReadOnly`, la única diferencia con `V` es `isExternal`). Verificado con salida real: `--nivel=puro` 22 scripts + 18 casos de PHPUnit rc=0, `--nivel=http` 71 scripts + PHPUnit rc=0, los 8 gates estáticos en verde, build del job estático 60 s, y **los tres guardarraíles nuevos puestos en rojo uno a uno y restaurados**: clase sin grupo → 2, test rojo → 1, binario ausente → 2. Queda en la cola `D-CI-2`: si los tests nuevos **deben** ser PHPUnit es regla de equipo, no decisión técnica · [[qa-y-gates]], [[test-sin-base-sale-verde]], [[el-archivo-que-tocas-puede-tener-un-contrato]]
- 2026-08-11 · ingest · Ejecutada la decisión **D-CI-1**: la aserción de `visual-ci-contract.test.mjs` deja de exigir que el workflow **nombre** `php tests/test_global_table_safety.php` y pasa a comprobar que el CI **la ejecuta**, cruzando el nivel que el workflow invoca (`run-php-tests.php --nivel=…`) con el que la prueba declara en su `// @requiere:`. Con eso se retira del workflow el paso explícito que la duplicaba, puesto ahí el día anterior solo para cumplir la cadena literal. **Lo que importa del cambio es que la aserción quedó más estricta, no más laxa**, y se demostró poniéndola en rojo por las tres vías que la versión vieja no detectaba: cambiarle el nivel a la prueba a uno que el CI no corre, quitarle la etiqueta, y quitar la invocación del runner del workflow — las tres dan rc=1, y restaurado vuelve a 0. Es el cierre del incidente de [[el-archivo-que-tocas-puede-tener-un-contrato]]: un gate que premiaba la forma hizo fallar un cambio que multiplicó por más de veinte la cobertura del CI, y la corrección no fue relajarlo sino apuntarlo al resultado · [[el-archivo-que-tocas-puede-tener-un-contrato]], [[qa-y-gates]]
- 2026-08-11 · ingest · Cerrado el hueco que la revisión de la coordinadora encontró en la aserción de `D-CI-1`, y **el hallazgo vale más que el arreglo**. La aserción nueva comparaba el nivel que el CI invoca con el que la prueba declara, y se había entregado con tres mutaciones en rojo —nivel cambiado, etiqueta quitada, invocación quitada—. Pero esa comparación **supone que el runner es acumulativo**, y nada lo comprobaba: cambiando los dos `<=` de `run-php-tests.php` por `===`, el contrato seguía en **RC=0** mientras `test_global_table_safety` **dejaba de correr en el CI**. `test_php_test_runner.php` sí se ponía rojo, pero de refilón —fallaba por la aserción de la base ausente, no por una escrita para esto—, así que un refactor se habría llevado la garantía sin que nadie lo notara. **La lección, en [[mutar-el-supuesto-no-solo-las-entradas]]:** quien escribió la aserción mutó *sus entradas*; el agujero estaba en el *supuesto*, una capa por debajo, y mutar entradas nunca destapa un supuesto falso. Cerrado con cuatro comprobaciones de acumulatividad de frente en `test_php_test_runner.php` (25 en total) y ampliando `--solo-listar` para que marque `[ejecuta]`/`[omite]` según el nivel pedido, lo que permite comprobarlo sin el entorno de ese nivel. Verificado: con la mutación puesta ahora falla por su propia razón —«pedir db ejecuta TAMBIEN el de nivel puro»— y restaurado vuelve a 0. El contrato lleva ahora un comentario que nombra dónde vive el supuesto, para que el siguiente no lo rompa a ciegas · [[mutar-el-supuesto-no-solo-las-entradas]], [[el-archivo-que-tocas-puede-tener-un-contrato]], [[qa-y-gates]]
- 2026-08-11 · ingest · Cerrado el ICE 400 que la coordinadora destapó auditando: `tests/test_php_test_runner.php` declaraba `// @requiere: puro` pero tres comprobaciones necesitaban el binario de PHPUnit, dependencia de desarrollo, así que en un contenedor `--no-dev` daba **RC=1 con cinco fallos** sobre el mismo commit donde en otra máquina daba 0. **El runner era más estricto que su propio test:** él aborta con 2 y explica que falta PHPUnit; el test fallaba con cinco aserciones que no mencionaban PHPUnit, como «un test etiquetado y verde devuelve 0». Al reproducirlo apareció la causa de fondo, peor que lo reportado: **tres de los cinco no tenían nada que ver con PHPUnit** — fallaban porque no pasaban `--dir-unit` y el runner miraba entonces el `tests/unit/` **real** del repositorio, así que el test estaba acoplado a un árbol vivo; y otras dos *pasaban por casualidad* en ese entorno, que es el mismo fallo en silencio. Arreglado aislando con un directorio vacío de fixtures todo lo que no va de PHPUnit, y declarando la precondición en las tres que sí: ahora sale **2** nombrando lo que no pudo comprobar y diciendo que las otras 22 pasaron pero que no ejecutar algo no es un verde — la misma regla del runner. Escrito en [[mutar-el-supuesto-no-solo-las-entradas]] como variante de al lado: **el test de un guardarraíl debe ser al menos tan estricto como el guardarraíl, y sus fixtures no deben tocar árboles vivos** · [[mutar-el-supuesto-no-solo-las-entradas]], [[qa-y-gates]]
- 2026-08-11 · ingest · Frente `focus-visible-verde` y, sobre todo, **dos trampas de método que casi convierten una medición en una conclusión falsa**. El encargo era aclarar el verde de `.lps-sidebar-trigger` para que el foco de teclado se viera mejor, y la premisa era falsa: ese verde **no es el indicador de foco**. El foco lo pinta el anillo teal del sistema (`outline` de `--ds-active-focus-ring` más `box-shadow`, en `design-system/adapters/lps-drawer.css:182`) y el verde es el **relleno**, con la regla compartida con `:hover`. Eso invierte el criterio: si el anillo lleva la señal, el relleno debe **apartarse** de él. Medidas las tres candidatas, anillo-vs-relleno: `--aia-green-primary` 4,34:1, el verde suelto de hoy 2,63:1, `--aia-green-medium` 1,86:1 — solo la primera pasa el 3:1, y es la que se aplicó, además sin reserva porque el token existe. **Las dos trampas valen más que el cambio:** medir `:focus-visible` con `element.focus()` desde la consola devuelve el estado de **reposo** en cuanto se cuela otra llamada de herramienta, y el síntoma es `:focus-visible` verdadero con `transform: none` — lo que funciona es dejar el foco en el control anterior y pulsar un `Tab` físico ([[medir-foco-de-teclado]]); y el `docker compose` del repo sirve el **árbol principal**, no el worktree, así que una captura «del después» enseña el archivo viejo y verifica un cambio que no es el tuyo ([[aislar-stack-docker-por-worktree]], ampliada con la consecuencia al verificar y una receta ligera de solo lectura). Queda anotado el coste que la decisión sí tiene: con este verde el relleno enfocado queda a 1,17:1 del de reposo, así que el **hover con ratón** se apoya en el `scale` y la sombra, no en el color; si alguien quiere señal cromática para el hover, se le da su propia regla, no se vuelve a aclarar la compartida · [[medir-foco-de-teclado]], [[aislar-stack-docker-por-worktree]], [[captura-playwright-miente]]
- 2026-08-11 · ingest · Escrita [[el-contador-no-mide-el-archivo]], que sale de un frente de una línea y **es el hallazgo más caro del día**. El hook de diseño reportó 40, 38, 37, 22, 2 y 1 hallazgos sobre `public/css/handsontable-module.css` sin que el archivo cambiara acorde, y la causa se comprobó ejecutándolo **dos veces seguidas sobre el mismo contenido: la segunda devuelve vacío**. Guarda estado de sesión y suprime lo ya señalado, así que el total describe cuántas veces se disparó antes, no el archivo. Lo grave no es el número: es que **tiene forma de dato reproducible** —sale de una herramienta, cita líneas y reglas ciertas— y se usó como **indicador de avance del programa de cierre**, viajando en informes durante un día entero entre dos sesiones. Lo reproducible es contar literales con `grep` (que también ve los de dentro de comentarios, [[audit-ve-color-en-comentarios]]) y el audit del gate estático contra `docs/design-system/exceptions.json`, que es el contractual. El criterio general que deja la página: **antes de usar un número como indicador de avance, ejecútalo dos veces sobre el mismo contenido y comprueba que da lo mismo**; si no coincide, no mide el mundo sino el estado de quien pregunta. De paso se corrigió otra afirmación que ya había viajado —que retirar esta reserva dejaría el archivo «sin literales de color»—: quedan **47**, medidos, y lo que salió fue uno. El frente en sí retira la reserva redundante de `--aia-green-dark` en `:758`, con la justificación buena escrita en el commit —el token está definido y resuelve con el **mismo** valor, luego la reserva no se evalúa nunca— y con la que **no** vale marcada como tal: que un token *vecino* se consuma sin reserva no prueba nada sobre este, porque la regla es por token · [[el-contador-no-mide-el-archivo]], [[medir-foco-de-teclado]], [[captura-playwright-miente]]
- 2026-08-11 · ingest · Corregida [[aislar-stack-docker-por-worktree]] en la receta ligera, que **estaba mal en dos puntos y por eso no arrancaba**. Afirmaba que el `.env` «sí viaja» al worktree: no viaja, y sin copiarlo la app responde `500` y la puerta de servicio no existe. Y la línea de `docker run` pasaba las `DB_*` por `-e`, que es justo lo que **rompe** la conexión: los valores del `.env` vienen entrecomillados (`DB_USER="root"`), el `-e` los propaga con las comillas dentro y el contenedor gana porque `Dotenv::createImmutable()` no sobrescribe lo que ya está en el entorno, así que MySQL responde `Access denied` para el usuario `'"root"'` — un mensaje que no apunta a la causa. Lo que funciona es lanzar el contenedor **sin ninguna `DB_*`** y dejar que mande el `.env`. Añadido el tercer paso que faltaba: el worktree no trae `vendor/`, así que hace falta `composer install` dentro o `public/index.php` muere en el `require_once` del autoload. Los tres se descubrieron encadenando `500` sin causa visible en el frente `vocabulario-estados-cascada`, al montar el stack propio para que el «después» de las capturas fuera de verdad el árbol de uno. La nota se **corrigió donde estaba mal** en vez de añadirle un párrafo que la contradijera, y se redactó en prosa porque el lint marca `MULTIHECHO` cuando una página parte en seis hechos numerados · [[aislar-stack-docker-por-worktree]], [[captura-playwright-miente]]
- 2026-08-11 · ingest · Escrita [[el-dom-dice-que-existe-no-que-se-ve]] al cerrar el frente `vocabulario-estados-cascada`, y **lo que la hace cara no es el error sino su recorrido**. La afirmación era que dos nombres del mismo estado «se contradicen en la misma pantalla»; medido a 1180×820, el botón de filtro está en `x=86` y la celda `ops-state-chip` en `x=1332`, o sea **fuera del viewport**: no se ven a la vez. Salió de un `querySelectorAll` que encuentra igual de bien lo que está dentro y lo que está fuera de la pantalla, así que la lectura respondía a «qué existe» y se usó para afirmar «qué se ve». Lo que sí comprueba esa pregunta es `getBoundingClientRect()` contra `window.innerWidth`, o una captura, **nombrando siempre la resolución**. La premisa **pasó por tres sesiones** —quien la midió, la coordinadora que la reenvió al archivar el frente del término duplicado con «los mismos elementos» como motivo escrito, y el ejecutor que la heredó y la repitió en un spec y en un commit— sin que ninguna la comprobara, y **la conclusión aguantó igual**: había que fundir los términos, pero porque el segundo iba a fijar vocabulario sin saberlo, no por compartir píxeles. Acertar por casualidad es justo lo que impide que el error se note, y de ahí el criterio: **una medición mal leída se propaga sin resistencia mientras su conclusión siga pareciendo correcta**. La página marca también dónde la afirmación **sí** valía —el modal se abre encima de la leyenda—, porque una trampa que solo cuenta el error enseña a desconfiar de todo. El spec quedó corregido con la medición y dejando dicho que el mensaje de `ca9c6bb7` quedó inexacto; la historia publicada no se reescribe · [[el-dom-dice-que-existe-no-que-se-ve]], [[valor-declarado-no-es-valor-computado]], [[captura-playwright-miente]]
- 2026-08-11 · ingest · Frente `buttons-important-leyenda`: **41 `!important` sobre la leyenda pasan a 16**, y las dos trampas que se corrigieron valen más que la resta. La primera amplía [[aislar-stack-docker-por-worktree]] con el borde que su propia receta ligera abrió: un contenedor lanzado con `docker run` **no existe para `docker compose`**, y `tests/browser/fixtures/base-url.mjs` deriva la URL de los e2e con `docker compose port app 80`, así que responde `0.0.0.0:8081` y **Playwright corre contra el árbol principal** mientras el navegador ve el worktree. Dos capturas visuales pasaron píxel a píxel con 25 `!important` retirados… contra el código viejo, y ese verde iba camino de un informe como evidencia; repetido con `E2E_BASE_URL=http://localhost:8095` pasó de verdad. La regla corta: **el `docker run` suelto cubre el navegador y nada que deduzca el puerto por compose**. La segunda amplía [[el-contador-no-mide-el-archivo]] con su pariente: `git stash` **sin cambios locales no guarda nada y no falla**, así que medir un «antes» con `stash`/`pop` sobre un árbol ya commiteado devuelve **el mismo árbol presentado como pasado** — salió «863 errores antes y después» con las dos cifras del mismo código, y el único aviso fue un `No stash entries found` al final. Lo que sí compara es `git checkout <sha> -- <ruta>`. El patrón común: **un instrumento que ante «no hay nada que hacer» devuelve algo con forma de resultado**. Del frente en sí, lo reutilizable es el método —quitar los `!important` en bloque, medir el computado y **reponer solo los que se movieron**, en vez de ir uno a uno— y el hallazgo que justifica verificar en las tres pantallas: de las 16 supervivientes **ninguna hace falta en las tres a la vez**, y sin `.indicator display` el punto de color desaparece en dos; medir solo el módulo del encargo habría retirado cinco necesarias en otro sitio, y la lista heredada de cuatro se quedaba corta por dos. Se declaran las dos cifras porque ninguna sobra: 41 → 16 es lo que pesa sobre la leyenda, 160 → 138 el total del archivo —22, no 25, porque las 3 de T3 siguen existiendo para `.badge` y `.aia-chip` y solo dejaron de alcanzar al chip—, y Biome corrobora el 22 sin buscarlo · [[aislar-stack-docker-por-worktree]], [[el-contador-no-mide-el-archivo]], [[css-layer-cascade]], [[valor-declarado-no-es-valor-computado]]
- 2026-08-11 · ingest · Corregida y renombrada [[el-contador-no-mide-el-archivo]], que hasta hoy se llamaba `contador-que-baja-porque-ya-lo-miraste`. **El título describía bien lo que sabíamos y mal lo que sabemos:** era direccional, sugería un techo que se va limando, y de ahí salía la conclusión tranquilizadora de que basta con no leer el número dos veces. La segunda serie lo desmonta — sobre `public/css/buttons.css`, en el frente `buttons-important-leyenda`, el hook fue **137, 121 y 16**, y después, con el árbol ya limpio y publicado, **121 y 16 otra vez**: **volvió a subir**, sin que el archivo cambiara entre lecturas. Con eso el encuadre correcto no es «se degrada al mirarlo» sino que **no mide el archivo en ninguna dirección**: un contador que sube y baja sin que cambie lo contado no es un contador con memoria, es otra cosa con forma de contador, y no vale ni como cota superior. El criterio de la página se afina en consecuencia: la pregunta no es «¿cuánto se ha movido?» sino **«¿se ha movido algo de lo contado?»**, y si la respuesta es no, se descarta entero en las dos direcciones. Actualizados los cinco enlaces entrantes —`docs/coordinacion-sesiones.md`, `valor-declarado-no-es-valor-computado`, el spec de `buttons-important-leyenda` y dos entradas de este mismo log—; la única mención al nombre viejo que queda es la nota del renombrado, y va en comillas y no como enlace, a propósito · [[el-contador-no-mide-el-archivo]], [[audit-ve-color-en-comentarios]], [[gate-solo-cuenta-elementos-no-los-lee]]

- `ingest` 2026-08-11 — [[cada-worktree-tiene-su-copia-congelada]]: el registro de sesiones vive en la raíz, pero cada worktree se lleva una copia que no se actualiza; medidas las cuatro divergentes, dos con el mismo recuento y distinto contenido.
- 2026-08-12 · ingest · Dos páginas corregidas con lo medido al cerrar el CI: [[visual-baselines-estado-real]] (decía «todas las familias visuales fallan» desde el 2026-07-28; hoy en local dan **20 passed** y en CI fallan 18 de 20 sobre el **mismo sha** — goldens sin sufijo de plataforma, `D-GAC-4`) y [[baselines-y-presupuestos]] (`runtime-baseline-0.3.3.json` se congeló el **2026-07-17**, el mismo día de la última corrida verde; dos violaciones medidas en árbol limpio, y `initializationMs` **no** es una de ellas — esa cifra salió de un árbol sucio y se corrigió, `D-GAC-5`). **Deliberadamente NO se anota como `veracidad`, y esto es lo que importa de esta línea:** la alarma se reinicia con cualquier línea `· veracidad ·`, y aquí se verificaron **2** páginas frente a las 12–44 de los pases anteriores. Escribirla habría apagado el aviso sin hacer el trabajo — el fraude más barato disponible, y con el gesto más inocente. **La alarma sigue encendida a propósito: el octavo pase sigue pendiente.**
- 2026-08-12 · ingest · Escrita [[golden-huerfano-no-lo-ve-ningun-gate]] al retirar los **18 goldens del tema `linen`**, que sobrevivieron **dieciocho días** al retiro del tema (DS-030, 2026-07-25) pese a que los esquemas, los contratos y hasta una prueba dedicada (`tests/design-system/linen-removal.test.mjs`) ya estaban limpios. La causa medida no es descuido: **los gates anclan cada golden desde su manifiesto y nunca preguntan lo contrario** — `scripts/design-system-contracts.mjs` valida `golden` + `sha256` de los escenarios declarados y comprueba que la ruta caiga bajo `GOLDEN_ROOTS` (`:986`), pero no lista el directorio buscando PNG que no declare nadie; la suite visual captura escenario por escenario desde el mismo manifiesto (`design-system-lab.visual.mjs:167`). Un golden huérfano es por tanto **invisible en las dos direcciones**: ni se compara ni se denuncia. Retirarlos no movió una aserción —static **8/8** y `test:visual:lab` **20 passed**, idénticos antes y después—, que es justamente la prueba de que no medían nada. El criterio que deja la página: al retirar un tema, un viewport o una familia, **borrar sus goldens es un paso explícito de la limpieza**, y a la inversa, que un PNG esté en `__screenshots__` no prueba que se compare — búscalo en los manifiestos antes de usarlo como evidencia. Intacto el juego de CI en `linux/`, que sí está anclado por hash desde `D-GAC-4` · [[golden-huerfano-no-lo-ve-ningun-gate]], [[manifiesto-ds-exige-golden]], [[visual-baselines-estado-real]], [[gate-solo-cuenta-elementos-no-los-lee]]
- 2026-08-12 · veracidad · Octavo pase, disparado por la alarma (131 commits desde el séptimo). Áreas revisadas: **design-system (46 páginas), qa (19), proceso+deploy+docker+worktrees (~14) y la repesca por antigüedad de arquitectura (25)**, con seis verificadores de solo lectura en paralelo sobre `c188c7bf`; los hallazgos sustantivos los re-comprobé yo antes de aplicarlos. **11 páginas corregidas, ningún hallazgo grave.** El de más fondo lo destaparon **dos verificadores desde lotes distintos**, que es lo que le da peso: [[gate-solo-cuenta-elementos-no-los-lee]], [[madurez-y-api-estable]] y [[manifiesto-de-modulo]] describían **quince** gates de cierre exigidos todos en `status: 'passed'`, y hoy son **ocho** y ese requisito **ya no existe** — `release-governance.test.mjs:75` comprueba `gates.length === 8` y el comentario de `:72-74` explica el porqué, que es la parte que valía la pena escribir: exigir el verde **obligaba a declarar aprobados gates que no lo estaban, y fue el incentivo que produjo los quince recibos `passed` sin ejecutar** que el sexto pase había encontrado. El arreglo no fue mirar más fuerte sino **quitar el requisito que premiaba mentir**; hoy uno de los ocho está `blocked` sin romper el contrato y los ocho recibos traen `command`, `exitCode` y `outputTail` (288–7.832 bytes) en vez de dos claves. Lo demás son estados y cifras corridos: [[visual-baselines-estado-real]] daba `D-GAC-4` por **abierta** y quedó resuelta y ejecutada ese mismo día (goldens de CI por plataforma, `00f1ad59`); [[baselines-y-presupuestos]] llamaba abierta a `D-GAC-5` y fijaba el `0.3.3` como baseline vigente, cuando el vivo es **`0.3.4`** (`package.json:25`) y la forma de cerrarlo importa —`D-GAC-6` intentó antes actualizar el `0.3.3` **en sitio** y chocó con el contrato que lo ancla por hash, así que la re-aprobación pasó a ser archivo con versión propia—; [[manifiesto-ds-exige-golden]] daba por universal una regla que hoy tiene dos ramas (`visualEvidence` delega la evidencia en una familia y entonces `scenarios` debe ir **vacío**, `module-manifest.schema.json:105`); cuatro citas `archivo:línea` corridas por commits posteriores (tokens, drawer, la aserción del lab y la tolerancia visual, que se movió con `D-GAC-4`); y la cifra de biome re-medida hoy, **863/2.604/404**. En arquitectura, [[flujo-pdc]] y [[subcontratistas]] hablaban en presente de [[contratos]], borrado con el PDC v1 el 2026-08-04 — y de paso se corrigieron los `resumen:` de [[contratos]] y [[listado-de-actividades]], que **seguían describiéndolos vivos en la columna del catálogo** pese al banner de derogación en el cuerpo: es justo el fallo contra el que avisa el manual. [[IMPLEMENTATION_PLAN_INVENTORY|Registro de trabajo]] estaba en 75 trabajos y su generador daba **94**: regenerado con el script, no a mano. **qa salió limpia en las 19**, coherente con que el séptimo pase la cubrió el día anterior. **Dos límites declarados, no disimulados:** (1) lo que exige Playwright, Docker, DB o SSH no se verificó y se dice en cada página —cifras de píxeles, mediciones de foco y animación, el `.env` real de la puerta de servicio, y el estado del servidor de producción en [[produccion-deploy]], cuya cifra de «~147 commits detrás» lleva tres semanas sin poder confirmarse—; (2) el diff de las zonas generadas de `memoria/arquitectura/` contra una regeneración fresca tampoco, porque `wiki-arquitectura.mjs --escribir` necesita la base levantada; sí corrió `--cobertura`: **200 rutas, ninguna huérfana**. **Un defecto de las fuentes, anotado sin tocarlas:** `docs/decisiones-pendientes.md` sigue dando `D-GAC-5` como «pendiente solo el sí/no del usuario» cuando `3bc3a662` y `e40f113e` la dan por aprobada y ejecutada · [[gate-solo-cuenta-elementos-no-los-lee]], [[madurez-y-api-estable]], [[manifiesto-de-modulo]], [[visual-baselines-estado-real]], [[baselines-y-presupuestos]], [[manifiesto-ds-exige-golden]], [[branch-preexisting-red-gates]], [[flujo-pdc]], [[subcontratistas]], [[IMPLEMENTATION_PLAN_INVENTORY|Registro de trabajo]]
- 2026-08-12 · ingest · **Operación espejo y reparación unique_id en los tres entornos**, cerrada el mismo día: dump de producción → local pisada y reparada (25.708+1.422 filas por reglas deterministas A/B; 8.027 históricas quedan NULL a propósito) → pruebas restaurada como clon verificado → y producción, en mantenimiento y con respaldo doble, reparada y nivelada de 71 a 102 objetos (PDC v2/BI/catálogos). El bug visible era «Id de actividad inválido» en Programación Intermedia y la causa era de datos, no de UI; producción estaba peor que pruebas (50,5% del consolidado). Los datos del PDC de Da Porto solo existían en pruebas y se rescataron de su snapshot antes de pisarla. Nada de desarrollo viajó a producción (sandbox 990100 y test.* verificados fuera). Trampa nueva anotada: `docker exec -T` dentro de `while read` se come el stdin y reporta éxito sin iterar — delatado solo por el re-conteo posterior · [[espejo-y-reparacion-unique-id]], [[produccion-deploy]], [[el-contador-no-mide-el-archivo]]
- 2026-08-12 · ingest · **Release completo de código a producción, desplegado** — `1aa7c694` → `939b7928`, **1.763 commits** en un solo `git pull --ff-only`, con el sitio en mantenimiento y respaldo de archivos previo (709 MB). Salió por fin porque la base ya estaba nivelada esa misma jornada: **cero migraciones ejecutadas**, que es la diferencia con todos los intentos anteriores. Dos cosas que vale la pena no re-aprender: (1) el `composer install` **no es trámite** — actualizó 11 paquetes con saltos mayores (`phpmailer` 6.12 → **7.1.1**, `phpspreadsheet` 5.4 → 5.9) y regeneró el classmap, sin lo cual las clases nuevas del PDC v2 sencillamente no existen (16 entradas `Services\Pdc` donde había cero); (2) **bajo mantenimiento el 503 no distingue «cerrado» de «roto»**, así que el smoke que sirve es el de las rutas exentas de `MaintenanceMode::isExemptRoute()` — la puerta `/_aia/operacion/7f3c9b` devolvió **200 con el login entero** y `/runtime/frontend-config.js` otro 200, probando front controller + autoloader + vistas; `php_errorlog` sin líneas nuevas desde el 23 de julio. El drift de julio (`indicadores.view.php`, `modal_reabrir.php`) se **descartó**, no se guardó: `main` ya lo supera e incluye la corrección de seguridad de `/indicadores` (`4b1a2be0`). Reapertura pendiente de confirmación explícita del usuario · [[produccion-deploy]], [[espejo-y-reparacion-unique-id]], [[powerbi-indicadores]]
- 2026-08-12 · ingest · **El mantenimiento tapaba sus propios estilos** — reportado como «el CSS no carga bien» tras el release y diagnosticado sin tocar nada: los 36 CSS existían y respondían 200; lo que fallaba eran las dos hojas que van **por PHP** (`/runtime/css/*`, entre ellas `core.css`), que caían en el chequeo de mantenimiento y devolvían **el HTML del cartel con 503** donde el navegador esperaba `text/css`, mientras las tres estáticas (`/css/*`) cargaban. Por eso la pantalla salía **a medio vestir**, que es exactamente como se ve un despliegue roto — y no lo era. Causa de fondo: la misma lista de rutas copiada en tres sitios, con la exención del cartel congelada en dos entradas desde `b00cdaf6`; los CSS **nunca** estuvieron ahí. Arreglado derivando las rutas de su dueño (`DesignSystemAssetController::publicRoutePaths()`) en vez de añadir una cuarta copia, con `tests/test_maintenance_asset_exemption.php` (**25/25**) que comprueba la **coherencia entre listas** y que la exención no abra el sitio; verificado además end-to-end en local con `.maintenance` puesto: CSS **200 `text/css`**, `/` y `/plan-compras` siguen en **503**. PHPStan limpio, `dev_door_guard` 17/17 y `global_table_safety` en verde. El criterio que queda: **bajo el cartel, el 503 no distingue «cerrado» de «roto»** — hay que pedir las rutas exentas y mirar el `content-type` · [[mantenimiento-tapa-sus-propios-estilos]], [[produccion-deploy]]
- 2026-08-12 · ingest · Arreglo del CSS bajo mantenimiento **desplegado y confirmado en producción** (`939b7928` → `9ae7cb19`, con el cartel puesto): las **cinco** hojas de la pantalla de entrada dan `200 text/css` —eran tres de cinco— y `/`, `/login`, `/plan-compras` y `/programacion-intermedia` siguen en `503`, que es la mitad que suele olvidarse comprobar. La prueba corre también en el servidor con PHP 8.3 (**25/25**) y el `php_errorlog` no sumó una línea. Sin `composer install`: el cambio no trae clases nuevas y ambas ya estaban en el classmap — regenerarlo habría sido ruido, no prudencia · [[mantenimiento-tapa-sus-propios-estilos]], [[produccion-deploy]]
- 2026-08-13 · ingest · **Clonado de «Da Porto» (73) sobre «Prueba» (27) en los tres entornos**, con `scripts/clonar-proyecto.php` nuevo (dry-run por defecto, `--apply` en transacción). Antes de tocar nada, el grilleo corrigió la premisa: «Prueba» **no estaba vacío** —11.013 filas, más que Da Porto— así que el clon **borra** datos reales, y había **dos** proyectos «Da Porto» (73 y 76); el usuario eligió el 73, copia completa y accesos de Da Porto (de 40 miembros a 17). Lo que enseñó la operación: mirando solo las **81 claves foráneas** parecían seis cadenas de identificadores en riesgo, y al leer las **claves primarias** resultaron **dos** —casi todas son compuestas con `project_id` y viajan intactas—, de modo que contar FKs sobrestima el problema. Las cuatro trampas reales **solo aparecieron al escribir**, y las tres primeras hicieron abortar la transacción sin dejar el destino a medias: nueve `bi_*` son **vistas** (se recalculan solas y MySQL las rechaza), `activa_unica` es una columna **generada**, `import_token` tiene UNIQUE **global** (se anula: el clon no viene de una importación) y las `semi_auto_*` son residuo del PDC v1 sin **ningún** consumidor en el código. Verificación idéntica en los tres: **22 tablas con conteos coincidentes, 0 discrepancias, 0 huérfanos en 7 comprobaciones** de integridad — que es lo que hay que mirar, porque un clon con las referencias apuntando al original da los mismos totales y falla en silencio; en local, además, comprobado en el navegador. Respaldos completos previos en pruebas y producción (61 MB, «Dump completed») · [[clonado-de-proyectos]], [[produccion-deploy]]
- 2026-08-13 · ingest · **Contraste roto en el modal TNP de Programación Semanal**, reportado por el usuario con captura de producción: `.tnp-option-title` usaba `--ds-color-brand-primary-dark` —un verde de marca pensado como fondo— como tinta sobre las superficies oscuras del desplegable Select2 (~1,4:1, invisible), y el campo de búsqueda del desplegable no tenía ninguna regla en todo el árbol (quedaba al user-agent). Arreglado con `inherit` en el título (13,09:1 en reposo, 8,67:1 resaltado, medidos computados en el navegador) y tokenizando el buscador en el adaptador `select2.css`, que cubre todos los desplegables de la app. Los otros seis modales de la vista revisados y sanos. Suite estática 8/8 en verde · [[tinta-de-marca-sobre-superficie-oscura]], [[css-layer-cascade]]
- 2026-08-13 · ingest · **Arreglo del contraste del modal TNP desplegado a producción** (`971cff0e` → `1d5fc489`, pruebas primero y producción después, cada uno con su tar previo): sin migraciones ni `composer install` (solo CSS y notas de la wiki). Verificado sirviendo el contenido, no solo el 200: `programacion-semanal.css` entrega el `color: inherit` y el adaptador `select2.css?v=1.1.0` las 3 reglas nuevas del buscador — sin bump de versión porque esa hoja no lleva `Cache-Control` y el ETag cambió solo, así que los navegadores revalidan. Render PHP 8.3 del login OK y `php_errorlog` sin líneas nuevas en ambos entornos. Falta solo el vistazo visual con sesión real, que no puede hacerlo el asistente (la puerta dev no existe en servidores y las credenciales no se teclean) · [[tinta-de-marca-sobre-superficie-oscura]], [[produccion-deploy]]
- 2026-08-13 · ingest · **Control Tower oculto de la navegación mientras se desarrolla**, ejecutado con subagentes en 6 tareas (`1ae05a8b`→`9483cd5a`). El diseño cambió a mitad y el cambio es la lección: la primera versión apagaba el módulo por `APP_ENV`, y el usuario corrigió que él necesita seguir entrando —no trabaja en local—, así que el gate acabó siendo **por rol** (capacidad nueva `internal.bi.preview`, solo rol `A`, 404 y no 403 para el resto). De paso esquivó que el `.env` de pruebas declara `APP_ENV="testing"`. Lo que costó una ronda de corrección: ocultar el ítem del sidebar **rompió con 500 las ocho vistas del propio módulo** para Admin, porque `DesignSystemComponent:392` lanza excepción si el destino activo no está entre los ítems pintados; lo destapó la verificación en navegador y no el `curl` del implementador, que daba 200 porque su sesión no había pasado antes por un módulo —y porque Apache registra 200 aunque el cuerpo sea la página de error—. Tres suites codificaban el contrato viejo y se actualizaron declarándolo (una suspendida con `.skip`, no borrada). Verificación: gate 15/15, scope PASS, dev-door 17/17, PHPStan limpio, suite estática 8/8, y en navegador `test.A` 200 con cero accesos visibles y `test.R` 404. Revisión final: 0 críticos · [[control-tower-oculto-mientras-se-desarrolla]], [[el-item-oculto-del-sidebar-rompe-su-propio-modulo]]
- 2026-08-13 · ingest · **Ocultamiento de Control Tower desplegado a pruebas y producción** (`7487fda7`→`e901bc55`, tar previo en ambos, sin migraciones). Aquí el `composer install` **sí era obligatorio** y se comprobó de la única forma que vale: `grep -c BiPreviewAccessPolicy vendor/composer/autoload_classmap.php` daba **0 tras el `git pull` y 1 después** en los dos servidores — con `--optimize-autoloader` la clase nueva no existe para PHP hasta regenerar el classmap, y entre el pull y el composer las rutas `/bi/*` habrían dado 500. Verificación en pruebas por HTTP con los dos roles, usando `curl` **desde dentro del servidor** contra `https://127.0.0.1` con cabecera `Host`: la puerta `/dev/entrar` exige `requestIsLocal()` y rechaza la IP del portátil, cosa que no estaba escrita en ningún sitio. Admin 200 con el cuerpo real del Control Tower y cero `data-bi-access-link` en Programación Semanal; Residente 404 en vista y en API (cuerpo JSON). En producción la puerta dev **está cerrada**, así que no hay sesión posible: se verificó evaluando el código en el servidor —`canAccess()`/`canAccessAny()` en `false`, `renderLink()`/`renderBootConfig()` vacíos, gate `true` solo para `A`— más el test 15/15 contra la base real y `php_errorlog` sin líneas nuevas · [[control-tower-oculto-mientras-se-desarrolla]], [[produccion-deploy]]
- 2026-08-13 · ingest · **Corrección el mismo día: los accesos del Control Tower vuelven, pero solo para Admin.** El usuario reportó desde producción que entrando como administrador no veía los accesos —y era exacto: el primer corte los ocultaba a todos, él incluido, leyendo «ocultar los accesos desde el frontend» como «para todo el mundo». No era una avería sino el diseño, y el diseño estaba mal: el ocultamiento existía para que el equipo no tropezara con trabajo a medias, no para estorbar a quien lo desarrolla. `canAccess()`/`canAccessAny()` pasan a exigir `BiPreviewAccessPolicy::canOpen()` **y luego** su lógica original de alcance por proyecto —el gate primero, para no pintarle a un Admin un enlace que `BiProjectScope` le rechazaría después—. **La lección: una petición de ocultar no dice a quién, y el valor por defecto (todos) deja fuera al que pide.** De paso se reactivó la suite `bi_control_tower_access` (entra con `test.A`): 8 casos en verde. Quedaron suspendidos con motivo escrito el bloque de roles `D`/`R` y **dos casos de deuda previa** que apuntan al menú retirado (`#informacionGeneral`, `ul.main-links`) y a un botón que `/indicadores` nunca pintó — comprobado en el DOM, llevan rotos desde la migración al shell sidebar y no los rompió este frente. Verificación: gate 15/15, scope PASS, PHPStan limpio, y en navegador `test.A` ve los accesos y `test.R` no · [[control-tower-oculto-mientras-se-desarrolla]]
- 2026-08-13 · ingest · **Corrección de los accesos del Control Tower desplegada a pruebas y producción** (`e901bc55`→`68ff78b4`, tar previo en ambos). Sin migraciones y **sin `composer install`**: no llegaron clases nuevas y `BiPreviewAccessPolicy` ya estaba en el classmap del despliegue anterior — comprobado antes de decidirlo, no supuesto. La verificación destapó un error **de medición mío, no de la app**: mis `curl` de smoke pedían `/programacion-semanal` sin proyecto en sesión, y esa ruta **redirige 302 a `/proyectos`**; sin `-L`, lo que se grepeaba era el cuerpo vacío de la redirección, así que el «0 accesos BI» era un artefacto. Con `-L` y un proyecto seleccionado (`p=Da Porto`): Admin 2 accesos y el ítem del sidebar, Residente 0 y 404. En producción, evaluado con cuentas **reales** de esa base: un admin da `canOpen`/`canAccess`/`canAccessAny` en `true`, y los roles `D` y `S` en `false` para ambas cosas. Home 200 y `php_errorlog` sin líneas nuevas. **Lección: un 0 en un grep sobre una respuesta HTTP no prueba ausencia si no se comprobó antes que el cuerpo es la página que se cree.** · [[control-tower-oculto-mientras-se-desarrolla]], [[produccion-deploy]]
- 2026-08-13 · ingest · **Red de caracterización de las 22 reglas de habilitación de Programación (F2a-2b-1), publicada** (`0e909227`..`d1b7b1a7`). Tres archivos nuevos y **ninguna regla tocada** (`git diff` vacío sobre los dos `hot.js`): un arnés que inyecta rol/fase/semana en los inputs que las reglas ya leen y re-decide **por la vía de cada módulo** —`render()` en Semanal, el hook `afterFilter` en Intermedia, `PSHotModule.reload()` para repintar las cards—, nunca con `location.reload()`, que habría perdido el contexto inyectado. El lector se ata al editor real (`expectDecisionMatchesEditor`) para no acabar caracterizando el CSS en vez de la regla. Cobertura honesta: 14 de las 22 cubiertas, 2 parciales con motivo (I1 no puede medir su complemento porque la grilla monta exactamente las columnas que la config declara; S5 decide por `renderer`, no por `prop`), 1 no cubierta (S12 gobierna botones, no celdas) y 6 fuera de alcance declarado por ser guards de valor con efectos —uno de ellos **borra la actividad**—. **Ninguna prueba quedó como `skip`**, y el caso que iba a serlo dejó la lección: ver [[regla-inalcanzable-parece-regla-sin-probar]]. Verificado en esta sesión: 8/8 static, 14/14 en las dos suites, tres corridas seguidas cada una · [[regla-inalcanzable-parece-regla-sin-probar]], [[gate-solo-cuenta-elementos-no-los-lee]]
- 2026-08-13 · ingest · **Revisados los tres hallazgos de la red F2a-2b-1; ninguno se corrige, y el de más peso resultó ser lo contrario de lo que parecía.** El «`Ejecutado_Real` ignora la semana histórica» **es deliberado**: `LpsWeekEditPolicy::allows()` abre la histórica confirmada a quien puede calificar, y el cliente resulta ser **más estricto** que el servidor, no más laxo. La sospecha que eso levantó —que por API se podría cambiar `Compromiso` en una semana cerrada, porque el guard autoriza por semana y rol sin mirar el campo— **era falsa**, y descubrirlo exigió bajar a una segunda capa: `SemanalApiController.php:309-315` devuelve **409** ante compromiso, responsables o planificación con la semana confirmada, que es exactamente la frontera de la interfaz; y `programacion-semanal-roles-phases.mjs:316` ya lo cubría de extremo a extremo. Se estuvo **a punto de encargar una tarea entera sobre una premisa inexistente**: la tarea se abrió con el alcance reducido a lo único que sí falta (un unitario para la composición de `LpsWeekEditPolicy`, hoy cubierta solo de facto por el e2e). Los otros dos hallazgos: `Descripcion` en `editableProps` sin columna se resuelve dentro de la extracción que viene, y la rama de filas cabecera de Intermedia **no se borra** —el concepto es real en Programa General (`programa_general/hot.js:847`) y su inalcanzabilidad depende de un filtro SQL de otro archivo, ya vigilado por la prueba nueva— · [[un-if-de-autorizacion-no-es-toda-la-autorizacion]], [[regla-inalcanzable-parece-regla-sin-probar]]
- 2026-08-13 · ingest · **Cobertura de la composición de `LpsWeekEditPolicy`, publicada** (`c9f602e4`, merge `b82b767d`). El test cubría las dos reglas por separado y nunca su combinación —que es donde nace el comportamiento observable: en una semana histórica ya confirmada, R y DCV pueden calificar avance—. Para probarla en puro se extrajo `LpsWeekEditPolicy::decide()`, la misma composición sin sesión ni base; la consulta de `Semanal_Confirmada` entra como `callable` para **conservar su pereza original**, y tres aserciones comprueban que no se dispara cuando no debe. Ninguna regla de autorización cambió. La cobertura se validó **por mutación**: al quitar la excepción de calificación, el caso «R califica semana histórica confirmada» se pone en rojo. Y ahí saltó la lección de la jornada: las primeras corridas daban verde **con el mutante puesto**, porque el contenedor `app` en marcha seguía montando el working tree principal pese a que el compose del worktree declaraba otra cosa — ver [[exec-en-contenedor-vivo-corre-el-repo-ajeno]]. El e2e `programacion-semanal-roles-phases.mjs` **no es concluyente** en local (8/14 rojos por un proyecto sin sembrar, `Optimización Aeropuerto JMC`); comprobado con `git stash` que **falla idéntico sin el diff**, así que es preexistente. Verificado tras integrar `origin/main`, no antes: test OK y PHPStan sin errores · [[un-if-de-autorizacion-no-es-toda-la-autorizacion]], [[exec-en-contenedor-vivo-corre-el-repo-ajeno]]
- 2026-08-13 · ingest · **Sembrada la membresía de `Optimización Aeropuerto JMC` (68) y escrita la decisión que faltaba del Control Tower.** El «Project card not found» de la suite de navegador **no era un proyecto sin sembrar** —el 68 existe, activo, con 11 semanas y 1.228 filas de programación—: era que ninguna cuenta `test.*` figuraba en `project_members`, y el listado filtra por ahí. Mismo caso que el proyecto 27 «Prueba», ya documentado en el propio `dev_test_users.php`, así que el arreglo fue añadir el 68 a esa lista: cinco filas nuevas, idempotente por el índice `unique_project_user`. Los cuatro casos que morían en la tarjeta desaparecieron. **Lo que destapó debajo es otro defecto, y de tamaño propio:** el spec deriva la semana del dato vivo —con un comentario que explica por qué no la fija— pero **ata la actividad a un literal** (`Movilización general`, `Descapote`), y el proyecto ya va por `Max_Semana=11`, así que la histórica es la 9 y esos nombres están en 1-4, 10 y 11. Quedan 7 rojos que no son de la siembra: tres por ese literal, uno por aislamiento Compose (`databaseSnapshotSupport.mjs:38` exige `COMPOSE_PROJECT_NAME` de CI) y tres de UI. Aparte, se escribió [[control-tower-oculto-mientras-se-desarrolla]], que cuatro entradas del log y una trampa enlazaban sin que existiera; el lint pasa de 5 hallazgos a 0 · [[control-tower-oculto-mientras-se-desarrolla]], [[exec-en-contenedor-vivo-corre-el-repo-ajeno]]
- 2026-08-13 · ingest · **Los 7 rojos de `programacion-semanal-roles-phases.mjs` eran tres causas distintas, y ninguna era una regresión del producto.** (1) **Pudrición del dato:** el spec derivaba la semana con un comentario que explicaba por qué no fijarla, y dos líneas después ataba la fila a `'Movilización general'` y `'Descapote'` —que con `Max_Semana=11` no existen en la semana histórica 9— y la semana 4 de «Prueba», que hoy solo tiene 1 y 2. Ahora la fila se elige por la **precondición** (responsables, compromiso > 0, semana confirmada) y falla nombrando la precondición si nadie la cumple. (2) **Pudrición del selector:** `.btn-pdc-modern` dejó de existir en la toolbar el 2026-08-01 (`ed9ca6db`, migración al design system) y la leyenda se mudó al menú «Mas» el 2026-08-03 (`f61f9661`); los tres casos de interfaz medían elementos inexistentes o invisibles. El producto está bien: con el selector corregido hay botones visibles con texto y sin overflow a 787 px. (3) **Candado de escritura:** las cuatro pruebas que llaman `runSql` exigen el stack aislado de CI, y ese stack **no trae el dato** que necesitan (una semana sin confirmar del 68, ningún proyecto 27), así que no hay entorno donde puedan pasar: quedan con `test.skip` y motivo escrito, no borradas, y ampliar el fixture queda como trabajo con plan propio. De paso salió un **intermitente** que no era del producto: Bootstrap 4 descarta el cierre de un modal si la apertura sigue en transición. Verificado: 10 pasan y 4 se saltan, dos corridas completas seguidas (antes 7/14 en rojo) · [[fijar-un-dato-de-la-base-en-un-test-lo-podre]], [[el-dato-esta-en-desarrollo-y-el-permiso-en-el-stack-aislado]], [[modal-bootstrap-ignora-el-cierre-en-transicion]]
- 2026-08-14 · ingest · Reincidencia de [[exec-en-contenedor-vivo-corre-el-repo-ajeno]] un día después de documentada: el contenedor llevaba 13h corriendo montado contra el checkout principal, destapado con un `grep` de un símbolo recién escrito que dio 0. Recreado con autorización explícita del usuario porque otra sesión suya corría en paralelo contra el mismo `localhost:8081`. Matiz nuevo: al recrear, la puerta de desarrollo dejó de autenticar porque `.env` es untracked y nunca viaja a un worktree nuevo — se copió del checkout principal. Página actualizada, no duplicada.
- 2026-08-14 · ingest · **Cerradas las 4 pruebas saltadas de `programacion-semanal-roles-phases.mjs`: ampliado el fixture aislado y regenerados los 3 gates de cierre que ese fixture anclaba.** JMC ganó las semanas 1-4 confirmadas con filas que cumplen la precondición de cada caso (`8a0d5e46`); apareció una trampa nueva en el camino — `ProgramChangeDetector::run()`, disparado automáticamente en cada carga de página por `changeMonitor.js` sin mirar si la semana está confirmada, borra silenciosamente cualquier fila de `programacion_semanal` sin pareja en `programa_consolidado` — diagnosticada desde un hang de 120s vía el `trace.zip` de Playwright, no a ciegas. También destapó un bug independiente y ya corregido: "rol R histórico..." nunca pasaba `credentials` a `loginAndSelectProject` y corría como Admin pese al nombre del caso, enmascarado hasta que `test.R` tuvo membresía real en JMC. **La ampliación del fixture invalidaba el `fixtureSha256` anclado en tres gates bloqueantes ya existentes** (`runtime`, `global-table-safety`, `full-app-flow` en `docs/design-system/closeout-evidence.json`) — se regeneraron con `gate-receipt.mjs` contra el commit real (`8a0d5e46`→`79debf28`), en dos commits porque el validador exige que el artefacto exista, tal cual, en el commit que `sourceRef` declara. **No se añadió un 9º gate** para el spec nuevo: el índice fija 8 a propósito desde el Frente 1b (bajó de 15), y sumar uno habría revertido esa consolidación sin que nadie la reconsiderara aposta — decisión del usuario. Verificado: 14/14 en el stack aislado, dos corridas seguidas; `npm run test:design-system:static` en verde tras cada commit que tocó el fixture o el índice · [[fixture-huerfano-de-programa-consolidado]], [[fijar-un-dato-de-la-base-en-un-test-lo-podre]], [[el-dato-esta-en-desarrollo-y-el-permiso-en-el-stack-aislado]]
- 2026-08-14 · veracidad · Noveno pase, disparado por la alarma (51 commits desde el octavo). Áreas revisadas: **bi+rbac (Control Tower, 14 páginas)**, **qa+lps (reglas de habilitación, 67)**, **design-system (gates de cierre y baselines, 55)** y **arquitectura+worktrees (menú flotante del shell, clonado de proyectos, 54)**, con cuatro verificadores de solo lectura en paralelo sobre `636f03cf`; los hallazgos sustantivos los re-comprobé yo antes de aplicarlos. **3 páginas corregidas, ningún hallazgo grave.** (1) [[torre-de-control-bi]] decía que Control Tower es «el módulo que casi todos los roles pueden ver» — desde `44632801`/`9483cd5a` (2026-08-13) está oculto de la navegación y de las rutas mientras se desarrolla, visible solo para Admin vía la capacidad `internal.bi.preview`; la corrección remite a [[control-tower-oculto-mientras-se-desarrolla]], que ya estaba vigente y no necesitó tocarse. (2) [[un-if-de-autorizacion-no-es-toda-la-autorizacion]] cerraba con «queda abierto: nunca se probó la composición de las dos reglas» — cerrado el mismo 2026-08-13 por `c9f602e4`, que extrajo `LpsWeekEditPolicy::decide()` y la prueba en puro; el hueco que describía ya no existe. (3) [[design-system]] (mapa) seguía citando el hallazgo del 2026-08-10 de «15 gates, solo 2 verificados de verdad» como estado vigente del goal `design-system-nucleo-gobernanza` — el Frente 1b bajó el índice a 8 el 2026-08-11, y hoy 7/8 tienen recibo real verificado (los tres que ancla el fixture, regenerados esta misma sesión). El resto de las páginas revisadas —clonado de proyectos, menú flotante del shell, baselines de runtime, goldens del tema linen— coincidían con el código: **ninguna corrección, no por sospecha sino porque los cuatro verificadores citaron archivo:línea y no encontraron divergencia.** Un verificador señaló una deuda que hoy solo vive en un mensaje de commit y no en la wiki (el umbral del menú flotante difiere entre la pieza canónica y el laboratorio, 1180px vs 1200px) — anotada aquí, no promovida a página propia en este pase · [[torre-de-control-bi]], [[control-tower-oculto-mientras-se-desarrolla]], [[un-if-de-autorizacion-no-es-toda-la-autorizacion]], [[design-system]]
- 2026-08-14 · ingest · **D-COORD-2 re-medida: el mapa de estado no ve los cierres de este repo por una comparación de igualdad exacta, y la correlación es completa.** Los dos ejemplos que la ficha citaba (`ci-en-verde` como `ENCOLADA`, `gates-al-ci` como `ENTREGADA 3 de 8`) **están arreglados**; el defecto de fondo no. Sobre `ef49b6a0`: `cas-estado.sh` devuelve **RC=0** con 42 filas, 4 con dato y 38 «no medible», y **20 de esas 38 son metas que la wiki declara cerradas**, pintadas `ENCOLADA` — la etiqueta que el propio script define como «nadie lo está trabajando». La causa es `cas_tiene_seccion` comparando con `$0 == s`: este repo escribe `## Cierre formal` en 19 metas y el literal `## Cierre` en solo 2 — **que son exactamente las 2 que el mapa acierta, 2 de 2**. En el repo del plugin, 24 de 24 usan el literal, así que el instrumento está validado solo contra su propia casa. Comprobado en aislado que el prefijo lleva la detección de 2 a 22, sin tocar el otro repo. **Se descartó normalizar los 20 encabezados de aquí:** sería doblar archivos fuente ante la rigidez de un instrumento, y un comentario del propio script ya dice que una meta cerrada no debe pintarse como encolada. Recomendación con orden: primero el código de salida distinto de cero, después el prefijo — al revés, el arreglo sería indistinguible de un fallo silencioso. Los dos arreglos son de otro mando: hallazgo preparado para el traspaso, y comprobado que allá no está registrado · [[gate-solo-cuenta-elementos-no-los-lee]], [[el-contador-no-mide-el-archivo]]
- 2026-08-18 · ingest · **El modal de cambio obligatorio de contraseña era una trampa cerrada, y la causa no estaba en el modal.** Se abría sin botón de cancelar, sin Escape y sin clic fuera, pero eso era solo la mitad: la bandera `must_change_password` seguía en sesión, así que cualquier recarga lo reabría, y **`/logout` tampoco servía de salida** porque no figura en `$publicRoutes` y `SessionMiddleware::check()` exige `$_SESSION['usuario']`, que en ese estado no existe —solo hay `usuario_temp`—, de modo que redirigía a `/login` sin destruir nada. Arreglado con ruta pública propia `/login/cancelar` y botón «Volver al inicio de sesión»; la ruta **solo destruye la sesión si hay cambio pendiente**, para no convertirse en un cierre de sesión por CSRF, y bajo mantenimiento devuelve a la ruta oculta porque destruir la sesión se lleva `maintenance_bypass`. Verificado en el navegador y por HTTP: modal presente → cancelar → sesión destruida → recarga limpia; con sesión autenticada normal la ruta no toca nada y `/proyectos` sigue en 200; `phpstan` limpio. **Dos hallazgos ajenos al frente, ninguno introducido por él:** `tests/test_login_design_system_contract.mjs` ya venía rojo en `main` (cuenta `AiaAlertInterceptor.js` dos veces porque un comentario lo menciona, desde `10d072cd`), y **la tecla Escape no cierra ningún modal de SweetAlert2 11.4.24 en la aplicación**, comprobado con pulsación real y con evento sintético sobre un `Swal.fire` de prueba — por eso el modal deja `allowEscapeKey: false` con el hallazgo escrito al lado en vez de una opción que no hace nada. Reincidencia de [[exec-en-contenedor-vivo-corre-el-repo-ajeno]] con matiz nuevo: el contenedor compartido montaba un worktree **ya borrado**, así que servía 404 y `docker exec` fallaba con «container breakout detected»; se recreó desde este worktree y hubo que copiar `.env` otra vez · [[logout-no-limpia-la-sesion-pendiente-de-clave]], [[exec-en-contenedor-vivo-corre-el-repo-ajeno]]

- 2026-08-18 · ingest · **Cerrado el rojo preexistente de `tests/test_login_design_system_contract.mjs` que la entrada anterior dejó abierto: el aserto medía menciones, no carga.** Contaba `AiaAlertInterceptor.js` sobre el texto crudo de `views/auth/login.view.php`, así que el comentario PHP que documenta por qué jQuery sigue siendo local (`10d072cd`) lo ponía en `2 !== 1` sin que la vista cargara el script dos veces. **Se endureció el test, no el comentario**: ahora cuenta solo etiquetas `<script src=...>`, porque el contrato existe para vigilar carga real y un comentario que explica una dependencia es información legítima —reformularlo habría dejado la trampa armada para el próximo que nombre el archivo en prosa. Misma familia que [[audit-ve-color-en-comentarios]], ahora en un test en vez de en el audit del DS: **un gate que lee texto crudo mide el texto, no el hecho**. Comprobado que el aserto sabe fallar (1 con la vista real, 2 duplicando la etiqueta, 0 borrándola), y publicado en `c215d808` con `npm run test:design-system:static` en **EXIT=0** y los 8 carriles verdes, medido después de fusionar. **Dos reincidencias propias, ambas cazadas antes de publicar:** leí el resultado de la suite con `${PIPESTATUS[0]}`, que es de bash —en zsh es `pipestatus` y 1-indexado—, así que la primera lectura salió **vacía** y no gobernaba nada, exactamente [[el-codigo-de-salida-se-pierde-en-la-tuberia]]; se repitió redirigiendo a archivo y leyendo `$?` del comando real. Y el otro rojo del carril `node-tests`, `stylesheet versions follow nested CSS changes`, **no se tocó porque no es del código**: compara el mtime que ve PHP dentro del contenedor —que montaba el worktree `password-change-modal-close-c1f5d8`— contra el que lee `statSync` en el worktree desde donde se corre, así que da verde o rojo según qué dos checkouts se enfrenten; sigue vivo [[exec-en-contenedor-vivo-corre-el-repo-ajeno]] · [[audit-ve-color-en-comentarios]], [[el-codigo-de-salida-se-pierde-en-la-tuberia]], [[exec-en-contenedor-vivo-corre-el-repo-ajeno]], [[branch-preexisting-red-gates]]
- 2026-08-18 · ingest · **«Escape no cierra los modales de SweetAlert2» era falso, y la wiki ya tenía escrita la causa desde hacía doce días.** El informe se apoyaba en dos medidas y las dos fallaron por su instrumento: la pulsación real se hizo en el panel Browser integrado, que tiene el reloj de animaciones congelado —y SweetAlert2 desmonta el popup en `animationend`, así que allí **ningún** diálogo se cierra nunca, ni con Escape ni con el botón—, y el evento sintético se lanzó sobre `document`, donde SweetAlert2 no escucha: registra su `keydown` en el `div.swal2-popup`, porque `keydownListenerCapture` es `false` por defecto. Verificado con Playwright contra el mismo contenedor (`chromium.launch()`, 1180×820): Escape cierra el diálogo por defecto, `AIA.Notice.confirm`, el `window.alert` interceptado y la forma exacta del modal de cambio obligatorio con el flag en `true`, tanto en `/login` como en `/programacion-semanal`. **No tocaba ni actualizar SweetAlert2 ni envolver el cierre en `AIA.Notice`: no hay defecto que arreglar.** La sonda que cierra el caso en un paso es `event.defaultPrevented` — salió `true`, o sea que el handler de Escape corrió y llamó a `preventDefault()`, luego el fallo estaba después del teclado. Corregido lo escrito: el comentario de `views/auth/login.view.php`, que afirmaba un defecto inexistente de la librería, y el párrafo de [[logout-no-limpia-la-sesion-pendiente-de-clave]] que lo había anotado. El modal se queda en `allowEscapeKey: false` por el motivo real —un Escape accidental cae en el `.then()` como `dismiss` y navega a `/login/cancelar`, que destruye la sesión pendiente— y no es trampa de teclado porque el botón de salida es alcanzable con Tab. Reincidencia de [[panel-browser-no-anima]], que describía este mismo síntoma con este mismo diálogo desde el 2026-08-06. Cerrado en `3125d9e5` (el comentario) y `6b16969c` (la wiki), publicado en `main` con `c69a4f05` tras integrar `origin/main` y volver a verificar: el contrato de login quedó **verde**, porque `c215d808` llegó de otra sesión arreglando justo ese aserto —contaba menciones en prosa, no cargas—. El único rojo que queda, `node-tests`, es el artefacto de dos árboles de [[suite-estatica-miente-en-worktree-secundario]]: el test compara la versión que renderiza el contenedor (repo principal) contra el `tokens.css` de este worktree, y el mismo test sale verde medido en un solo árbol · [[panel-browser-no-anima]], [[evento-sintetico-no-alcanza-al-popup]], [[logout-no-limpia-la-sesion-pendiente-de-clave]]
- 2026-08-18 · ingest · **Ampliada [[audit-ve-color-en-comentarios]] de un caso del audit del DS al mecanismo que lo explica: un gate que busca sobre el texto crudo no distingue el hecho de un comentario que habla del hecho.** La página cubría solo los hex y `rgba()` citados en comentarios CSS; ahora lleva las dos instancias medidas —esa y el contrato de login contando menciones de `AiaAlertInterceptor.js` en vez de cargas, rojo **doce días** en `main` (2026-08-06 → 2026-08-18)— más el criterio para escribir gates: **anclar el patrón al constructo que porta el hecho** (una declaración, una etiqueta `<script src>`) y **mutar lo medido para comprobar que sabe fallar**, porque un patrón endurecido a ojo puede quedarse en verde permanente. Se marca también la salida falsa: reformular el comentario para contentar al gate lo deja igual de ciego. **De paso se corrigió una explicación equivocada que la página llevaba desde el 2026-07-29**: decía que `color-mix()` se salva «porque el patrón exige un dígito tras el paréntesis», y no es eso —`colorFunctionPattern` admite `palabra + dígito`, así que `color(display-p3 0.5 0 0)` **sí** cuenta; `color-mix` se salva porque el nombre no está en la lista, que exige `color(`. Comprobado ejecutando el patrón sobre cinco casos, no razonándolo. El puntero del mapa [[design-system]] se amplió en consecuencia, para que el catálogo no siga describiendo la nota por su primera instancia · [[audit-ve-color-en-comentarios]], [[design-system]], [[gate-solo-cuenta-elementos-no-los-lee]]
- 2026-08-18 · ingest · **Cerrada la Task 5 del plan del fixture: el noveno gate de cierre, `semanal-roles-phases`, con recibo real.** Ampliar el fixture aislado había vuelto ejecutable la suite de roles y fases de la Semanal, pero **nada la ejecutaba** — cobertura disponible no es cobertura vigente, y esa distinción es la que el índice de cierre existe para no perder. El gate entra en el registro de comandos, en el contrato (ids, `kind`, y la lista de los que dependen del fixture, porque su recibo se ata por `fixtureSha256` al dato con el que se midió) y en el workflow, después de `full-app-flow` y con el patrón obligado: `gate-receipt.mjs` levanta acta y **una segunda línea lee el recibo y propaga su `exitCode`**, porque el script siempre sale 0 a propósito. Las pruebas dejan de repetir el número de gates y lo leen del contrato. **Y salió un hallazgo que no se buscaba:** el primer recibo dio **rojo, 13 de 13 casos caídos en 8.072 ms**, y el segundo pasó en 46 s sin cambiar nada. No era el producto ni el fixture: el healthcheck de `db` responde al **servidor temporal** con el que MySQL carga las semillas (`port: 0`, solo socket), así que `app` arranca creyendo la base lista y queda una ventana de **8 segundos exactos** sin base — la anchura del fallo. `curl /login` no protege porque esa página responde 200 sin tocar la base. Medido leyendo los logs de `db`, no deducido. Verificado: suite estática 8/8 con el noveno gate dentro, y el recibo tomado sobre árbol limpio · [[el-healthcheck-de-db-responde-al-servidor-temporal]], [[el-dato-esta-en-desarrollo-y-el-permiso-en-el-stack-aislado]]
- 2026-08-18 · ingest · **Escrita [[verificas-un-arbol-y-publicas-otro]]: el gate de cierre verifica en el paso 5 y publica en el 6, y entre ambos el `main` del worktree principal es estado compartido que otra sesión puede mover.** Medido con las horas de `git log`: cerré mi merge a las **10:15:32** y verifiqué ese árbol (suite estática, contrato de login y lint, los tres `EXIT=0`); a las **10:15:35** y **10:15:58** otra sesión commiteó y fusionó su rama de Docker en ese mismo `main`; mi `git push origin main` de las ~10:16 publicó `7f198a83..cdad1e08`, es decir **dos commits ajenos que mi verificación nunca tocó** (`docker-compose*`, `CLAUDE.md`). Salieron verdes al revisarlos después, pero eso es suerte: la verificación no gobernó la publicación. **No es [[worktree-compartido-arrastra-commits]]** —ahí lo que se cuela son cambios sin commitear— porque aquí todo estaba commiteado y `git status` limpio en cada paso: lo que se movió fue la **rama**, que ninguna comprobación de limpieza mira. Misma familia que [[el-codigo-de-salida-se-pierde-en-la-tuberia]]: el gate sigue existiendo, deja de gobernar, y el síntoma es un verde. **Contramedida, ejecutada en esta misma publicación en vez de solo recomendada:** no fusionar en el `main` del worktree principal, sino integrar y verificar en el worktree propio y publicar con `git push origin HEAD:main`, que sube exactamente el commit medido y se rechaza solo si alguien publicó entre medias · [[verificas-un-arbol-y-publicas-otro]], [[worktree-compartido-arrastra-commits]], [[el-codigo-de-salida-se-pierde-en-la-tuberia]], [[un-verde-solo-vale-para-el-arbol-donde-se-midio]]
- 2026-08-18 · ingest · **Reparado el montaje del stack local y destapada una trampa de dos filos, la segunda mitad cometiéndola yo.** El contenedor `app` montaba `./` —la carpeta desde la que se ejecuta compose— con `name: last-planner-aia` fijo, así que la última sesión que hacía `up` se llevaba el montaje; apuntaba a un worktree ya borrado y `localhost:8081` daba 404 en TODAS las rutas sin avisar. Arreglado con ruta absoluta a la raíz (`${LPS_CODE_ROOT:-...}`, con escotilla para servir una rama a propósito). **Filo 1:** las `DB_*` se inyectaban sin valor por defecto, llegaban VACÍAS desde un worktree, y una variable vacía cuenta como definida para `Dotenv::createImmutable()` — el `.env` montado y correcto ya no podía rellenarlas; `MAIL_HOST` funcionaba precisamente porque compose no la toca. **Filo 2, el error propio:** quitar esas tres del bloque `environment:` parece la conclusión obvia, deja la web entera en verde y revienta `docker compose exec app php tests/test_*.php` con «Access denied for user \x27\x27», porque los tests no cargan Dotenv y la web sí. Se publicó a `main` así, verificado solo por navegador; lo cazó la propia wiki al leer [[servir-worktree-stack-efimero]] para escribir la nota. Revertido con el porqué escrito en el compose, y el problema original resuelto por la otra vía: enlace del `.env` en los 9 worktrees (**había 6 copias sueltas**, hoy idénticas pero condenadas a divergir en silencio). Verificadas ahora las DOS vías, web y CLI · [[variable-vacia-tapa-el-env]], [[aislar-stack-docker-por-worktree]], [[servir-worktree-stack-efimero]]
- 2026-08-18 · ingest · **El paso 6 del gate de cierre (`AGENTS.md` §Publicación) pasa de `git push origin main` a `git push origin HEAD:main` desde el worktree propio**, con el paso 5 anotando el SHA verificado y el paso 7 confirmando contra `origin/main`. Cierra la ventana descrita en [[verificas-un-arbol-y-publicas-otro]]: publicar por nombre de rama sube a dónde apunte `main` en ese instante, no lo que se midió. El paso 4 gana además una frase explícita —**integrar en la rama propia, no fusionar en el `main` del worktree principal**—, que es donde nacía el problema. De paso se corrigió la evidencia del propio paso 6, que decía «dos veces, a la misma sesión»: son **tres** casos y **dos** sesiones, y el tercero (leer la suite con `${PIPESTATUS[0]}` bajo zsh, que no existe ahí) se cazó antes de publicar, así que no cabe en el «quedó sano por suerte» que la nota generalizaba. Comprobado que `GEMINI.md` y `README.md` no repiten estos pasos, así que no quedan contradiciendo al contrato; las apariciones de `git push origin main` que quedan viven en planes fechados de `docs/superpowers/`, que son registro histórico y no se tocan · [[verificas-un-arbol-y-publicas-otro]], [[el-codigo-de-salida-se-pierde-en-la-tuberia]]
- 2026-08-18 · ingest · **Los dos casos de tablet vuelven al gate: 15 de 15, sin excepciones.** E3 dejó 787 px sin grilla —bajo `UMBRAL_CARDS` (1180, `public/js/modules/aia_ui/view-switch.js:6`) la Semanal monta tarjetas— y con ella sin objeto las dos pruebas que medían `getHotInstance().countRows()` y las superficies de Handsontable; llevaban saltadas desde el 2026-08-14. Reescritas contra lo que el producto hace hoy, y una de ellas cubre algo que la hermana de 551 px **no** cubría: que bajo el umbral la grilla **no llegue a montarse**, que es justo la conducta que E3 introdujo y que nadie vigilaba. **Ninguna toca el interior de la tarjeta** —ni chip, ni avance, ni título—: eso lo está rehaciendo E2-bis en paralelo, y una aserción sobre su contenido habría convertido este gate en un freno para ese trabajo; solo se mira lo estructural, que E2-bis conserva. La segunda **deriva la semana con actividades** en vez de fiarse de la máxima, porque la máxima de Da Porto es hoy la semana vacía sembrada para el caso hermano. **Verificado por mutación, no solo por verde:** subiendo el viewport por encima del umbral las dos se ponen en rojo, y al restaurarlo vuelven a pasar. De paso se corrigió un dato que esta wiki se había inventado por analogía el mismo día: la tabla de la espera es `general_usuarios`, no `usuarios` — el `SELECT` fallaba y el bucle habría girado en vano · [[el-healthcheck-de-db-responde-al-servidor-temporal]]
- 2026-08-18 · ingest · **La cuota de SiteGround se llenó porque el respaldo de despliegue duplicaba en disco lo que git ya guardaba; arreglado en el origen, y el clon shallow probado y descartado con la medida.** SiteGround bloqueó Site Tools con `~/backups` en 19 GB: 29 tarballs de pre-deploy de 540-690 MB. La limpieza a 4.6 GB fue el paliativo (40.90% → 13.32% de cuota, y el bloqueo tardó en levantarse aunque la cifra ya estuviera baja). La causa: el `tar` del paso 3 empaquetaba `public_html` entero, y `public_html` pesa 759 MB por 370 MB de `.git` y 298 MB de evidencia binaria de QA — de los que **lo irremplazable son ~4 MB**, medidos con `git status --porcelain --ignored`, no supuestos (`public/storage`, `.env` y siete `.bak` de drift del 23 de julio). Ahora el tar excluye lo que git o Composer reponen y baja a **5.1 MB** (la estimación de 45 MB era en crudo: lo que queda es texto y comprime 8×), viaja con un `.manifest.txt` con el SHA —sin `.git` dentro, un respaldo que no se puede situar en la historia no es un respaldo— y rota a 3 por sitio. Del repo salieron 20 `trace.zip` y 26 `.webm` (282 MB, `docs/qa` de 298 a 16 MB) a `lps-aia-evidencia/`, verificados por `sha256` **antes** de borrar. **Tres trampas medidas:** (1) `.gitignore:210` ya ignoraba `docs/qa/` entera y aun así los binarios seguían dentro — git no deja de rastrear lo que ya rastreaba, así que una regla nueva no habría cambiado nada, y añadir un archivo ahí exige `git add -f`; (2) `scripts/design-system-worktree-preservation.mjs` calcula `sha256(git diff --cached --binary)` con un buffer de 128 MB, así que **borrar 282 MB de binarios lo revienta con `ENOBUFS` y sale con código 2 en vez de 1**, poniendo rojo un gate por el mero hecho de tener el borrado preparado sin commitear — se cura al commitear, no tocando el gate; (3) el clon shallow **es incompatible con esta rutina**: podar no libera nada mientras la rama local apunte al commit anterior (362 MB antes y después del `gc`), y al mover la rama `git pull --ff-only` aborta con `Not possible to fast-forward` porque `origin/main` queda injertado sin padres — la única salida sería `reset --hard`, que descarta trabajo local en silencio donde `--ff-only` se niega; revertido con `fetch --unshallow`. Verificado: `worktree-preservation` 9/9, 431 de 432 en la suite estática, tar con `.env` y `public/storage` dentro y `.git`/`vendor` fuera, producción y pruebas en HTTP 200. **Dos rojos ajenos, probados ajenos con el árbol limpio:** `stylesheet versions follow nested CSS changes` (preexistente) y los cuatro tests PHP de evidencia, cuyos asertos de archivo pasan todos y solo fallan los de BD — la base es **compartida por los ~10 worktrees** (proyecto Docker `last-planner-aia`, un solo contenedor `db`) y otra sesión la tenía en vuelo · [[fixture-huerfano-de-programa-consolidado]]
- 2026-08-18 · ingest · **Los tres pendientes que quedaban resultaron estar hechos o superados, y el único trabajo real fue destrabar un gate que llevaba una semana en rojo por un recibo viejo.** Contrastados contra el código, no contra sus documentos: *PHPUnit incremental* estaba completo —runner exigiendo `#[Group]`, CI construyendo con `COMPOSER_INSTALL_FLAGS=""`, 103 scripts sueltos y 1 clase—, con la única falla en `CLAUDE.md`, que en dos sitios seguía afirmando «no hay PHPUnit ni Pest» desde el 2026-08-11; *fixture CI de roles* tenía las Tasks 0-3 hechas y las 4 y 5 **descartadas a propósito** (la 4 se contradecía a sí misma: pedía borrar los `test.skip` y a la vez que en desarrollo siguieran saltándose), y su condición de hecho «14 verdes, 0 skip» ya no era alcanzable porque el spec declara 15 casos y dos de tabla en tablet saltan en firme por el retiro de esa tabla — se reescribió a **13 verdes y 2 saltadas**. **El gate `runtime-budgets` no estaba en rojo por el código:** su recibo se midió el 2026-08-11 contra el baseline `0.3.3` y `0.3.4` se aprobó el 12, sin regenerarlo nunca. Al remedir, las dos violaciones quedaron atribuidas sin regresión —`cssGzipBytes` +121 B (0,06 %), determinista en siete corridas, trazado a las 81 líneas que el menú flotante bajo 1180px añadió a `adapters/shell-sidebar.css`; `handsontableInteractionMs` 271-277 frente a los 134,6 que `0.3.4` congeló, que es **el mínimo de toda la serie** (280,7 en 0.3.3, 180,4 en 0.3.6, 234,2 en 1.0.0)—. Aprobada la generación **0.3.5** por el usuario. **Tres barreras impedían registrarla, las tres deuda del tiempo en que solo existía una generación:** la lista blanca de `BASELINE_GENERATIONS`, las rutas literales a `0.3.4` dentro del validador de procedencia, y cinco aserciones que fijaban el nombre del baseline en `visual-ci-contract.test.mjs` — parametrizadas por generación. **Lo que no se cerró y por qué:** el medidor toma una sola muestra y `initializationMs` varió de 174,9 a 744,2 en la misma máquina y el mismo commit, así que el baseline nuevo lo rompió la corrida siguiente; el recibo válido lo produce CI, no un portátil, y medir varias veces para quedarse con la mediana es cambio de diseño con plan propio — decisión del usuario. La hipótesis fácil («la máquina está cargada») se descartó con una medida y no con una suposición: `initializationMs` bajó de 1.070,6 a ~230 en este equipo, y un arranque cinco veces más rápido no convive con una interacción inflada por falta de CPU. Reincidencia de [[exec-en-contenedor-vivo-corre-el-repo-ajeno]] por un tercer camino: `foundation.test.mjs` hace `docker compose exec app` y alcanzó el contenedor de desarrollo montado contra el checkout principal, comparando el CSS de un árbol con los tokens del otro — verde 28/28 al forzar el proyecto correcto. Verificado: `test:design-system:static` 8/8, `test:wiki` sin hallazgos, `runtime-budget:check` PASS sobre `14263200` · [[baseline-de-una-muestra-congela-el-atipico]], [[exec-en-contenedor-vivo-corre-el-repo-ajeno]]
- 2026-08-18 · veracidad · Décimo pase, disparado por la alarma (50 commits desde el noveno). Áreas revisadas: **design-system** (56 páginas), **qa** (52), **proceso+deploy** (19) y las cuatro más quietas —**docker, pdc, datos, admin** (30)—, con cuatro verificadores de solo lectura en paralelo sobre `424e45b2`; los hallazgos sustantivos los re-comprobé yo citando archivo y línea antes de aplicarlos. **7 páginas corregidas, ninguna derogada, ningún hallazgo grave.** Cuatro son el mismo dato movido: el índice de cierre pasó de 8 a 9 gates al entrar `semanal-roles-phases` —[[gate-solo-cuenta-elementos-no-los-lee]], que además citaba `release-governance.test.mjs:75` comprobando `length === 8` cuando el test ya lee la cifra del contrato; [[design-system]] (mapa), que daba 7/8; [[madurez-y-api-estable]], con «las ocho gates»; y [[el-dato-esta-en-desarrollo-y-el-permiso-en-el-stack-aislado]], que decía «los 14 casos» cuando hoy son **15 y ninguno saltado en firme** (los cuatro `test.skip` que quedan son condicionales dentro de un caso, no casos excluidos: saltan solo fuera del stack aislado)—. Las otras tres son deriva real: [[baselines-y-presupuestos]] daba `0.3.4` como baseline vigente y hoy `package.json:25` apunta a `0.3.5`; **[[pdc]] (mapa) mandaba reutilizar los contratos `auto/*` y `public/js/modules/semi_auto_review.js`, que no existen desde el borrado del PDC v1 el 2026-08-04** —la corrección más importante del pase, porque ese mapa es justo el que se lee antes de tocar el PDC y mandaba a un flujo inexistente—; y [[panel-admin]] afirmaba que «nada de lo de `src/` se reutiliza aquí», falso desde el 2026-08-03: `admin/` comparte a propósito `RbacService`, `EventService`, `ProgressTracker`, `DevDoor` y `RbacCatalog`, y duplicar la normalización de roles habría dejado a las dos apps discrepando sobre quién puede qué. **Hallazgo propio, no de los verificadores:** [[suite-estatica-miente-en-worktree-secundario]] daba un remedio **incompleto**. `COMPOSE_PROJECT_NAME` propio resuelve a qué contenedor vas, pero no qué árbol monta: `docker-compose.override.yml:26-28` hace el bind desde `${LPS_CODE_ROOT:-/Volumes/Crucial X6/Developer/lps-aia}`, ruta absoluta al checkout principal por defecto. Medido: solo con el nombre de proyecto, 27/28 y el `filemtime` del PHP es el del principal; añadiendo `LPS_CODE_ROOT="$PWD"`, **28/28 y la suite entera 8/8**. El síntoma es idéntico por los dos caminos, así que no se distingue por el mensaje. Las 19 páginas de proceso y deploy salieron **todas vigentes**, incluidas las que citan el paso 6 del gate, que ya usaban `git push origin HEAD:main`. Verificado: `npm run test:wiki` sin hallazgos de forma y `test:design-system:static` 8/8 · [[gate-solo-cuenta-elementos-no-los-lee]], [[baselines-y-presupuestos]], [[pdc]], [[panel-admin]], [[suite-estatica-miente-en-worktree-secundario]]
- 2026-08-18 · ingest · **El rojo de `stylesheet versions follow nested CSS changes` no era del CSS: el gate medía dos copias del repositorio a la vez.** Calculaba la versión ejecutando PHP **dentro del contenedor** —que monta `${LPS_CODE_ROOT:-/Volumes/Crucial X6/Developer/lps-aia}`, el checkout principal— y la comparaba contra un `statSync` del **worktree que lanza la suite**. Con los dos extremos en el mismo árbol cumple (contenedor: `1787024214 ≥ 1786462575`; worktree: `1787062717 = 1787062717`); cruzados, falla (`1787024214 < 1787062717`). Confirmado además corriendo la suite desde el checkout principal: 28/28 en verde sin tocar nada. **Lo grave no era el falso rojo sino el falso verde** que el cruce inverso permite: un árbol montado con CSS más reciente habría aprobado un entrypoint desactualizado. Arreglado leyendo el `mtime` por la misma vía que la versión, verificado 28/28 desde el worktree, desde el checkout principal y lanzando el mismo archivo con el `cwd` del otro; la suite estática queda **8/8, verde entera por primera vez en la sesión**. La regla que sobrevive al caso concreto: si una comprobación toma un valor de dentro del contenedor y otro de fuera, no mide un invariante, mide si las dos copias coinciden hoy · [[gate-que-mide-dos-arboles-a-la-vez]]
- 2026-08-18 · ingest · **Escrita [[borrar-el-worktree-no-cierra-la-sesion]]: borrar el worktree donde vive una sesión no la saca de los worktrees — el harness crea otro y la mueve allí, con la misma rama.** Se borró `nifty-hellman-5a2f63` tras comprobar árbol limpio, cero commits sin publicar y ningún contenedor montándolo; la sesión reapareció en `great-kare-4470f3` con `claude/nifty-hellman-5a2f63` otra vez checkouteada, y **lo notó el usuario, no la sesión**, que ya había dado el borrado por cerrado. Dos efectos secundarios: la rama sobrevive y queda **ocupada** —`git branch -d` se niega mientras esté checkouteada en algún worktree, hay que soltarla antes con `git checkout --detach`, que no cambia ningún archivo si se queda en el mismo commit—, y «borrado» no es un estado final comprobado: se confirma con `git worktree list` **y** con dónde quedó la sesión. La página recoge además las tres comprobaciones previas, ninguna teórica aquí: trabajo sin publicar, que no lo monte un contenedor vivo ([[exec-en-contenedor-vivo-corre-el-repo-ajeno]], que ya dejó un stack sirviendo 404 al borrarse el worktree que montaba) y los ignorados que se van con él —el `.env` debe ser **enlace** y no copia, y el `sesiones.md` del worktree suele ser la copia vieja ([[cada-worktree-tiene-su-copia-congelada]]). Cerrado el ciclo: los dos worktrees y la rama borrados, sin pérdida —el `.env` era symlink en ambos— y los cuatro commits del día vivos en `origin/main` · [[borrar-el-worktree-no-cierra-la-sesion]], [[exec-en-contenedor-vivo-corre-el-repo-ajeno]], [[cada-worktree-tiene-su-copia-congelada]]
- 2026-08-18 · veracidad · Undécimo pase, **complementario del décimo del mismo día, no disparado por la alarma**: el aviso que lo motivó (44 commits desde el 2026-08-14) se midió sobre una copia local **cuatro commits atrás**, y al integrar `origin/main` apareció la línea del décimo pase, hecho horas antes sobre `424e45b2` —exactamente el HEAD que yo tenía—. Con el árbol al día el lint da **0 commits** y ningún hallazgo. Se hizo pase igualmente porque el décimo dejó fuera cinco de las trece áreas, y el trabajo que el usuario nombró —la salida del cambio obligatorio de contraseña— cae justo en ellas. Áreas revisadas: **rbac (10 páginas), lps (25), arquitectura (32), worktrees (10) y bi (7)**, verificado inline sin subagentes. **3 hallazgos, 3 páginas corregidas más una zona generada, ninguna derogada, ninguno grave.** (1) La zona generada de [[autenticacion]] **no listaba `GET /login/cancelar` → `LoginController::cancelPasswordChange`**, añadida esa misma mañana en `667a6b21`: el inventario del módulo omitía justo la ruta que la trampa [[logout-no-limpia-la-sesion-pendiente-de-clave]] manda usar como única salida del estado a medias. Regenerada con `wiki-arquitectura.mjs --escribir`, y el diff fue **una sola línea en las 25+ páginas de módulo**, así que el resto de los inventarios estaban al día; `--cobertura` da **201 rutas, ninguna huérfana** (eran 200 en el octavo pase). (2) El hallazgo de más fondo: [[rbac-y-rutas]] y [[lps-dominio]] —**los dos mapas, que son lo que se lee antes de tocar el área**— citaban [[reabrir-semana-asimetria-cliente-servidor]] **en presente** como trampa medida viva, y esa página está `estado: derogada` desde el séptimo pase (2026-08-11). El mapa de LPS llegaba a decir que «ninguna de las dos reglas es la que el producto quiere». Verificado en código antes de corregir, no por confianza en la derogación: `SemanalReabrirPolicy::allows($role, $fechaInicioSemana)` guarda dentro de `SemanalApiController::reabrir()` y responde 403 **antes de mutar** (`src/Controllers/Api/SemanalApiController.php:1003`), con la regla real —Admin y Director siempre, Residente solo hasta el fin del día de inicio, el resto nunca—. Las dos corregidas conservando la lección, que no depende del caso: el cliente puede esconder, solo el servidor puede impedir. Se dejó **sin tocar** la tercera cita, en [[regla-solo-en-cliente-no-es-regla]], porque está en pasado («donde el endpoint sí concedía de más») y como comparación histórica sigue siendo cierta. (3) [[variable-vacia-tapa-el-env]] apoyaba «el worktree nace sin `.env`» en [[cada-worktree-tiene-su-copia-congelada]], que trata de las copias congeladas de `.claude/sesiones.md` — **otro mecanismo**: la cita no sostenía lo que se le pedía. Reapuntada a [[CLAUDE]] §Runtime & commands, donde vive el remedio real, que es **enlazar** el `.env`, no copiarlo. **Verificadas sin corrección, citando archivo y línea:** [[logout-no-limpia-la-sesion-pendiente-de-clave]] coincide exacta con `LoginController.php:152-177` (solo destruye si hay cambio pendiente; bajo mantenimiento devuelve a `SECRET_PATH`), y su párrafo sobre el falso hallazgo de Escape ya venía autocorregido del mismo día; [[aislar-stack-docker-por-worktree]] con `name: last-planner-aia` en la línea 1 y `db_data external: true / htdocs_db_data` en 58-61; y [[dev-door-acceso-local]], cuyo candado triple coincide con `src/Core/DevDoor.php` y cuyas cinco cuentas coinciden con el `.env` real —**esto cierra un límite que el octavo pase había declarado no verificable**—. El piloto móvil F2A no desmiente nada: **ninguna página afirma un alcance «solo escritorio»**. **Tres límites declarados, no disimulados:** (1) **bi** no se releyó página por página —ningún archivo de BI se movió desde el noveno pase, que la cubrió—; (2) el trabajo de tarjeta móvil (~6 commits) **no tiene página en la wiki**, lo que es un hueco de `ingest` y no una falsedad, así que se anota aquí y no se escribe en este pase; (3) nada que exija Playwright ni medición visual se verificó. Verificado: `npm run test:wiki` sin hallazgos de forma, 137 páginas · [[autenticacion]], [[rbac-y-rutas]], [[lps-dominio]], [[variable-vacia-tapa-el-env]], [[reabrir-semana-asimetria-cliente-servidor]], [[logout-no-limpia-la-sesion-pendiente-de-clave]], [[dev-door-acceso-local]]
- 2026-08-18 · ingest · **Cerradas las tres decisiones de coordinación que quedaban abiertas; no queda ninguna.** `D-COORD-1`: las colas de decisiones de las sesiones **entran en git** (16 archivos, `decisiones/` fuera del `.gitignore`), y el motivo que pesó no fue el respaldo sino que un choque entre sesiones pase a ser un conflicto **visible** en vez de trabajo que desaparece sin rastro; revisados antes de subirlos, ninguno lleva secretos. `D-COORD-2`: el mapa de estado del plugin **se retira como fuente en este repo** — no se normalizan los 20 encabezados propios ante la rigidez de un instrumento ajeno, y comprobado al cerrar que **ningún documento operativo lo citaba** ya, solo su ficha y el informe de traspaso. `D-COORD-3`: **`scripts/publicar.sh` pasa a ser obligatorio** (`AGENTS.md` §Publicación paso 6), y **no** se instala un `pre-push`, que cambiaría el entorno de quien clone el repo. **Lo caro del frente fue auditar el propio gate antes de obligar a usarlo, y encontró dos defectos:** (1) su última línea era `git push origin main`, exactamente lo que el paso 6 prohíbe desde el mismo día — el script que existía para hacer cumplir la regla la incumplía en su código; (2) **verificaba el árbol de otra sesión y daba verde**, el lado peligroso de [[suite-estatica-miente-en-worktree-secundario]]: dio los tres verdes desde un worktree en `06627082` mientras el contenedor compartido servía el principal en `081a33c8`, commits distintos. Un gate obligatorio que avala con evidencia ajena es peor que no tener gate; hoy exporta `LPS_CODE_ROOT` y un `COMPOSE_PROJECT_NAME` propio, y se comprobó que el conteo del audit ya responde al árbol local (4.078 → 4.081 al mutarlo). Entregado con su mutación ejecutada: árbol sucio → `DENEGADO`, `RC=1` **leído sin tubería** — leerlo *con* tubería devolvió 0 y por un momento pareció que no denegaba, el mismo error que el script previene, cometido al medirlo. **Laguna anotada y no cerrada:** la mutación de prueba —hex crudo con `!important` en un adaptador— **no tumbó el gate estático**, porque `docs/design-system/audit-baseline.json` tolera `totalViolations: 7161` y el código va por 4.081: ~3.080 violaciones nuevas de margen antes de que el audit bloquee. Bajar esa baseline es cambio de contrato y merece frente propio · [[publicar-sh-el-gate-ejecutable]], [[suite-estatica-miente-en-worktree-secundario]], [[verificas-un-arbol-y-publicas-otro]]
- 2026-08-18 · ingest · **Escritas las dos páginas que le faltaban a la tarjeta móvil de programación**, el hueco que el undécimo pase de veracidad había anotado sin cubrir (~11 commits entre el 2026-08-14 y el 18 sin una sola nota). Se parte en dos porque son dos hechos: [[tarjeta-movil-e2-bis]] es la **referencia** —bajo 1180 px (`UMBRAL_CARDS`, `aia_ui/view-switch.js:6`) los dos módulos dejan la grilla y la tablet cae de ese lado; cinco elementos en la cara visible y el resto en un `<details>` **nombrado por su contenido** («Ver fechas y presupuesto» en Semanal, `hot.js:3501`; «Liberar restricciones» en Intermedia, `hot.js:4376`)— y [[una-decision-escrita-no-llega-sola-al-codigo]] es la **trampa**. Nada se copió de la spec: cada afirmación se verificó contra el código de hoy, y la spec describe el punto de partida, no el resultado. Verificado así: el desplegable existe en los dos módulos; la cara visible lleva los cinco elementos con el capítulo **separado** por `AIACardTitle.separarCapitulo()` y no truncado; el campo editable de Semanal **cambia con la fase** (`hot.js:3495-3500`); Intermedia pasó de solo lectura a liberar sus siete restricciones (`construirDetalleRestricciones`, `hot.js:4370`) consumiendo las reglas de `aia_ui/enablement-rules.js` en vez de replicarlas; y el candado I4 por Responsable AIA se comprueba **en el propio listener móvil** (`hot.js:4558`), no solo en el de la grilla. **El hallazgo que más vale de la referencia:** la línea de foco no hubo que inventarla —ya se calculaba y solo se entregaba al lector de pantalla, así que **quien veía la pantalla obtenía menos que quien la escuchaba**—; hoy se pinta en las dos tarjetas (`.ps-mobile-foco`, `.pi-mobile-card__foco`). **La trampa:** E2 decidió el modelo A el 2026-08-07 y **nunca llegó al código** — lo construido era exactamente la alternativa que había descartado por escrito, y pasó **una semana** sin que nadie lo notara; no lo destapó releer la spec sino **medir la tarjeta ya construida** (562 px, 1,5 por pantalla, ~17.000 px de scroll para 31 tarjetas). El detalle que lo hacía difícil de ver lo comprobé yo en `90dc5a5d~1` y no lo tomé de la spec: había un `details` en el código que **no era un desplegable** sino el nombre de una variable para un `<dl>` de campos siempre visibles (`var details = document.createElement('dl')`), así que un `grep details` daba un acierto y confirmaba la creencia equivocada. Actualizado [[lps-dominio]] con una sección «En móvil no hay grilla», porque el mapa es lo que se lee antes de tocar el área y no mencionaba que bajo el umbral no hay Handsontable. **Un límite declarado:** las alturas finales —360 px en Semanal (+10,8 % sobre lo estimado) y 269 en Intermedia (−2,2 %)— y la lección de que **el espaciado entre bloques fue el mayor consumidor de altura** vienen de la medición registrada en la spec; no se re-midieron aquí, porque exigen navegador. El lint cazó de paso un defecto real de mi primera versión —`MULTIHECHO`, cinco hechos numerados en la página de referencia—, reescrito como tabla · [[tarjeta-movil-e2-bis]], [[una-decision-escrita-no-llega-sola-al-codigo]], [[lps-dominio]]
- 2026-08-18 · ingest · **Los correos de restablecimiento no llegaban a `@aia.com.co`, y la aplicación no tenía ningún defecto.** El FortiMail del destinatario (`fml2.its-consultores.com`, por delante de su Microsoft 365) aceptaba el mensaje con un `250` y lo descartaba: sin rebote, sin cuarentena visible y sin que llegara nunca a Microsoft —comprobado con la cuarentena de M365 vacía—. Todo el instrumental daba verde: auditoría `RESET_CLAVE_ENVIADO`, Brevo «Entregado» con Message-ID, IP limpia en cinco listas negras, y SPF/DKIM/DMARC publicados y resolviendo. **Lo resolvió una medición de tres envíos con el mismo remitente y contenido, cambiando solo la salida:** por Brevo llegaba a Gmail pero no a `@aia.com.co`; por el `sendmail` local del hosting llegaba a los dos. Que Gmail —severísimo con la autenticación— aceptara el de Brevo es lo que descartó la hipótesis fácil: no era la firma. Lo que decide es la **categoría de la IP**, pool de envío masivo frente a IP de hosting. Por el camino se arregló un defecto real que resultó no ser *el* defecto (Brevo reescribía el `From` a un subdominio suyo por no poder acreditar un `@gmail.com`; se autenticó el dominio y se cambió el remitente). Implementado `MAIL_TRANSPORT` con TDD (rojo 4/4 antes), desplegado a producción **solo el archivo del correo** —no los 121 commits acumulados desde el 13/08, que quedan pendientes y con el CI en rojo— y recepción confirmada por el usuario · [[el-filtro-corporativo-acepta-y-descarta]], [[variable-vacia-tapa-el-env]]
- 2026-08-18 · ingest · **El desplegable de celda de Programación Intermedia no obedecía al teclado, y lo peor no era eso: al desplazar la grilla se quedaba clavado sobre OTRA actividad mientras seguía editando la de origen.** Auditoría pedida por el usuario sobre la columna Sub-Contratista, medida en el navegador antes de tocar nada: **Enter no confirmaba, añadía otro subcontratista** —dos pulsaciones dejaban «TRANSCAR ANTIOQUIA SAS, ARQUITOP SAS» sin que nadie eligiera el segundo, porque al ocultarse el ya elegido la opción activa se corre sola, y no existía ninguna tecla que confirmara—; **Tab confirmaba pero no avanzaba** (en celda normal sí: columna 1 → 2, medido) y dejaba el foco en `<body>`; **← y →** cerraban el editor y destruían lo escrito en el buscador; **↑ y ↓** con la lista cerrada se comían la pulsación sin editar ni navegar. Reparado con un contrato de teclas **único para las dos variantes** del editor, que es compartido con `/programa-general-actualizar`. **Dos hallazgos de implementación que no se ven desde fuera:** (1) el reanclaje **no puede colgarse de eventos de DOM** —el desplazamiento virtual de Handsontable no emite un `scroll` utilizable, ni en `.ht_master .wtHolder` ni en `document` en captura—, así que va por los ganchos `afterScrollVertically/Horizontally`; (2) el foco se devuelve **al cerrar y no al pulsar**, porque `finishEditing()` no cierra en el acto y la destrucción posterior de Tom Select se llevaba el foco — con Tab no se notaba (el cambio de selección de HOT reenfoca solo) pero con Enter y Escape, que confirman sin moverse, el teclado quedaba fuera de la tabla. De paso, el pintado de opciones **deshacía el escapado** del texto (`&lt;` → `<`) antes de meterlo en `innerHTML`, con nombres que teclea una persona en otro módulo. **La excepción `tom-select-editor-light-skin` se retira**, no se renueva: esperaba «un lote transversal de vendor» que ya existía a medias —`tom-select-premium-aia.css` en `@layer vendor`, que la pantalla hermana ya cargaba—, y su otra razón («compartido con Listado de Actividades») había caducado el 2026-08-04 con el PDC v1; faltaba solo el `<link>`, porque `aia-design-system.css` importa el adaptador de Select2 pero **no** el de Tom Select. Fuera 37 hex sueltos. Verificado: 6 pruebas nuevas de navegador sobre las dos variantes, suite estática 8/8, PHP 24/24, y las **dos capturas de referencia pasan sin regenerarse** —el editor sale cerrado en ellas, así que la autorización de cambio visual que el usuario dio no hizo falta—. **El paso 5 del gate se justificó solo:** al integrar `origin/main` apareció `tests/test_mail_transport.php` sin `// @requiere: <nivel>`, que **aborta la corrida entera** en vez de saltarse el archivo, dejando `main` en rojo para cualquiera; se le puso `puro` —deducido de lo que el archivo usa, solo autoload y `putenv`, no de una preferencia— y queda dicho aquí porque es de otro frente. **Lo que NO se hizo, a propósito:** unificar las dos clases casi gemelas, sacar «➕ Crear» de la lista de opciones y rebajar el botón LIMPIAR; todo eso lo rehace el control único acordado y hacerlo dos veces es pagar dos veces · [[restart-devuelve-el-montaje-a-la-raiz]]
- 2026-08-18 · ingest · **La cola de pendientes entra a la wiki como tablero de control, y el orden lo fijó el usuario: Fases 0 (mudanza del repo) y 0b (vault como herramienta) van primero; todo lo demás —chips incluidos— espera.** Sesión de coordinación: inventario de planes, specs y sesiones (42 goals, 7 frentes abiertos, 49 decisiones del 3-ago, plan SiteGround), matriz esfuerzo/impacto, y 22 decisiones del usuario registradas en `decisiones/<frente>.md`. Tres páginas nuevas: [[TASKS|Cola de pendientes]] (el tablero), [[programa-design-system-en-cuatro-fases]] (conclusión del usuario: el DS no está bien definido, implementado ni controlado — se rehace por programa F0–F3 y los 15 gates se reemplazan) y [[programa-general-actualizar-es-otra-herramienta]] (aclaración que tumba la premisa de D-VOC-3: no son dos vistas de la misma tabla). Vista nueva «Abierto ahora» en `paginas.base` filtrando `estado: abierto`. Hallazgos de la pasada: `semana-fija-visual` ya estaba publicado (`4aaeaea0`) aunque su goal decía «en espera»; D-7 (test.C en CI) llevaba resuelta desde el 12-ago sin aplicarse · [[TASKS|Cola de pendientes]], [[programa-design-system-en-cuatro-fases]], [[programa-general-actualizar-es-otra-herramienta]]
- 2026-08-18 · ingest · **Fase 0 ejecutada: el repo se mudó del SSD externo a `/Users/felipebenitez/Developer/lps-aia`, y la mudanza se verificó antes de tocar el original.** Orden del usuario tras elegir interno sobre iCloud (advertido el riesgo de `.git` bajo el sync: desalojo por «Optimizar almacenamiento», copias en conflicto con sesiones concurrentes). Método: contenedores abajo → rsync -aH → verificación (fsck limpio, `git status` idéntico, 2.7G iguales; los únicos «errores» fueron los sockets `fsmonitor--daemon.ipc`, artefactos de runtime que no se copian) → delta final con el daemon apagado → cutover renombrando el original a `lps-aia.pre-mudanza-2026-08-18` (respaldo, no borrado). Después: `git worktree repair` de los 6 worktrees, montaje por defecto de `docker-compose.override.yml:27` a la ruta nueva (único archivo funcional con la ruta vieja; el otro hit era un comentario histórico), stack arriba con la BD intacta (volumen `htdocs_db_data`, nunca estuvo en el SSD), web HTTP 200, PHP 24/24, suite estática verde, wiki 145 páginas sin hallazgos. Trampa conocida que esta mudanza esquiva: [[suite-estatica-miente-en-worktree-secundario]] — el default nuevo apunta al checkout principal mudado, así que `LPS_CODE_ROOT` sigue siendo obligatorio solo para servir worktrees a propósito · [[TASKS|Cola de pendientes]]
2026-08-19T00:41:42Z | ingest | trampa del gate de publicacion medida al publicar 6abe2436
- 2026-08-19 · ingest · **Wiki v2: el vault entero queda etiquetado y navegable, y las seis tandas se publicaron por separado.** Esquema nuevo con `capa` (fuente/wiki/esquema), diecisiete `tipo` y siete `tags` de vocabulario cerrado; frontmatter en las 413 fuentes de `docs/`, `goals/`, `decisiones/` y la raíz; las 13 áreas con su MOC; 13 vistas Bases, 3 canvas y un dashboard. **Ningún cuerpo se tocó, y es una cuenta y no una promesa: `413 files changed, 4171 insertions(+)` y cero borrados.** Lo que más enseñó la tanda fue aplicar, no diseñar: seis defectos de deducción que solo aparecieron al correr el backfill sobre archivos reales —entre ellos uno **heredado del lint v1**, donde `\s*` se comía el salto de línea y un campo vacío devolvía como valor la línea siguiente; llevaba ahí desde que se escribió porque hasta ahora nunca hubo un campo vacío—, más dos falsos positivos del lint sobre sintaxis válida (`.canvas` sin indexar y el `\|` de alias en tablas). **Dos números que costaron una decisión cada uno:** «217 fuentes sin resumen» resultó ser un fallo de mi propia deducción y no del repo —los planes abren con una cita y la regla se paraba una línea antes del `**Goal:**` que buscaba—, y quedaron 16; y «los 9 `tipo: mapa` son los MOCs» era falso, eran 7 de 9, lo que dio la vuelta a la decisión sobre el tag `moc`, que acabó saliendo del vocabulario porque `tipo: mapa` ya significa MOC. **Lo que NO se hizo, declarado en vez de disimulado:** ningún plugin de comunidad (decisión del usuario) y ningún grupo de color del grafo, porque no hay forma de comprobar esa consulta sin abrir Obsidian y el criterio fue que sin verificación no se escribe — el mismo criterio que retrasó una tanda las vistas Bases hasta poder verificar `containsAny` contra el binario de Obsidian instalado · [[wiki-operacion]], [[TASKS|Cola de pendientes]], [[worktrees]], [[datos]], [[bi]], [[admin]], [[procesos-y-sesiones]]
- 2026-08-19 · veracidad · áreas revisadas: design-system, lps, deploy · 4 páginas verificadas contra el código, 0 corregidas, 0 derogadas · **Alcance por rotación, medido y no supuesto:** la alarma saltó con 69 commits, pero al desglosarlos el código de producto tocado desde el pase anterior son **8 archivos**, no 69 commits de deriva: `public/css/programacion-intermedia.css`, `tom-select-premium-aia.css`, `public/js/HandsontableTomSelectEditor.js`, `modules/programa_general/hot.js`, `tablet-viewport-scale.js`, `src/Controllers/Auth/LoginController.php`, `src/Services/Mail/SmtpMailer.php` y `AGENTS.md`. El resto del recuento es el propio backfill de frontmatter de la wiki v2, que tocó 413 archivos de `docs/` **sin cambiar una línea de contenido**. Verificadas una por una, citando archivo y línea: [[css-layer-cascade]] (el bloque `@layer components` sigue en `programacion-intermedia.css:1069` y `programacion-semanal.css` lo usa en 9 sitios), [[el-filtro-corporativo-acepta-y-descarta]] (`SmtpMailer::buildMailer()` existe en la línea 35 y `MAIL_TRANSPORT` se lee en la 38), [[guard-valida-declaracion-contra-si-misma]] y [[regla-inalcanzable-parece-regla-sin-probar]] (los dos bloques citados de `hot.js` siguen ahí en sustancia). **Un hallazgo sobre el instrumento, no sobre la wiki:** las citas ancladas a número de línea envejecen aunque la afirmación siga siendo cierta — `hot.js` creció y las dos referencias apuntan ya unas líneas antes. Y **la alarma cuenta como deriva de código los commits que solo añaden frontmatter**, que por construcción no pueden volver falsa una página; queda escalado, no tocado, porque cambiar lo que mide una alarma no se decide desde el frente que la disparó · [[css-layer-cascade]], [[el-filtro-corporativo-acepta-y-descarta]], [[guard-valida-declaracion-contra-si-misma]], [[regla-inalcanzable-parece-regla-sin-probar]]
- 2026-08-19 · ingest · **El inventario total del design system (DS-F0) queda publicado: 68 hallazgos con archivo y línea, y cuatro de los siete críticos no son de código sino de mecanismo — el sistema no puede ver su propia deuda.** Frente `ds-f0-auditoria`, publicado en `1af2e9ac`. 41 pantallas censadas y verificadas contra `public/index.php` (201 rutas) y `admin/public/index.php` (56); solo 16 tienen escenario declarado. Lo que cambia la lectura de todo lo demás: contar `!important` no dice nada — de los 1 520, **903 (59%) pelean contra CSS de proveedor** (la salida que el contrato prevé y exime) y **617 (41%) contra selectores propios o contra primitivas `aia-*`**, que ningún adaptador arregla. Los cuatro gates ciegos: `audit-baseline.json` tolera 7 161 hallazgos contra 3 896 reales (`hardcoded-hex` puede multiplicarse por siete sin ponerse rojo); una regla ausente de `maxViolations` **no se evalúa nunca** porque el gate itera las claves que hay; `pdc-app/` no está en `scanRoots`, así que el Plan de Compras aporta cero hallazgos; y su gate propio comprueba que la cadena `var(--ds-` aparezca **al menos una vez** y además no lo invoca ningún script ni CI. Handsontable aparte: **63 de sus 85 selectores no los alcanza ninguna hoja nuestra** —el menú de filtros entero, el contextual, los comentarios, los editores— y el vendor trae su propia paleta clara con la que los pinta; cinco módulos `pilot` dependen de eso. Dos trampas nuevas medidas en el propio cierre · [[publicar-sh-choca-con-dos-worktrees-verificando]], [[vault-en-raiz-colisiona-nombres-de-archivo]]
- 2026-08-19 · ingest · **DS-F1 se parte en cuatro contratos y arranca por la escala de estado; el brainstorming destapa que el contrato de estados describe un producto que ya no existe.** Frente `ds-f1a-estado`, decisiones del usuario en conversación directa: partir DS-F1 (estado/severidad, stacking, primitivas, tokens), diseñar vocabulario y escala **juntos**, **tres niveles** de gravedad, y **recuperar `No Requerida`**. Lo medido sobre las 65 549 filas de `programa_consolidado`: la gravedad real está muy concentrada —urgente 7,8%, atención 9,0%, sin acción 83%—, así que un cuarto nivel tendría que partir un grupo de 110 filas. Y el hallazgo que excede el frente: **el 51,1% de las filas tiene un estado que `state-semantics.json` no declara**; el contrato declara `Con Alerta Restricciones`, que **no existe en ninguna fila**, mientras `En Liberación de Restricciones` (8,3%) no está declarado en ninguna parte. `No Requerida` (18,8%, 10 de 16 proyectos) está marcada como eliminada por `database/patches/20260527_remove_no_requerida_state.sql`, que recorre proyectos por `Base_de_Datos` — la arquitectura de prefijos anterior a las tablas globales—, y `ProgramaGeneralController.php:257` sigue contándola como lookahead. Cuatro documentos con jurisdicción sobre los estados y ninguno coincide · [[TASKS|Cola de pendientes]]
- 2026-08-19 · veracidad · áreas revisadas: design-system, qa-y-gates, programacion-intermedia, programa-general, laboratorio-design-system · 19 páginas · 0 corregidas, 0 derogadas — verificado contra `docs/design-system/state-semantics.json` (axisRules, execution-blocked→urgent), `public/css/styles.css:3658-3733` (fondo=`--ds-state-tint-*`, ya no `--ds-cell-state-*`), `public/css/design-system/components/severity-rail.css` y `docs/design-system/component-catalog.json:315-357` (maturity candidate, visualApproval pending): ninguna de las 19 páginas del área afirmaba la escalera vieja, los niveles viejos de PI ni que el color codificara gravedad — las que tocan el tema (`guard-valida-declaracion-contra-si-misma`, `dos-capas-de-tokens`) hablan de Programa General, no de Intermedia, y siguen ciertas · [[design-system]], [[qa-y-gates]]
- 2026-08-19 · ingest · **Las dos páginas de estado iban atrasadas por la misma causa: se escribieron desde un worktree, y un worktree no es el proyecto.** `memoria/goals/estado.md` describía 26 goals con corte del 10-ago cuando ya había 56, y `TASKS.md` daba por activos cinco frentes que habían cerrado y publicado ese mismo día — su autor tenía el árbol 114 commits por detrás de `origin/main`. Releídas las dos contra `origin/main` y reescritas: 34 cerrados, 12 abiertos, 10 en plantilla. Segundo hallazgo, independiente: `cola-de-pendientes.md` y `TASKS.md` **decían ambos ser «la fuente única de pendientes»**, que es una contradicción con coste —dos listas divergen y entonces no manda ninguna—; se reparten por pregunta, fases y su orden en la cola, pendientes en `TASKS.md`. Y la sección «Qué viaja en git» de `estado.md` llevaba desde el 18-ago prescribiendo una acción caducada (añadir la excepción del goal al `.gitignore`) que `9711ae3f` había vuelto innecesaria. Depuración de forma: `openspec/` y `backups/` —tooling vendorizado y volcados de BD, ambos ignorados por git— salen del vault en `.obsidian/app.json`; eran 12 de los 24 hallazgos del lint. Los otros 12 se cerraron con el backfill y las 10 fechas que no se pueden deducir de un archivo sin alta en git · `npm run test:wiki` RC=0, sin hallazgos, 161 páginas y 443 de 446 fuentes · [[goals/estado]], [[TASKS|Cola de pendientes]]
- 2026-08-20 · ingest · **Frente 1 de deuda de CI cerrado y publicado (`13ae83f1`): las actions ancladas por SHA, Dependabot semanal, timeouts 20/60 y actionlint como gate del propio workflow.** Origen: contraste con la mejor práctica (corpus NotebookLM de 50 fuentes sobre ~320; Pareto de 8 brechas). Tres hallazgos del cierre: (1) el contrato `visual-ci-contract.test.mjs:255` **fijaba el tag mutable** `upload-artifact@v4` — el guard custodiaba justo la forma vulnerable; pasó a exigir SHA + comentario de versión. (2) actionlint solo encontró un hallazgo en 303 líneas (SC2034, variable de bucle sin usar): el YAML estaba mejor de lo esperable. (3) `publicar.sh` denegó tres veces por causas legítimas y distintas —contenedor sirviendo otro árbol, wiki sin frontmatter v2, merge sin declarar— y las tres eran el guardarraíl funcionando, no fricción. Condición de hecho contra la realidad medida: «cero rojos nuevos», porque `main` ya venía rojo en presupuestos por `initializationMs` (frente runtime-budgets); tanto el PR #6 (`32392721353`) como la primera corrida de `main` (`32394566769`) cumplieron. Diferidos con dueño en [[TASKS|Cola de pendientes]]: G4 path filters, G7 paralelización, G8 job summaries, zizmor, renombre `design-system.yml`→`ci.yml`, y dos decisiones de Felipe (G6 branch protection; hook `composer test` en host) · [[goals/deuda-ci-frente-1/goal]], [[docs/superpowers/specs/2026-08-20-deuda-ci-design]]
- 2026-08-20 · ingest · **El Control Tower se abre al Director de Obra (`828483a4`, publicado en `26dba271` y desplegado a pruebas y producción el mismo día).** Petición de Felipe que primero pareció ya resuelta —«habilitar en producción para el Administrador» estaba hecho desde el 2026-08-13— y era para el rol `D`: la capacidad `internal.bi.preview` pasa de solo `A` a `A` y `D` en `RbacManager.php:30`, con la pregunta de alcance grillada antes (¿todos los D o una persona? — todos). Verificación: `test_bi_preview_gate` 16/16, PHPStan limpio, HTTP local D→200 y R→404, y en ambos servidores el gate evaluado con el binario 8.3 da A/D `true` y R/V `false`; home 200. El deploy trajo dos hallazgos anotados en [[TASKS|Cola de pendientes]]: la migración `20260819_recalculo_estados.php` viaja pero queda sin aplicar (recálculo de datos bloqueado por diseño, exige el sí de Felipe sobre su dry-run) y producción tenía drift real —el arreglo de `MAIL_TRANSPORT` aplicado a mano el 2026-08-18— que quedó en stash `pre-deploy-20260820-185447` porque `21243c7e` lo trae versionado. `publicar.sh` denegó una vez por el merge de integración sin declarar: guardarraíl, no fricción · [[control-tower-oculto-mientras-se-desarrolla]], [[torre-de-control-bi]], [[produccion-deploy]]
- 2026-08-20 · ingest · **Recálculo de estados aplicado en pruebas con el sí de Felipe sobre el dry-run.** Los dos entornos contaron casi lo mismo en seco (pruebas 40.664 / producción 40.198 filas de 65,5k, ~62 %), y el grueso es retitulación a la taxonomía nueva —`Fuera de Ventana` (~25k), vacíos→`Capítulo` (~7,7k)— no recálculo sorpresivo: solo ~16 filas cambian de juicio a `Atrasada` en producción. En pruebas: respaldo propio verificado (65.549 = 65.549, `coinciden: si`), `--apply`, y el dry-run posterior en **0 filas por cambiar** — la prueba de hecho es la re-corrida en seco, no el código de salida. Smoke: login R, `/programa-general` y `/programacion-semanal` en 200 sin líneas nuevas en `php_errorlog`. Producción queda esperando la revisión visual de pruebas y el visto propio de Felipe, como exige la cabecera de la migración · [[TASKS|Cola de pendientes]], [[produccion-deploy]]
- 2026-08-20 · ingest · **Frente 2 de deuda de CI cerrado: la imagen PHP se pre-construye con buildx y cache `type=gha`, y compose dejó de reconstruirla — 81 s → 20 s el build del estático (−75 %) y 93 s → 72 s el runtime (−23 %), medido en caliente sobre la corrida `32406977581`.** Alcance A a propósito (decisión de Felipe tras sincerar el premio): el Dockerfile de una sola etapa no se tocó y `composer install` sigue sin cachearse porque `COPY . .` lo precede — cachearlo exigiría reordenar y ajustar el contrato por hash, por ~30-40 s más. Dos hallazgos: el cache gha se comparte **entre jobs de la misma corrida** (el runtime ya bajó a 72 s en la corrida fría, sembrado por el estático minutos antes), y Dependabot dio fruto a horas del Frente 1 — tres PRs subiendo checkout/setup-node/upload-artifact a v7, cuyo merge en `main` fue justo lo que dejó el PR #10 en CONFLICTING y sin corrida hasta integrar. La trampa aprendida: **un PR CONFLICTING no dispara `pull_request`** — el silencio del CI no es cola, es conflicto · [[goals/deuda-ci-frente-2/goal]], [[TASKS|Cola de pendientes]]
- 2026-08-20 · ingest · **Recálculo de estados aplicado también en producción, tras la revisión visual de pruebas por Felipe y con su autorización explícita (auditada por el guardarraíl `AUTORIZADO_POR_FELIPE=1`, que denegó la primera ejecución sin la marca — funcionando, no fricción).** Doble red antes del apply: dump completo de `programa_consolidado` (24 MB, `Dump completed`, `~/backups/programa_consolidado-prerecalculo-20260820-190903.sql`) además del respaldo propio de la migración (65.505 = 65.505, `coinciden: si`). Apply de 40.198 filas y dry-run posterior en **0 por cambiar** (65.393 igual + 112 sin semana activa). Home 200, `php_errorlog` sin líneas nuevas. Con esto el pendiente del deploy del 2026-08-20 queda cerrado; restan solo el stash de SmtpMailer y los 7 `.bak` por limpiar · [[TASKS|Cola de pendientes]], [[produccion-deploy]]
- 2026-08-20 · ingest · **Limpieza post-deploy en producción confirmada por Felipe:** eliminado el stash `pre-deploy-20260820-185447` (SmtpMailer, superado por `21243c7e` versionado y verificado en el archivo servido) y los 7 `.bak` de `indicadores.view.php` del 2026-07-23. Quedan dos stashes anteriores **no tocados** por no estar en la confirmación: `pre-da-porto-prod-20260710` y un WIP sobre `8b95f08` — revisarlos merece su propia mirada antes de borrar. Árbol de producción limpio · [[produccion-deploy]]
- 2026-08-20 · ingest · **Borrados los dos stashes viejos de producción con el sí de Felipe, tras revisión del contenido:** `pre-da-porto-prod-20260710` era el `require_once` de `CommitmentLockGuard` en `ProgramacionIntermediaController`, ya incorporado literal al archivo versionado (`:10`); el `WIP on main: 8b95f08` era un `.htaccess` alternativo sobre una base antigua más permisos de scripts docker, obsoleto frente al `.htaccess` versionado que produce los 200 de hoy. `git stash list` en 0, árbol limpio y alineado con `origin/main`. Producción queda sin drift de ninguna clase · [[produccion-deploy]]
- 2026-08-20 · ingest · **`ds-f1a-estados-severidad` cierra: la adaptación al contrato de 3 niveles ya estaba publicada y lo que faltaba era mirarla en pantalla.** La rama del frente quedó íntegra en `origin/main` (`2fc5998e` ⊂ `0a944cd4`), y la verificación post-fix que `a23640b8` dejó como pendiente explícito se hizo hoy reejecutando las sondas contra el árbol publicado, a 1180×820 dark por `/dev/entrar`: Intermedia da `railGrosor: none` en `healthy`/`neutral` y solo dos tratamientos dibujados (urgente 6px, atención 4px); Programa General pinta sus siete estados vivos sin un solo par idéntico, con `fuera-de-ventana` en teal `#134841` y `Con Alerta Restricciones` reducido a chip de filtro con 0 filas. La tensión que la spec anotó —en el fixture de Intermedia 6 de 9 filas dibujan barra— no satura en pantalla a juicio del ejecutor: el filete es un canto fino y la ausencia se lee de inmediato; las capturas quedan en `goals/ds-f1a-estados-severidad/evidence/` para veto de Felipe. Lección del cierre: la captura de las 11:27 cazó un bug (`feafc211`) pero quedó ANTERIOR al fix — una captura pre-fix no verifica el fix, y por eso la verificación se repitió entera · [[goals/estado]], [[TASKS|Cola de pendientes]]
- 2026-08-20 · ingest · **Interruptor del Control Tower administrable desde `/admin`, ejecutado con subagentes en 4 tareas (`2e6c8773`→`ae4919ac`).** Diseño grillado con Felipe: interruptor **global** (no por rol ni por usuario) y con el switch apagado **el Admin sigue entrando** — condición puesta por él, y es exactamente la lección del ocultamiento del 2026-08-13 que dejó fuera a quien lo pidió. Piezas: tabla `general_flags` (clave-valor global, sin `project_id` a propósito, con auditoría del último cambio), `App\Core\FlagsService` con fail-safe —flag ilegible = apagado, nunca 500—, el gate leyéndolo solo para roles no-Admin, y la pantalla `/admin/modulos` con CSRF y lista blanca. **El hallazgo caro lo destapó la revisión final de rama, no las revisiones por tarea:** `ModulosController` era el único controlador del panel que no llamaba a `requireAdminRole()` —el constructor de `AdminController` solo exige sesión y cuenta activa, el guard de rol es un método aparte—, así que un rol `D`/`R`/`V` con sesión de admin recibía 403 en todas las pantallas menos en la del interruptor, donde podía moverlo. La causa de que 4/4 en verde no lo viera: **al test le faltaba el rol denegado**, que el spec pedía y AGENTS.md exige en toda ruta protegida. Arreglado en `ae4919ac` con el guard en el constructor (cubre métodos futuros) y el caso GET con `test.D`. Segundo hallazgo de proceso: el contenedor `app` estaba montado sobre el worktree de **otra** sesión, así que las verificaciones se repitieron contra el mount correcto y las re-corrí yo mismo antes de creerlas. Verificación final: 6/6, 22/22, 7/7, suite `puro` 24/24 + PHPUnit en verde, PHPStan limpio · [[control-tower-oculto-mientras-se-desarrolla]], [[docs/superpowers/specs/2026-08-20-interruptor-control-tower-admin-design]]
- 2026-08-20 · ingest · **Interruptor del Control Tower desplegado a pruebas y producción** (`26dba271`→`6fa3cff1`, tar previo con manifiesto y rotación en ambos). Aquí el `composer install` **sí era obligatorio** y se comprobó de la única forma que vale, como en el despliegue del 2026-08-13: `grep -c` sobre `vendor/composer/autoload_classmap.php` daba **0 antes y 1 después** para `FlagsService` y `ModulosController` en los dos servidores — con `--optimize-autoloader` las clases nuevas no existen para PHP hasta regenerar el classmap, y entre el pull y el composer `/admin/modulos` habría respondido 500. La migración `20260820_general_flags.sql` se aplicó por el cliente `mysql` (sin `DELIMITER`, comprobado antes) y dejó la fila sembrada en `1` en ambas bases. **La verificación que de verdad prueba el frente no es el smoke sino el toggle**: apagando el flag, el gate da `A: true, D: false`; encendiéndolo, `D: true` — medido en pruebas y en producción, y el flag quedó restaurado en encendido en las dos (`actualizado_por='migracion'`). Home 200 y `/admin/modulos` 302 sin sesión. Nota de coordinación: el deploy no necesitó el contenedor local, que estaba cedido a la sesión hermana del frente de coloreado · [[control-tower-oculto-mientras-se-desarrolla]], [[produccion-deploy]], [[docs/superpowers/specs/2026-08-20-interruptor-control-tower-admin-design]]
- 2026-08-20 · ingest · **«¿Cómo logramos que nunca se tenga que usar overflow?» — la pregunta de Felipe convirtió un frente de color en una regla de sistema.** `replanteo-coloreado-estados` cerró con el chip sólido como portador de identidad (paleta auditada WCAG AA + manual AIA: naranja/ámbar/violeta/teal del manual, rojo custom porque el del manual falla AA con 4.01) y el filete homogéneo en los tres módulos con marcador `ready`. Pero lo que perdura es la regla: **nada se recorta sin que el usuario lo sepa** — el texto cabe, envuelve entre palabras, o se acorta con un nombre corto OFICIAL del contrato (`displayShort`, 10 aprobados por Felipe uno a uno). Un censo de las 22 tablas de la app destapó ~20 recortes, 4 fragmentaciones y ~20 tamaños fuera de rampa; tres olas en workflows dependientes cerraron lo grave. **Cuatro hallazgos que valen más que el diff:** (1) tres tamaños eran invisibles a cualquier censo por venir en `em` encadenados — `.ps-origin-badge` resolvía a ~7,5px reales, bajo el piso de 11px de PRODUCT.md; (2) silenciar el detector con `ignoreValues` era esconder el contrato en otro archivo — el detector lee la rampa del frontmatter de DESIGN.md, y declararla ahí llevó los hallazgos de 52 a 1 y permitió RETIRAR los silenciamientos previos; (3) dos lecturas visuales del asistente alucinaron tarjetas claras que una rejilla de 60 píxeles desmintió (56/60 oscuros) — la evidencia de detalle va a 2x desde entonces; (4) el `overflow-x: hidden` de Semanal llevaba `!important`, o sea que el recorte era además irreversible. Efecto no previsto: Intermedia muestra sus 9 filas donde antes cabían 5 · [[goals/replanteo-coloreado-estados/censo-tablas]], [[TASKS|Cola de pendientes]]
- 2026-08-21 · veracidad · áreas revisadas: design-system, programacion-semanal, programacion-intermedia, programa-general, qa-y-gates, entorno-y-despliegue · 8 páginas · 1 corregida, 0 derogadas · **Alcance por rotación, medido:** la alarma saltó con 52 commits, pero el código de producto tocado desde el pase del 2026-08-19 son las hojas de los tres módulos, `styles.css`, `tokens.css`, la primitiva `state-tooltip`, los tres `hot.js` y el workflow del gate. Verificadas una por una citando archivo y línea: [[tinta-de-marca-sobre-superficie-oscura]] sigue cierta —`.tnp-option-title` conserva `color: inherit` y el adaptador `select2.css` existe— pero **su cita de línea envejeció** (1720 → 1827) y es la única corrección del pase; [[pdc-legend-item-clase-compartida]] cierta (la clase sigue en los tres módulos más `styles.css` y `aia-design-system.css`); [[dos-capas-de-tokens]] cierta (47 `--aia-*` y 314 `--ds-*` en `tokens.css`); [[comentario-de-token-afirma-uso-inexistente]] cierta (el exportador sigue montando su paleta aparte, `ReportController.php:398-400`); [[important-invierte-el-orden-de-capas]] cierta (306 `!important` vivos en `styles.css`); [[suite-estatica-miente-en-worktree-secundario]] y [[gate-que-mide-dos-arboles-a-la-vez]] ciertas, y **confirmadas por experiencia y no solo por lectura**: `publicar.sh` me denegó hoy por montar otro árbol, que es el guardarraíl haciendo exactamente lo que la página describe (`docker-compose.yml:1`, `docker-compose.override.yml:27`). **El hallazgo del pase no estaba en ninguna página:** el `.env` enlazado que `CLAUDE.md` manda crear **se rompe dentro del contenedor** en cuanto se le apunta al worktree, porque el enlace va a una ruta del host; la app da 500 en todo y el síntoma habla de Composer, no del `.env`. Escrito como [[env-enlazado-se-rompe-dentro-del-contenedor]], con la tabla de los dos remedios que se pelean. **Límite declarado:** este pase es de lectura de código; no se midió nada en navegador porque el bloqueo del `.env` es justamente lo que lo impidió · [[tinta-de-marca-sobre-superficie-oscura]], [[env-enlazado-se-rompe-dentro-del-contenedor]], [[suite-estatica-miente-en-worktree-secundario]]

- 2026-08-12 · ingest · Cierre del frente `ci-en-verde` (F-0). Nueva [[contrato-que-exige-important-mide-la-forma]]: la última aserción roja de `test_programa_general_sprint_contract.mjs` —el archivo llevaba en rojo desde el **2026-07-17**— exigía cuatro declaraciones del chip de leyenda **con `!important`**, y eso es la forma, no el resultado: obligaba a deshacer la resta bien medida de `0a228a39`. Se reescribió contra los valores. **Lo que la hoja no puede probar es que esos valores ganen**, así que se midió el computado en `/programa-general` a 1180×820 dark: **7 elementos `.pdc-legend-item`** —el recuento se declara porque **cero no es «no aplica», es «no lo encontraste»**— y en los 7 `white-space`, `overflow-wrap` y `word-break` computan `normal` **sin ningún `!important`**. De paso, un dato que la hoja tampoco describe: `display` computa `flex`, no el `inline-flex` que declara `buttons.css:977`. Corregida en consecuencia [[pdc-legend-item-clase-compartida]], que decía que `buttons.css` «la llena de `!important`»: son **6** los que quedan en el bloque, y los de envoltura no estaban entre los necesarios; su consejo de desacoplar no cambia, porque esos 6 siguen siendo invencibles desde el módulo. Dos avisos de método que costaron tiempo: la primera mutación **no mató el test** y pareció que la aserción era laxa — era `perl -0pi -e 's///'` sustituyendo la **primera** ocurrencia del archivo, que cae en el `:where(…)` de `buttons.css:25` y no en el chip; y antes de medir hubo que comprobar por `shasum` que el CSS servido por Docker era el mismo que analiza el test. El verde que cierra la fase es el de Actions, no el local: `design-system-static` **success** sobre `65c44435` y confirmado dos runs después sobre `0e45ba1d`; `design-system-runtime` sigue en rojo y es de otro frente · [[contrato-que-exige-important-mide-la-forma]], [[pdc-legend-item-clase-compartida]], [[valor-declarado-no-es-valor-computado]], [[el-codigo-de-salida-se-pierde-en-la-tuberia]]
