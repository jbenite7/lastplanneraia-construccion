# Experiments

Backlog único de este repositorio. Lo comparten el journey `improve-app`
(`docs/IMPROVE-APP-PLAN.md`) y la biblia de flujos (`docs/flujos/`): **una sola lista de
pendientes, no dos**.

La columna `Origen` es lo que une ambos proyectos: lleva el `id` del escenario de biblia que
destapó el hallazgo, o la fase de `improve-app` que lo produjo.

**ICE** = Impact × Confidence × Ease, cada uno de 1 a 10. La confianza baja cuando el hallazgo
depende de interpretar *cuál es la conducta correcta*: esos van marcados `decide: usuario` y no se
tocan sin su visto bueno.

## Experiment Cards

*(Ninguna todavía. Una tarjeta se abre cuando un cambio se lleva a producción con su métrica
comprometida de antemano.)*

## Experiment Backlog

| Idea | Origen | Impact | Confidence | Ease | ICE | Owner | Status |
|---|---|---|---|---|---|---|---|
| El filtro «proyecto cerrado pero visible para la jefatura» está escrito en **tres sitios con tres criterios distintos**: `ProjectSelectorController::index()` usa SQL con `pm.role IN ('A','D')` (`:41`), `enterProject()` lo comprueba en PHP con el rol ya normalizado (`:122`), y `BiProjectScope::authorizedProjectIds()` usa SQL con `pm.role IN ('A','D','P')` — incluye el alias legado `'P'` que los otros dos omiten. Una cuenta con `role = 'P'` vería un proyecto cerrado en la Torre de Control y no en el selector. BI acierta (`'P'` es Director en `RbacCatalog::roleAliases()`) y el selector se queda corto | BI-003 (T5) | 5 | 9 | 6 | 270 | — | abierto |
| **Seis módulos mutan datos sin validar CSRF.** Ninguno de `SubcontratistasApiController` (4 sentencias de escritura), `ProfesionalesApiController` (5), `ControlCambiosApiController` (3), `CicApiController` (10), `CncApiController` (1) ni `CnpApiController` (2) llama a validación de token: todos autorizan solo con `rbac_guard_require_permission`, que **no valida CSRF** (comprobado: `grep -i csrf src/Legacy/rbac_guard.php` no encuentra validación dentro de esa función). Y la herramienta existe **en ese mismo archivo**: `legacy_require_csrf()` (`src/Legacy/rbac_guard.php:83-89`), que solo usan `src/Legacy/nueva_semana.php` y `eliminar_semana.php`. `AGENTS.md` exige CSRF en toda mutación autenticada | SOP-002 / APR (T4) | 9 | 9 | 7 | 567 | — | abierto |
| La operación `sanear` de Programación Semanal **muta datos sin protección CSRF**. En `SemanalApiController::save()` hay dos listas: la que exige token CSRF (`:128-129`) enumera nueve opciones y **no incluye `sanear`**, mientras `$mutatingOptions` (`:145-148`) sí la incluye — y con razón: `sanear` ejecuta `DELETE FROM …programacion_semanal` y un `INSERT` (verificado hacia `:1160-1230`). `AGENTS.md` exige mantener CSRF en toda mutación autenticada. Sí conserva las otras defensas (prefijo de sesión y política de semana), así que exige sesión válida; lo que falta es la barrera contra petición forjada desde otro sitio | PS-002 (T2) | 8 | 9 | 8 | 576 | — | abierto |
| La restricción «solo Admin y Director editan el pasado del programa general» **existe solo en el navegador**. `canEditPastGeneralProgram` aparece en `public/js/rbac_capabilities.js:57,132-135` y en la declaración de `RbacManager`, pero **ningún archivo PHP la consulta**: los endpoints de `GeneralApiController` autorizan solo con `lps.programa_general.editar` (`:139`, `:382`), que el Residente tiene. Si existe una barrera real de fechas en el servidor, no está en esta capacidad y no la encontré. Un Residente que llame la API directamente podría editar semanas pasadas | PG-pendiente (T2) | 8 | 8 | 6 | 384 | — | abierto |
| `canDeleteRows` (solo `A` y `D`) **no la consulta nadie**: `grep -rl canDeleteRows` sobre todo el árbol devuelve únicamente `src/Security/RbacManager.php`, su propia declaración. Ni servidor ni cliente. Segunda capacidad inerte tras `canSeeReports`, y esta sí protege algo destructivo | PG-pendiente (T2) | 7 | 10 | 7 | 490 | — | abierto |
| Las dos piezas del candado de semana fallan hacia **lados opuestos** ante la misma situación: si el proyecto no se puede resolver, `LpsWeekEditPolicy::allows()` devuelve `false` y cierra (`src/Security/LpsWeekEditPolicy.php:23-26`), mientras `CommitmentLockGuard::guard()` retorna y **deja pasar la mutación** (`src/Core/CommitmentLockGuard.php:33-35`, comentado como «se deja pasar por seguridad»). Cuál es la conducta correcta es decisión de producto; que sean distintas entre sí es lo que hace difícil razonar sobre el candado | CAS-006 (T2) | 6 | 9 | 6 | 324 | — | abierto · decide: usuario |
| `CommitmentLockGuard::guard(..., allowIfConfirmed: true)` **retorna en su primera línea sin comprobar nada** —ni rol, ni política, ni estado de la semana— (`src/Core/CommitmentLockGuard.php:27-29`). Confía en que quien lo active haya validado por su cuenta con `LpsWeekEditPolicy`. Medido el 2026-08-04: ninguna de las nueve llamadas actuales lo usa, así que es riesgo latente y no agujero abierto; pero un `true` puesto sin su comprobación abriría la escritura sobre semanas confirmadas a cualquier rol | CAS-005 (T2) | 7 | 8 | 6 | 336 | — | abierto |
| El Visualizador (solo lectura por definición) ve en `/programa-general` **los mismos 17 botones** que el Residente, incluidos «Actualizar Ejecución», «Descargar Corte» y «Exportar CSV»; la única diferencia por rol está **dentro** del flyout de semanas. Medido con sonda Playwright el 2026-08-04. Falta comprobar si «Actualizar Ejecución» muta datos para un rol de solo lectura — confirmarlo exige pulsarlo, así que se registra en vez de probarlo a ciegas | RBAC-001 (T1) | 8 | 6 | 5 | 240 | — | abierto |
| Las capacidades RBAC se implementan **dos veces**: en servidor (`src/Security/RbacManager.php`) y en cliente (`public/js/rbac_capabilities.js:45,156-159`), sin ningún gate que compare ambas matrices. Una divergencia produce una interfaz que ofrece acciones que el servidor rechaza, o que esconde acciones permitidas, y hoy nada lo detectaría. Candidato de alto valor a prueba automática rol a rol | RBAC-D (T1) | 7 | 9 | 5 | 315 | — | abierto |
| `canSeeReports` vale `true` para los diez roles —incluidos Visualizador y Subcontratista externo— y `grep` sobre todo el árbol no encuentra **ningún consumidor**. Es una capacidad inerte: ni restringe ni se usa. O falta la lógica que iba a diferenciar quién ve informes, o sobra la entrada | RBAC-B (T1) | 4 | 10 | 9 | 360 | — | abierto · decide: usuario |
| Cuatro de las 17 capacidades son **alias exactos** de otra, sin lógica propia: `canManageGeneralProgram`, `canManageWeeklyProgram`, `canManageMediumTermProgram`, y `canAutoDefineContracts`/`canManagePdC` sobre `canManageContracts`. El vocabulario promete una distinción entre «editar» y «gestionar» que el código no hace, así que cualquier regla de negocio que dependa de ella está sin implementar | RBAC-A (T1) | 5 | 10 | 4 | 200 | — | abierto · decide: usuario |
| El selector de proyecto normaliza roles con un `normalizeRoleCode()` privado (`src/Controllers/Core/ProjectSelectorController.php:161-179`) que solo conoce `P→D` y `U→V`, en vez de con `Admin\Core\RoleManager::cleanCargo()` como manda `AGENTS.md`, y sin consultar `RbacCatalog::roleAliases()` (`src/Security/RbacCatalog.php:13-23`), que sí traduce ocho cargos escritos en texto. Una cuenta con `project_members.role = 'Director de Obra'` entra como **Visualizador**. Falla hacia el lado seguro —no es escalada— pero pierde permisos legítimos en silencio y sin explicación para quien lo sufre | PROY-007 (T1) | 6 | 8 | 6 | 288 | — | abierto · decide: usuario |
| La invariante que el propio `ProjectSelectorController:81-85` se impone —que la lista y la entrada traten `Acceso` igual— no se cumple: `index()` filtra en SQL con `pm.role` **crudo** (`:41`) y `enterProject()` comprueba en PHP con el rol **ya normalizado** (`:122`). Para una cuenta cuyo `role` no sea literalmente `A`/`D` pero normalice a uno de ellos, un proyecto cerrado no aparece en la lista y sin embargo deja entrar por nombre. Incoherencia, no escalada | PROY-006 (T1) | 5 | 8 | 7 | 280 | — | abierto |
| La barra de progreso del selector de proyectos es `rand(0, 100)` (`src/Controllers/Core/ProjectSelectorController.php:49`): un número inventado presentado como dato de avance en la primera pantalla que ve todo el mundo. O se cablea a una métrica real o se retira | PROY-001 (T1) | 5 | 10 | 8 | 400 | — | abierto · decide: usuario |
| Cuando la sesión caduca durante una petición de datos de una grilla, el usuario **pierde el trabajo sin ver un error entendible**. `SessionMiddleware` solo responde el 401 JSON si la petición trae la cabecera propietaria `X-AIA-Expect-Json` (`src/Core/SessionMiddleware.php:91-96`); si no, manda un `Location: /login`, que un `fetch` sigue y devuelve el HTML del login como si fueran datos. Y esa cabecera la envían **solo 2 archivos** —`public/js/core/SessionTimeoutManager.js:145` y `public/js/components/notifications.js:25`—: **ninguno** de los módulos de grilla la manda (`grep -rl X-AIA-Expect-Json public/js/modules/` → 0), incluidos `programacion_intermedia/hot.js` y `programa_actualizar/hot_actualizar.js`. Encaja con la subentrega del Residente en `docs/CUSTOMER.md`: el trabajo se pierde justo cuando más caro sale | AUTH-002 (T1) | 8 | 8 | 6 | 384 | — | abierto |
| `AGENTS.md:23` afirma que «Listado de Actividades, Contratos y PDC comparten los contratos `auto/preview`, `auto/apply`, `auto/undo`, `auto/feedback` y `auto/metrics`», y hoy **solo el PDC los usa**. Las 13 rutas `auto/*` de `public/index.php:256-268` son todas `/api/pdc/` y apuntan a los métodos `*Pdc`; `SemiAutoController` tiene además **12 métodos `*Listado` sin una sola ruta que los invoque** (`grep "Listado']" public/index.php` → 0), y `semi_auto_review.js` solo lo carga `views/pdc/pdc.view.php:601`. O el contrato de `AGENTS.md` describe una intención que se quedó a medias, o el cableado de Listado se perdió: hay 12 métodos de servicio esperando rutas que no existen | T4 (redacción del plan, 2026-08-04) | 7 | 9 | 5 | 315 | — | abierto · decide: usuario |
| `/indicadores` oculta el informe solo en el navegador: el servidor no comprueba el rol. `IndicadoresController` no tiene ningún guard (ni RBAC, ni 403), y `views/indicadores/indicadores.view.php:111` declara `POWER_BI_REPORT_URL` **antes** de que `:151` decida ocultarla, así que la URL viaja en el HTML de todos los roles, incluidos los cuatro restringidos `G`, `S`, `SG`, `C`. El contraste está en el módulo hermano: `src/Controllers/Bi/BiViewController.php:179` sí responde 403 de servidor | T5 (redacción del plan, 2026-08-04) | 6 | 9 | 7 | 378 | — | abierto |

### Nota sobre el hallazgo de `/indicadores`

Conviene medir su gravedad sin inflarla ni minimizarla:

- **Lo que no es:** una filtración de datos privados. Ese informe es *publish-to-web*, es decir,
  **público por enlace por diseño** — decisión registrada y aceptada en
  `memoria/decisiones/powerbi-indicadores.md`. Quien tenga la URL lo ve, tenga cuenta o no.
- **Lo que sí es:** una regla declarada que no se cumple donde debería. El repositorio afirma que
  los roles `G`, `S`, `SG` y `C` no ven el dashboard, y esa restricción existe solo como adorno del
  cliente: se salta con abrir el código fuente de la página. Además es una **asimetría entre dos
  módulos que hacen lo mismo**, y esa inconsistencia es la que hace difícil razonar sobre permisos
  en toda la app.
- **Por qué no lo arreglo:** la regla de la biblia es registrar y seguir. Y aquí hay una decisión de
  producto detrás, que es tuya: ¿la restricción debe ser real (guard de servidor, como en BI) o era
  cosmética a propósito y lo que sobra es la promesa?

Encontrado **escribiendo el plan de T5**, antes de ejecutar ninguna tanda: es evidencia temprana de
que el método funciona.
