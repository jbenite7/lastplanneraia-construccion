# Decisiones pendientes del usuario — cierre de dark mode

**Modo acordado el 2026-08-03:** el usuario pidió no ser interrumpido durante la ejecución. Todo
lo que requiere su criterio se acumula aquí y se le presenta **al final del plan**, en bloque.

Regla que sigue esta sesión mientras tanto: donde haga falta decidir para no detenerse, se toma la
opción **más conservadora y reversible**, se ejecuta, y se registra abajo como *asumida* — nunca
como aprobada. Lo irreversible o lo que consagra un estado visual (goldens) **no se aplica**: se
deja preparado y aquí anotado.

---

## A · Requieren aprobación antes de aplicarse (nada se ha aplicado)

| # | Qué | Estado | Por qué es tuya |
|---|---|---|---|
| **A-1** | **Goldens de tabla** (task 15) | **Sin recapturar**, a propósito. El visual de Programa General está en rojo y ese rojo es correcto: refleja los cambios de hoy | Recapturar consagra el aspecto actual como «lo correcto» para siempre. Ya te envié el par (esperado / actual / diferencias) de `/programa-general`. **Lo que decides:** si el «actual» es lo que quieres, se recapturan y el rojo desaparece; si ves algo que no esperabas, se investiga antes. Ficheros en `test-output/programa-general.visual.mj-b4b64-…/`. Los demás goldens de tabla se recapturarían en el mismo lote |
| **A-2** | **Densidad compacta** (task 19) | **Aplicada** (`67f35c4`) — reversible con `git revert` | Ya la aprobaste dos veces (extender la escala + «maximizar espacio»), así que se aplicó. Te quedan por ver las capturas antes/después: `task-19-densidad/`. **Ojo al resultado honesto**: el alto de fila bajó de 36 a 24 px, pero **no se ganan filas** en PG/PI con datos reales, porque el texto envuelto domina la altura (el token es un mínimo, no un máximo). Lo que sí se arregló: las cabeceras truncadas e ilegibles de `/pdc` |
| **A-3** | **Variante B de bordes en tablas reales** | Decidida en maqueta, **no aplicada** | Aprobaste B + numéricas a la derecha sobre una maqueta HTML. Handsontable y DataTables pintan sus propios bordes: hay que verlo ahí antes de producción. **MEDIDO POR FIN (ciclo 29), que era justo lo que faltaba:** el borde que pinta Handsontable en `/programacion-intermedia` es `rgb(203, 213, 225)` —un gris casi blanco de la paleta de Bootstrap, no un token—, aplicado a **34 celdas y cabeceras** en un solo viewport, a **11,96:1 contra el fondo de celda** (`rgb(17, 26, 21)`). Es, medido y confirmado en captura, **lo más brillante del área de contenido**: la rejilla grita más que los datos que contiene, que es exactamente lo contrario de la premisa de deferencia al contenido. Los selectores concretos a vencer son `TD.htMiddle` (17) y `TH` (15, más 2 de `pi-soft-restriction-th`). **Con esto, A-3 ya no está bloqueada por falta de información:** quien la aplique sabe qué color, qué selectores y qué contraste tiene que sustituir. **Sigue sin aplicarse por una sola razón:** cambiar la rejilla altera el render y por tanto los goldens, y tú pediste explícitamente no recapturarlos (A-1). Es el mismo nudo que A-1: en cuanto autorices la recaptura, esta entrada se puede cerrar |

## B · Asumidas por la sesión, ratifícalas o revócalas

| # | Decisión asumida | Razón | Reversible |
|---|---|---|---|
| **B-1** | El barrido horario de todas las superficies corre **en esta sesión**, no como tarea programada en la nube | Tú mismo pediste que los hallazgos «se envíen a esta sesión para incorporarlas al plan»: una tarea en la nube no podría hacerlo. Además muere al cerrar la sesión, que es lo conservador | Sí — se puede mover a la nube con `/schedule` |
| **B-2** | La primera pasada del barrido horario **no arranca de inmediato**, sino cuando cierre el task en vuelo que está editando el CSS de tablas | Medir un árbol a medio cambiar produce hallazgos falsos que luego contaminarían el plan | Sí |

## C · Deuda inventariada que espera tu criterio

| # | Qué | Tamaño | Nota |
|---|---|---|---|
| **C-1** | **Las 22 ramas viejas** | Censo en `docs/superpowers/ramas-viejas-2026-08-03.md`: 22/22 sin contenido único, verificado por muestreo | Borrarlas es seguro según la medición, pero borrar ramas es tuyo |
| **C-2** | **Fase 6 — el grueso estructural** | ~2 600 hallazgos: `!important`, `css-outside-layer`, `raw-token-in-module` | Requiere que apruebes el inventario inicial de excepciones justificadas. Es la campaña larga; el punto natural de corte si quieres parar |
| **C-3** | **`pdc.css:318` — borde-acento del toast** | 1 línea | El detector lo marca en cada parada. O se suaviza el acento, o se declara intencional en config (necesita tu confirmación) |
| **C-4** | **23 hallazgos de `pdc.css` y 16 de PS** fuera de rampa tipográfica | Preexistentes, con techo en presupuesto | Parte los racionaliza el task 19; el resto entra en C-2 |
| **C-5** | **Sidecar `.impeccable/design.json` desactualizado** respecto a `DESIGN.md` | Trivial | Regenerarlo es un comando; no se hizo por no tocar el sistema de diseño sin necesidad |
| **C-6** | **Contenido de «HOMECENTER CALI» en la fila 1 del sandbox** `pdc_sandbox_e2e` | Desconocido | No coincide con el seed. Puede ser residuo de una importación manual o un bug de importación real; no se investigó |
| **C-7** | **DataTables: gatillo de filtro sin verificación visual** | Menor | Ninguna tabla alcanzable tiene ordenación activa hoy; la regla está escrita pero nadie la ha visto en acción |
| **C-8** | **`state-tint-exceptions.json` sigue anclado por número de línea** | Menor | Su hermano ya migró a firma (task 12-bis). Las 3 entradas actuales están verificadas correctas |
| **C-17** | **Cinco acciones de Programación Semanal pasaron a un menú «Más»** | Cambio visible de UX | Era la única salida: dos botones caían fuera de la pantalla y no se podía llegar a ellos. **Visibles quedan** Autoprogramar, Agregar Actividad, Confirmar Compromisos, Reabrir Semana y Registrar TNP. **Al menú fueron** Leyenda, Imprimir, Exportar CSV, Recargar y BI Semanal. Ninguna acción se perdió ni cambió de comportamiento. **Lo que decides tú:** el revisor y yo coincidimos en una duda concreta — **«Recargar» no debería estar en el menú**. Es una acción de recuperación: la buscas cuando algo no cuadra y los datos parecen desincronizados, es decir, en el peor momento para tener que abrir un desplegable. «Leyenda» sí encaja como secundaria (se consulta una vez, no repetidamente). Mi recomendación: sacar «Recargar» a la barra y dejar en el menú Leyenda, Imprimir, Exportar CSV y BI Semanal. Dilo y se cambia |
| **C-18** | **`fitActionsRowSingleLine()` es código muerto con comentario falso** | Menor, preexistente | En `hot.js:1203` la función empieza con `return; // DISABLED…` desde antes de este plan. Era la que compensaba el rango de anchos 1161–1440 px que ninguna regla CSS cubre. Hoy ya no hace falta para la semanal (el menú resuelve), pero el comentario sigue engañando a quien lo lea |
| **C-21** | **La suite de navegador está caída por entorno: 34 fallos de 35** | Alto para tu confianza en las pruebas | Medido hoy en `tests/browser/programacion-semanal-*`: 34 fallan y 1 pasa. **No es por nuestros cambios** — verificado intercambiando el archivo por el del commit anterior, donde falla idéntico y en la misma línea. Se cae antes de empezar, al abrir la semana (`openProgrammingWeek:103`) y al buscar un proyecto de fixture («Project card not found: Prueba»). Es decir: **esas pruebas llevan tiempo sin proteger nada** y nadie lo notaba. Arreglar los fixtures es trabajo aparte, pero conviene saberlo: hoy esa red no está puesta |
| **C-20** | **Hay tokens de color sin variante oscura** | Familia de bugs, no un caso suelto | El task 27 encontró que `--ds-color-surface` y `--ds-color-brand-architecture` son **estáticos**: no cambian con el tema. Los usa `.btn-dropdown-trigger` en 6 sitios. Intentó cambiarlos por tokens dark-aware y **midió que el contraste bajaba a 1,67:1** —peor que el original—, así que lo revirtió en vez de dejarlo. Es la misma raíz que el defecto del PDC: un color que no sabe en qué tema está. Auditar cuántos tokens más están así es trabajo de sistema, no de módulo. **Actualización del task 28 (`312ba9b`):** el síntoma quedó curado en el consumidor —el disparador y su menú pasaron a los tokens dark-aware que ya usa `.aia-btn--secondary`, y el contraste subió de inaccesible a 13,4:1 cerrado y 15,13:1 abierto— **cambiando el par completo, fondo y texto a la vez**, que es justo lo que faltó en el intento anterior. La raíz sigue intacta: `--ds-color-surface` y `--ds-color-brand-architecture` continúan sin variante oscura y esperan tu decisión |
| **C-19** | **El tooltip del nombre de columna aparece incluso donde no hace falta** | Menor | El task 26 pone el texto completo al pasar el ratón en TODAS las cabeceras, no solo en las cortadas: «Id» muestra un tooltip que dice «Id». Es ruido, no un fallo. Condicionarlo a que haya recorte real es media hora de trabajo; se hace si te molesta |
| **C-16** | **La caja interna de la cabecera desperdicia 23 px por columna** | Estructural, causa raíz — **ahora con beneficio medido** | El `.colHeader` renderiza 33 px donde el `th` mide 56. Medido tras el task 26: «Crítica» necesita 38 px y «Unidad» 41 — **ambas cabrían enteras** si la caja usara el ancho real de la celda, sin necesidad de elipsis ni de tooltip. Los tasks 24 y 26 trataron el síntoma dos veces; la cura está a un paso y devuelve 23 px por columna en todas las tablas. Toca el `colWidths` del JS, no solo CSS. **Segunda instancia medida (ciclo del 04, Programa General con datos):** en las columnas de fecha, la flecha del selector **tapa el último dígito** — 9 a 10 px de solape sobre «2026-02-02». La aritmética no deja salida por CSS: el contenido disponible son 84 px para 76 de texto más 16 de flecha, o sea 92. Probé darle carril propio a la flecha con relleno y **empeoró** (de 9 a 13 px): la flecha está anclada al borde del contenido, así que más `padding-right` la mete hacia dentro más rápido de lo que retrocede el texto. Lo revertí. Una fecha con el último dígito tapado en una app de programación no es un detalle estético; refuerza que esto merece el arreglo estructural |
| **C-15** | **`buttons.css` entero vive en el escalón más bajo de su capa, sin que nadie lo decidiera** | Estructural, preexistente | Hallazgo de la re-revisión del task 22: `buttons.css` (1 215 líneas) se auto-encapsula en `@layer components` **y además** se importa con `layer(components)`, creando `components.components`. Resultado: pierde silenciosamente ante cualquier otro componente que colisione en selector. Hoy juega a favor —es lo que permite que el componente compartido gane—, pero es un accidente, no un diseño. `access.css` tiene el mismo patrón con `utilities`. Merece ticket propio: auditar todos los `@import … layer(x)` sobre archivos ya auto-encapsulados en la misma capa |
| **C-14** | **El aviso de «qué retiene el estado» YA existe — y aun así no lo viste** | Ninguno de código; es de descubribilidad | El task 23 encontró tres mecanismos ya construidos y verificados en vivo: (1) marca `⚠ Sin asignar` **visible en la propia celda** que falta, (2) el chip de estado lleva `title`/`aria-label` con las razones al pasar el cursor, (3) al hacer clic, un panel lista cada condición pendiente. Si aun así no lo encontraste, hay dos posibilidades y solo tú puedes distinguirlas: **(a)** tu caso lo retenía otra condición (restricciones abiertas, no responsables) y esa no se marca en celda, solo al pasar el cursor; **(b)** el aviso está pero no invita a mirarlo. **Lo que te pido, cuando te vuelva a pasar:** mira si en la fila aparece `⚠ Sin asignar`, y pasa el cursor por encima del chip de estado. Con esa respuesta se decide en un minuto si hay que hacer el motivo visible sin interacción |
| **C-10** | **Los chips contadores del PDC no se pueden usar con teclado** | Pequeño, pero es accesibilidad **nivel A** | Filtran al hacer clic, pero no son alcanzables ni activables con teclado (les falta `role`/`tabindex`/`keydown`). Es un fallo más básico que el tamaño que acabamos de ajustar: WCAG 2.1.1 es nivel A, el mínimo del mínimo. **No se arregló** porque añadir manejo de teclado es funcionalidad nueva y tú pusiste ese límite. Mi recomendación: autorizarlo — hacer alcanzable un control que ya existe no cambia lo que hace |
| **C-11** | **Hay reglas pensadas para tablet aplicándose a tu pantalla de trabajo** | Estructural | Segunda vez que aparece hoy: el task 17 halló un `@media 768–1199.98px` que agrandaba el gatillo de filtro, y el task 22 otro que forzaba 44 px con `!important` — ambos alcanzan el viewport canónico de 1180 px. El repo prohíbe trabajar tablet, pero su CSS sigue ahí y muerde el escritorio. Merece task propio: auditar todos los media query que solapen 1180 px |
| **C-12** | **Programación Intermedia se queda sin acción primaria** | Menor | En las otras tres toolbars se pudo señalar cuál es la acción principal; en PI no había candidata defendible sin inventar criterio de dominio. Si sabes cuál debería destacar («Restricción Compartida»?, «Listas»?), se aplica en un minuto |
| **C-13** | **Los chips de PI y PS envuelven a dos líneas** | Menor | Un ancho fijo de 155 px preexistente los obliga a partir en dos renglones, lo que come el espacio que acabamos de ganar |
| **C-22** | **El bloque blanco de `/indicadores` no es nuestro: es Power BI** | Corrige el top-1 del barrido anterior | Medido hoy: el contenido central es un `<iframe>` servido desde `app.powerbi.com`. Es **otro origen**, así que su interior no se puede tematizar desde nuestras hojas por ningún medio legítimo — el blanco es el tema del propio informe, configurado en el servicio de Power BI, no en este repo. El barrido anterior lo listó como la peor infracción visual y daba a entender que era CSS nuestro; no lo es, y por eso nunca se arregló. **Lo que decides tú:** (a) cambiar el tema del informe a oscuro en Power BI —fuera de este repo, en tu cuenta—, (b) dejarlo como está y aceptar la isla blanca, o (c) enmarcarlo para que el salto sea menos abrupto. Lo que **no** voy a hacer es un `filter: invert()` sobre el iframe: destroza los colores del informe y convierte un problema estético en uno de lectura de datos |
| **C-23** | **Tres de las ocho pestañas de BI quedan fuera de vista** | Navegación, severidad 3 | Medido a 1180×820: las pestañas suman 1626 px y el carril tiene 1116. Quedan invisibles «Plan de Compras», «Proveedores (CIC)» y «Responsables (CIP)». **No caben**: incluso quitando los iconos y bajando a 13 px suman 1363 px, y acortar las etiquetas es tocar texto de navegación. Hoy el carril sí desplaza y —tras el arreglo de esta pasada— la barra de scroll es visible y discreta, así que el destino es alcanzable. Intenté además un desvanecido en el borde derecho para anunciar que hay más, y **lo descarté**: el presupuesto de esa ruta es de cero funciones de color y un degradado no cabe; moverlo a otro archivo habría sido esquivar el gate. **Lo que decides tú:** autorizar la excepción de presupuesto para ese degradado, o aceptar que la barra de scroll sea la única señal |
| **C-24** | **Los chips en cero gritan igual que los chips con alarma** | Transversal a PI y PS | Hallazgo del ciclo de tres lentes del 04. En PS hay 5 chips y en PI 8, **todos en `(0)`**, cada uno con su relleno saturado, ocupando la banda superior justo encima de una tabla que ya dice «Sin actividades programadas esta semana». Es lo más ruidoso de la pantalla y lo que menos informa: contradice de frente la premisa de deferencia al contenido y la de un solo acento por vista. **La mejora obvia** es que un contador en cero se atenúe y recupere color solo cuando tiene algo que contar. **Por qué no la apliqué:** el sandbox no tiene datos, así que solo podría verificar el estado en cero — la mitad del cambio. Verificarlo entero exige mirar un proyecto con datos (Da Porto, solo lectura) o poblar el sandbox, que es mutar datos. **Lo que decides tú:** autorizas que lo verifique contra un proyecto con datos y lo aplique, o se queda así. **Actualización del ciclo del 04, y cambia el caso:** medí los chips de **Programa General** y ya hacen justo esto — sus 7 chips comparten **un único fondo neutro oscuro** (`--ds-active-surface-raised`) y llevan el color de estado en un **punto marcador**, dimensionados por contenido y a una línea. O sea que el patrón no hay que inventarlo ni verificarlo a ciegas: **ya está en producción, en el módulo piloto del design system**, y los raros son PI y PS. Eso convierte esto de «idea sin verificar» en «dar paridad con lo que PG ya publica», que es mucho menos arriesgado. **Sigue siendo tuya la decisión** por una razón concreta: pasar de relleno saturado a punto marcador cambia **cuánto grita una alarma**, y «RC inicio vencido» en rojo pleno comunica urgencia. Si quieres esa urgencia intacta en PI y PS, se queda como está. **CORRECCIÓN IMPORTANTE, del ciclo con datos reales:** todo lo anterior se midió sobre un sandbox vacío, donde los siete chips marcaban `(0)`. Medido ahora en un proyecto con datos: los chips dicen «Con Alerta Restricciones 202», «Actividad Futura 1332», «Atrasada 31», «En Curso 8». **Con datos, el color saturado está haciendo su trabajo**: un chip rojo con 31 actividades atrasadas *merece* llamar la atención. El problema que describí —«lo más ruidoso de la pantalla es lo que menos informa»— **solo existe cuando todo está en cero**, que es un estado de proyecto nuevo, no el habitual. Rebajo mi propia recomendación: esto ya no es «dar paridad con PG», sino como mucho «atenuar el chip cuando su cuenta sea cero». Menos ambicioso y bastante menos arriesgado |
| **C-25** | **La marca «AIA» de `admin/` se queda a 0,04 del mínimo de contraste** | Mínimo, en las 8 rutas de admin | Medido hoy: 4,46:1 donde el mínimo AA para texto normal es 4,5:1. Son 16 px en negrita, que **no** cuenta como «texto grande» (haría falta 18,66 px en negrita), así que técnicamente incumple por un pelo. **No lo toqué** por dos razones: la diferencia está dentro del ruido de mi propia sonda, y el color sale de la paleta de `admin/`, que `AGENTS.md` deja fuera del design system («AdminLTE no se migra»). Cambiarlo por decisión propia sería meter el sistema donde el repo dijo que no entra. **Lo que decides tú:** si quieres que la marca cumpla, se sube un escalón de luminancia y listo |
| **C-26** | **Hay un modal de Programación Intermedia que nadie puede abrir** | Funcional, no estético | Medido hoy: `/programacion-intermedia` tiene **5 ids duplicados**, y uno duele. Dos modales **distintos** comparten `modal_leyenda_colores`: «Guía Operativa - Programación Intermedia» y «Leyenda de Colores de Las Actividades». El botón «Leyenda» apunta a ese id, y tanto `getElementById` como Bootstrap resuelven al **primero** del documento. Verificado en vivo: abre la Guía Operativa y **la Leyenda de Colores es inalcanzable por cualquier vía**. (El nombre accesible sí resuelve bien, porque el modal que abre y su título son ambos los primeros; el daño es la pieza inalcanzable, no una etiqueta equivocada.) **No lo toqué porque no es diseño:** renombrar el id haría aparecer contenido que hoy nadie ve, y eso cambia lo que la aplicación muestra. **Lo que decides tú:** (a) la Guía Operativa sustituye a la vieja Leyenda y esta es markup muerto que se borra, o (b) las dos deben convivir y hace falta un segundo disparador. Solo tú sabes cuál. Los otros 4 ids duplicados (`permiso_canonico`, `Semanal_Confirmada`, `modal_leyenda_colores_Label`, `modalEliminarLabel`) conviene revisarlos en el mismo lote |
| **C-27** | **La guía operativa escribe sin tildes lo que los chips escriben con tilde** | Menor, de consistencia | El modal dice «Ejecucion Pendiente» y el chip de esa misma pantalla dice «En Ejecución Pendiente». Sistemático: «Guia», «Ejecucion», «habilitacion», «critica», «preparacion», «Gestion», «Programacion», «tecnico». **No lo corregí** porque son nombres de estado del dominio y el naming lo gobierna `GLOSARIO.md`: reescribir copia de dominio por mi cuenta es justo lo que me pediste no hacer. Es media hora con tu visto bueno |
| **C-28** | **El formulario de crear usuario no dice qué campos son obligatorios** | Menor, de usabilidad | Medido en `/admin/usuarios/crear`: **5 campos obligatorios** (Nombre, Usuario, Contraseña, Proyecto, Rol) y **2 opcionales** (Email, Cargo), con **cero marcas visuales** en toda la página — ni asterisco, ni «obligatorio», ni «opcional». Solo te enteras al intentar guardar. **No lo apliqué** porque añade texto visible a un formulario que crea usuarios y reparte permisos, y la convención es tuya. **Mi recomendación:** como 5 de 7 son obligatorios, marcar **los dos opcionales** («Email (opcional)», «Cargo (opcional)») en vez de asteriscar cinco — es la guía de Nielsen y deja el formulario más limpio. Dilo y son diez minutos |
| **C-29** | **Los mensajes de error se ven casi blancos: el rojo no llega** | Menor, pero es la señal de error | En `/admin/usuarios/crear` los mensajes de campo son `.text-danger`, que el adaptador aclara a un 35% del rojo crítico sobre tinta primaria. Resultado medido: `rgb(250,234,231)` — prácticamente blanco, indistinguible del texto normal. El error no se ve de un vistazo. **La regla no está mal, su premisa caducó:** su propio comentario dice que «el único sitio donde se usa como texto suelto es el enlace *Salir*», y eso dejó de ser cierto cuando estos mensajes aparecieron. Para «Salir» el aclarado es correcto; para un error, el matiz carga significado. **Lo intenté y el gate me paró, con razón:** usar `--ds-color-state-critical-text` suelto rompe el contrato de «un matiz por estado», que exige el token de texto con su fondo. Medí 11,21:1 sobre la superficie del formulario, así que el contraste sobraba, pero la objeción del gate es de coherencia, no de contraste. La salida es una entrada en `state-token-exceptions.json`, y **ese inventario es tuyo** (C-2). Lo revertí y aquí queda. **Lo que decides:** autorizas la excepción, o el error se queda en blanco |
| **C-30** | **Casi ninguna pantalla declara `<main>` ni empieza en `h1`** | Estructural, transversal | Medido en el barrido del 04, primera vez que se mira la estructura del documento: de las rutas censadas, **solo 3 declaran `<main>`** (`/programa-general`, `/proyectos`, `/bi/control-tower`) y la mayoría **no tiene `h1`** — arrancan en `h2` o `h3`. Además `/programa-general` salta de `h1` a `h3`. Consecuencia real: quien navega con lector de pantalla no puede saltar al contenido ni recorrer la página por encabezados, que son las dos formas rápidas de moverse. **No lo apliqué** porque envolver el contenido en `<main>` y decidir cuál es el `h1` de cada pantalla toca la estructura de once vistas y puede mover estilos que cuelgan de esos selectores — es un task propio con su verificación, no un retoque de barrido. Los títulos de pestaña, que eran la mitad barata del problema, sí quedaron resueltos en `e6f7f4c`. **Matiz del ciclo 35, y conviene decirlo porque mejora el diagnóstico:** `/dashboard/escalamientos` —que el censo original no cubrió— tiene la **mejor estructura medida en toda la sesión**: un solo `h1`, cuatro `h2` para las columnas y `h3`/`h4` dentro del cajón, **sin saltos de nivel y sin ids duplicados**. Es decir, el problema no es que el proyecto no sepa estructurar un documento: hay al menos una vista que lo hace bien y puede servir de patrón para las demás. Lo que sí le falta, como al resto, es `<main>` |
| **C-31** | **Dos actividades distintas pueden mostrar el mismo código: la columna «Id» corta en seco** | **Alto — no es estético, es leer mal un dato** | Encontrado al auditar por fin con **datos reales** (proyecto «Optimización Aeropuerto JMC», 26 filas, solo lectura). La columna **«Id»** muestra códigos jerárquicos tipo `3.5.2.1.1`, tiene 54 px y el código necesita 66. Corta con **`text-overflow: clip`** —sin elipsis— y **sin `title`**, así que el valor completo no está disponible en ninguna parte. Consecuencia concreta y medida: `3.5.2.1.1` se ve igual que `3.5.2.1`, **que es otra actividad presente en la misma tabla**. No hay ningún aviso de que falte texto. **Por qué no lo arreglé:** probé `white-space: nowrap` + elipsis y funciona sin alterar el alto de fila, pero la columna «Id» no tiene selector propio en Handsontable y aplicarlo a todas las celdas rompería el ajuste de «Actividad», que sí debe envolver. La cura buena es el ancho de columna (`colWidths` en JS), que es C-16 y espera tu decisión. **Mi recomendación:** subir esto por encima de C-16 en prioridad — el resto de C-16 es espacio desperdiciado; esto es un identificador que se lee mal. **AMPLIACIÓN del barrido con datos (mismo día): no es de Programa General, es de toda la app.** Censo sobre el proyecto con datos: **51 truncamientos silenciosos** y **cero celdas con elipsis en toda la aplicación**. Repartidos así: **Programación Intermedia 29 —26 de sus 27 códigos «Id», más nombres de subcontratista partidos**—, Programación Semanal 9, Programa General 6, y **Subcontratistas 7, que son direcciones de correo** cortadas a media palabra («proyectos@concreacero.»). Un correo cortado sin aviso es un correo que se copia mal. **`/profesionales`, con 24 filas, sale limpia**, lo que demuestra que no es inevitable sino una laguna del contrato de tabla: nunca se especificó qué pasa cuando el contenido no cabe. **Segundo intento de mitigación, también descartado:** las celdas que deben envolver se marcan con `force-wrap`, así que podría poner elipsis a las demás — pero eso ocultaría texto que hoy sí se ve entero en celdas que envuelven por defecto, cambiando un problema por otro. La salida sigue siendo el ancho de columna. **TERCERA AMPLIACIÓN — y PDC nunca se había medido de verdad.** Los barridos anteriores devolvían «0 celdas» en `/pdc` y yo lo leí como «limpio»; en realidad significaba que mi sonda no llegaba, porque en los proyectos que miraba el módulo estaba vacío. Medido por fin sobre Da Porto **en solo lectura** (34 filas, 330 celdas): **54 truncamientos mudos más**. Los peores están en **«ESTADO DEL PROCESO»**, que corta «Información pendiente 71» —el estado de una compra— y en **«FAMILIAS ASOCIADAS»**, que pierde 36 px. **Total de la aplicación: 105 truncamientos silenciosos**, cero elipsis en ninguna parte. **El peor caso medido de toda la sesión está aquí:** el texto completo de esa columna es «Información pendiente **71 días de retraso**» —necesita 138 px y tiene 124—, así que **lo que se corta es justo la cifra de retraso**, el dato más accionable de la celda, en **10 de las 34 filas**. Una compra con 71 días de retraso muestra su retraso truncado y sin aviso. Los chips de arriba no compensan: dan el conteo por estado (64 en «Información pendiente»), no cuánto lleva atrasada cada fila. **INTENTO REAL DE ARREGLO, con receta para quien lo retome.** Primero corregí un razonamiento mío: llevaba tres ciclos diciendo que la cura «vive en el JS y por tanto es tuya», pero **el ancho de columna no es funcionalidad** —no toca comportamiento, datos, reglas, rutas, permisos ni texto de dominio—, así que estaba confundiendo dónde vive el código con qué tipo de cambio es. Con eso claro, implementé la solución **mejor que ensanchar**: devolver el texto íntegro al pasar el cursor solo si la celda se recorta, que es exactamente lo que el task 26 ya hace con las cabeceras en `public/js/modules/pdc/hot.js`, y que **no toca ningún ancho, así que ninguna columna encoge**. Medido: recuperó 29 de los 54 recortes, incluido el caso del retraso. **Pero lo revertí**, porque generó **62 tooltips redundantes** —una fecha «2026-03-31» con un tooltip que dice «2026-03-31»—, que es el ruido que yo mismo registré como defecto en C-19. La causa está diagnosticada: Handsontable renderiza varias veces y medir dentro del renderer ocurre **antes de que se aplique el ancho definitivo**, así que se ven desbordes que luego no existen. **La receta correcta:** hacer ese mismo barrido en un hook `afterRender`, cuando la medida ya es la definitiva. No existe ese hook hoy en `hot.js`; añadirlo es un task pequeño con su verificación, no un retoque de barrido |
| **C-32** | **Los gráficos de BI se nombran pero no se leen** | Accesibilidad, nivel A | Medido con la técnica de muestreo de canvas estrenada anoche. Los gráficos **sí traen `aria-label`** —«Gráfico de barras PPC por semana», «Gráfico de curva S, programado vs ejecutado»—, lo cual ya es más de lo habitual. Pero eso **nombra el gráfico, no comunica sus datos**: no hay `aria-describedby`, ni tabla alternativa, ni resumen textual de la serie. Quien usa lector de pantalla sabe que existe un gráfico de PPC por semana y nada más. Es WCAG 1.1.1: el alternativo de un contenido complejo debe transmitir la información equivalente. **Matiz justo:** no se quedan a cero — las tarjetas de KPI de la misma vista sí exponen 5 cifras en texto, así que hay contexto; lo que falta es la serie. **No lo hice** porque generar el resumen textual es producir contenido nuevo a partir de los datos, y una cifra mal derivada en un alternativo sería peor que no tenerlo. **Mi recomendación:** una tabla equivalente oculta visualmente (`.sr-only`) junto a cada gráfico, generada de la misma fuente que alimenta la serie — así no puede desincronizarse |
| **C-33** | **El estado vacío de Control de Cambios es el único que deja al usuario sin salida** | Menor, pero rompe un estándar propio | Censo de los estados vacíos de la app, hecho sobre el sandbox (que para esto es el fixture ideal). **El resto está notablemente bien**: Programación Semanal dice «Sin actividades programadas esta semana» y añade «Usa «Agregar Actividad»… o «Autoprogramar Actividades» para traerlas desde la programación intermedia»; PDC dice de dónde se arman los paquetes; el panel lateral explica que hay que hacer clic en una celda. Todos explican **por qué** está vacío y **qué hacer**. **`/control-cambios` no**: dice «No hay solicitudes de cambio registradas para este proyecto.» y ahí termina, sin explicar de dónde salen las solicitudes y **sin ninguna acción disponible en toda la vista** (medido: cero botones de crear/solicitar). **No lo escribí yo** porque la frase que falta explica una regla de dominio —de dónde nacen las solicitudes de cambio— y eso está fuera de mi mandato. Es una frase: dime de dónde vienen y la pongo, o escríbela tú |
| **C-34** | **Al pasar el ratón, un botón secundario adelanta a la acción principal** | Jerarquía, transversal a toda la app | Primer ciclo que mide el **hover**, estado que ninguna pasada había tocado —y que es justo de lo que habla la premisa: «controles discretos que ganan presencia solo al necesitarse»—. Los 31 elementos interactivos de PS tienen regla de hover, así que el problema no es que falte: es cuánto. Medido con ratón real y conversión de color por canvas: el primario «Confirmar Compromisos» en reposo es `rgb(108,144,119)`, luminancia **0,245**; el secundario «Agregar Actividad» **en hover** pasa a relleno verde `rgb(149,187,156)`, luminancia **0,443** — un **80 % más luminoso que la acción principal**. Es la comparación realista: pasas el ratón por uno mientras el otro está en reposo. **No es accesibilidad** —ambos cumplen de sobra, 4,87:1 y 8,16:1—, es que el control discreto no gana presencia sino que **adelanta al acento**, y la vista pasa a tener dos primarios con el equivocado ganando. **No lo cambié** porque `.aia-btn--secondary:hover` es una primitiva compartida: tocarla altera el lenguaje de interacción de toda la aplicación y movería los goldens que esperan tu firma. **Mi recomendación:** que el hover del secundario suba a la superficie elevada con el borde más vivo, en vez de rellenarse del verde del acento — gana presencia sin robar la jerarquía |
| **C-35** | **El motivo por el que un botón está bloqueado solo llega con el ratón** | Menor, pero deja fuera a quien no usa ratón | Primer ciclo que mide el **estado deshabilitado**. **Lo primero, en positivo: está bien resuelto.** Se distingue del activo por cuatro canales independientes —fondo neutro frente a rojizo, color de texto, **icono de candado frente a papelera**, y cursor— y sigue siendo plenamente legible, en vez del típico atenuado que rompe el contraste. Y cada uno **explica su motivo**: «No se puede eliminar: el profesional está administrado desde Admin». Eso es mejor que la práctica habitual. **El pero:** medidos 13 en `/profesionales`, todos con `disabled` nativo, así que **no son focalizables** —fuera del recorrido de tabulación— y el motivo vive **solo en el `title`**, que aparece al pasar el ratón. No está en ningún texto visible ni en `aria-describedby`. Con teclado o lector de pantalla el botón no existe y su explicación es inalcanzable. **No lo cambié** porque el remedio estándar —cambiar `disabled` por `aria-disabled` y mantenerlo focalizable— hace que el botón vuelva a ser pulsable y exige que el JS lo bloquee: eso es funcionalidad, no diseño. **Lo que decides tú:** autorizar ese cambio, o dejar el motivo también como texto visible en la fila |
| **C-36** | **34 de los 53 campos del modal de contrato del PDC tienen etiqueta visible pero no asociada** | **Alto** — es el formulario más largo de la aplicación | Encontrado al auditar por fin **el contenido de los modales cerrados**, que ninguna pasada anterior alcanzaba: todas mis sondas contaban solo controles visibles en reposo. Medido en `/pdc`: el modal de contrato tiene **53 campos**, **34 sin etiqueta programática**, y **33 de esos sí muestran su etiqueta en pantalla** —dentro de un `<span class="h6">`, como «8. Inicio en obra:», en vez de un `<label for>`—. Para quien lo rellena con lector de pantalla, dos tercios de los campos se anuncian sin nombre, en el formulario más largo que tiene la aplicación. **No lo apliqué** porque son 34 emparejamientos y una etiqueta mal asignada es peor que ninguna: exige revisar campo por campo, con su verificación. Es un task propio. **La buena noticia:** el texto ya existe en pantalla, así que no hay que inventar nomenclatura de dominio — basta con dar `id` a cada `<span>` y apuntar el campo con `aria-labelledby` |
| **C-37** | **La mitad de los gatillos decorativos de Programa General no están marcados como decorativos** | Menor, de consistencia | Medido: PG tiene **24** botones `.changeType` —el indicador de Handsontable dentro de la celda— y **solo 12 llevan `aria-hidden="true"`**. Los 24 tienen `tabindex="-1"`, así que ninguno es alcanzable por teclado y el daño está acotado, pero un lector de pantalla puede anunciar los 12 sin marcar como botones sin nombre mientras ignora los otros 12 idénticos. En Programación Semanal los equivalentes sí están marcados de forma uniforme, así que es una inconsistencia interna, no una decisión |
| **C-9** | **La densidad no gana filas por el ancho de la columna «Actividad»** | Medio | Hallazgo del task 19: bajar el alto de fila no sirve de nada mientras el texto largo se envuelva en varias líneas. Ganar filas de verdad exige decidir qué hacer con esa columna (truncar con detalle al pasar el cursor, ancho fijo, o dos líneas máximo). Es una decisión de producto: afecta a cuánto texto lees de un vistazo. **Ahora con la cifra dura, medida sobre 26 filas reales:** el token de fila vale 24 px y la **fila mediana mide 111 px**, con un máximo de 216. Es decir, **la fila real es 4,6 veces el token**. Lo que gobierna la altura no es la densidad que ajustamos, es el texto envuelto de «Actividad». Esto confirma con datos lo que ya te dije en A-2 —que la densidad compacta no gana filas— y convierte a esta entrada en la que de verdad decide cuántas actividades ves de un vistazo |
| **C-38** | **El adaptador de `admin/` cubre tres variantes de botón y deja fuera las otras tres — y no puede alcanzarlas** | Estructural, transversal a las 8 rutas de admin | Medido en `/admin/matching/family-catalog`: los botones de exportar rinden **texto verde con borde azul** y **texto verde con borde ámbar**, porque el color de texto viene ya tokenizado de otra regla y el borde se queda en el valor original de Bootstrap (`rgb(40,167,69)`, `rgb(23,162,184)`, `rgb(255,193,7)`). La causa está localizada: `admin-lte.css` tokeniza los rellenos `.btn-success/.btn-info/.btn-warning` y **solo tres** contornos —`primary`, `secondary`, `danger`—, y deja `.btn-outline-success/info/warning` sin cubrir. En esa misma vista quedan además ~130 botones «Aprobar» y los «Pasar a Contratos» pintados con color crudo de vendor. **Escribí el arreglo y lo revertí, porque no funciona y conviene que conste por qué:** el adaptador usa `:where()`, que vale **especificidad cero** a propósito, y AdminLTE trae `.dark-mode .btn-outline-success` con especificidad (0,2,0). El vendor gana siempre. Comprobado además que **no era la caché** —inyecté el archivo recién servido y tampoco aplicaba—, así que subir la versión del `@import` no arregla nada. Es decir: el patrón del adaptador **no puede** alcanzar estas reglas sin cambiar de estrategia. **Lo que decides tú:** (a) autorizar que estas variantes suban de especificidad —por ejemplo anclándolas a `.dark-mode` como hace el vendor—, con el radio de impacto de las 8 rutas de admin y sin `!important`, o (b) aceptar que los botones de contorno de `admin/` se queden con la paleta de Bootstrap, que es coherente con «AdminLTE no se migra» de `AGENTS.md`. **Mi recomendación es (a) acotada a estas tres variantes**: no introduce primitivas `aia-*` ni migra vistas, solo termina un trabajo que el propio adaptador ya empezó a medias. Contrastes actuales medidos, todos por encima de AA (5,24:1 a 10,07:1), así que **no es accesibilidad: es coherencia** |
| **C-39** | **`Esc` no cierra los modales, aunque su configuración dice que debería** | Menor, pero rompe una expectativa universal | Primer ciclo que prueba el teclado dentro de un modal. Medido en `/programacion-semanal` con el modal de guía operativa abierto por **clic real**: pulsar `Esc` **no lo cierra**. Y no es que la tecla se pierda — puse una escucha en captura y la registré llegando al propio modal, sin `preventDefault` previo; la instancia de Bootstrap reporta `keyboard: true`, que es justo la opción que debería cerrarlo. O sea que la configuración dice una cosa y el comportamiento hace otra. El modal lleva `data-backdrop="static"`, así que **tampoco cierra pulsando fuera**: la única salida es la «×». **No es una trampa de teclado** en el sentido de WCAG 2.1.2 —la «×» es alcanzable con Tab, así que hay salida—, pero sí incumple la convención que todo usuario da por hecha, y es la que uno intenta primero. **No lo arreglé** porque hacer que `Esc` cierre un diálogo es cambiar comportamiento, y ese es el límite que pusiste. **Lo que decides tú:** autorizar que `Esc` cierre estos modales. **Salvedad honesta sobre el alcance:** lo verifiqué **en un solo modal**. Intenté medir los otros cuatro en cadena y el script colgó el panel del navegador, así que **no sé si es de este modal o de todos** — y como los 12 comparten `data-backdrop="static"`, lo probable es que sea general, pero probable no es medido. Quien lo retome, que empiece por confirmar el alcance de a un modal por vez |
| **C-40** | **Los 225 avisos del hook de diseño, por fin separados: dos tercios son deuda real, no ruido** | Informativa — cierra un bucle que dejé abierto dos ciclos | Las dos veces anteriores aplacé estos avisos dando a entender que serían mayormente artefacto, como el `#fff` que resultó estar dentro de un comentario. **Medido, me equivocaba.** De **255** hallazgos sobre las cuatro hojas del plan: **5 están en comentarios** (falsos positivos), **88 son *fallbacks* de `var(--ds-x, valor)`** —patrón legítimo y documentado—, y **162 son declaraciones reales**: 88 tipografías, 60 colores y 14 radios escritos a mano. Reparto: `buttons.css` 54, `programacion-intermedia.css` 54, `programacion-semanal.css` 32, `pdc.css` 22. **Pero el marco cambia la conclusión:** esa deuda **ya está registrada** en `docs/design-system/audit-baseline.json`, que lleva **7.161 violaciones** contadas por categoría (`off-scale-typography: 402`, `hardcoded-color-function: 547`, `hardcoded-radius: 102`…) con la política escrita «reducir por módulo migrado; no aumentarlo sin justificación». Es decir: el hook no descubre nada nuevo, re-expone una porción de un libro de deuda que el repo ya lleva —y por eso la suite estática pasa: los totales no suben—. **Consecuencia visible, que es lo que de verdad importa:** medí los tamaños de letra *renderizados* en `/programacion-intermedia` y hay **16 distintos en un solo viewport**, de los cuales **diez forman dos racimos indistinguibles** (14,4 / 14,08 / 14 / 13,6 px, y 12,8 / 12,48 / 12,16 / 12 px). Diferencias por debajo de un píxel no comunican jerarquía: cuestan complejidad y no compran nada. En radios la escala sí está sana (9999/4/8/12/16) salvo tres intrusos: `7px`, `6px` y un `999px` que es un `9999px` mal tecleado. **No apliqué nada** porque colapsar la rampa tipográfica cambia el render de cuatro módulos y con ello los goldens, que esperan tu firma (A-1). **Lo que decides tú:** si esto merece un task propio de «reducción de deuda por módulo» —que es lo que la propia baseline pide— o se queda contabilizado y quieto. **Mi recomendación:** un task acotado a la rampa tipográfica de los dos racimos, que es donde se gana jerarquía real con el menor número de cambios. **REFORZADA por la medición del ciclo 30, que mide la otra mitad del sistema:** el **espaciado sí está sano**. Medido en `/programacion-semanal`, descontando 49 usos de `1px` que son los finos de celda de tabla, queda **un único valor fuera de escala** —`10px`, con 28 usos— más tres casos sueltos (`5px`, `100px`); los `gap` de flex y grid van al 95 % en escala. Es decir: el proyecto **sí sabe mantener una escala** cuando la tiene clara, y la tipográfica es la excepción, no la norma. Eso acota el trabajo y sube mi confianza en la recomendación: no hace falta una campaña de espaciado, solo la de tipografía |
| **C-41** | **Control de Cambios repite doce ids, y diez son campos de filtro** | Medio — rompe la asociación etiqueta-campo | Medido en el barrido del 04 sobre `/control-cambios`: **12 ids duplicados** en la página. Diez son buscadores de columna —`buscadorSolicitanteCambio`, `buscadorFechaSolicitud`, `buscadorPrioridad`, `buscadorTipoCambio`, `buscadorResponsableDefinicion`, `buscadorValorAprobado`, `buscadorFechaTentativaDefinicion`, `buscadorFechaEntregaInterventoria`, `buscadorFechaDefinicion`, `buscadorAprobacion`— más `Id` y `opcion`. Un id repetido hace que `getElementById` y cualquier `label[for]` resuelvan **siempre al primero**, así que la mitad de esos filtros no puede recibir su etiqueta ni ser alcanzada por script de forma fiable. Es el mismo mecanismo que dejó inalcanzable un modal entero en C-26, aquí multiplicado por diez. **No lo toqué** porque renombrar ids que el JS de filtrado usa para leer y escribir valores es tocar el cableado del módulo, no su apariencia: si se me escapa una referencia, se rompen los filtros de Control de Cambios. Exige revisar cada uso en el JS, y eso es un task propio con su verificación. **Nota de honestidad sobre el alcance:** `Id` y `opcion` aparecen duplicados también en `/pdc` y en `/programacion-semanal`, así que el patrón no es exclusivo de esta vista; los diez `buscador*` sí lo son |
| **C-42** | **El recorrido del teclado por la barra lateral va acolchado con selectores de semana** | Menor (Nielsen 7, severidad 2) — eficiencia, no accesibilidad | Primer ciclo que mide el **orden del tabulador** con pulsaciones reales. **Lo primero, y es buena noticia: está bien resuelto donde importa.** De 20 paradas medidas en `/programacion-semanal`, **las 20 tienen anillo de foco `2px solid`, ninguna queda fuera del viewport y ninguna está tapada** por las barras fijas — que es justo lo que suele romperse y aquí no. **El pero es el coste del recorrido:** cada enlace de módulo (Programa General, Intermedia, Semanal, Actualizar Cronograma, Plan de Compras) arrastra su propio selector de semanas, y tabular lo revela y lo atraviesa. Resultado medido: **6 de las 20 paradas son entradas de semana intercaladas** entre los enlaces de navegación. Para ir de «Programa General» a «Programación Intermedia» con teclado pasas por una entrada de semana que no pediste. **Matiz honesto que rebaja mi propio hallazgo:** comprobé que el menú **se hace visible al recibir el foco**, así que no hay foco perdido en lo invisible — es un patrón legítimo, solo ineficiente. La convención ARIA para menús sería que el menú entero fuese **una sola parada** y se recorriera con flechas. **No lo cambié** porque pasar de «tabulable» a «una parada + flechas» es cambiar el comportamiento del teclado, y ese es tu límite. **Lo que decides tú:** autorizar ese cambio de patrón, o aceptar el acolchado |
| **C-43** | **Filtrar en el PDC cambia la tabla entera sin decirlo, y el chip no dice si está activo** | Medio (Nielsen 1, severidad 3) — con datos reales | Primer ciclo que mide **los anuncios de estado** (WCAG 4.1.3). Medido en `/pdc` sobre **Da Porto, en solo lectura**: pulsar el chip «Información pendiente 64» filtra la tabla de **33 filas a 10** y **no se anuncia absolutamente nada**. Lo llamativo es que la infraestructura **ya está puesta**: existe `#pdc-table-state` con `role="status"`, pensado justo para esto — y está **vacío y oculto**. Alguien lo previó y nadie lo conectó. **Segundo defecto, en el mismo gesto:** los siete chips no tienen `role`, ni `tabindex`, ni `aria-pressed`; el estado activo vive **solo en una clase CSS** (`inactive-filter` en los seis apagados). Con lector de pantalla son `<span>` mudos: ni se alcanzan, ni se sabe cuál está aplicado. **El dato que lo vuelve incoherencia y no laguna:** los chips de Programación Semanal usan **la misma clase `pdc-legend-item`** y sí llevan `role="button"` y `tabindex="0"`. Es decir, el patrón ya se resolvió una vez y PDC —de donde viene el nombre de la clase— se quedó atrás. **No lo apliqué, y esta vez la razón es concreta y no de prudencia:** poner `role`/`tabindex` sin manejador de teclado crea un control **enfocable pero no accionable**, que es peor que el estado actual; y rellenar la región de estado exige JS. Ambas cosas son comportamiento. Es la extensión natural de C-10. **Mi recomendación:** un task único que haga las tres cosas juntas —teclado, `aria-pressed` y conectar `#pdc-table-state`—, porque partirlo produce estados intermedios peores que el actual. **Corrección de mi propia lectura, por si alguien repite la medición:** mi primera sonda registró un anuncio de «No hay paquetes de contratación» y estuve a punto de reportar que la app anunciaba algo falso; al comprobarlo, esa región está `display:none` y no anuncia nada. El defecto es el silencio, no el error |
| **C-44** | **Las filas de «Capítulo» del PDC son el bloque más ruidoso de la aplicación** | Menor de accesibilidad, notable de jerarquía | Medido con datos reales en `/pdc`: las filas de capítulo se pintan con `rgb(139, 64, 17)` —un naranja quemado en crudo, no un token— con **luminancia 0,092 frente a 0,014 de una fila normal**, es decir **6,6 veces más brillante**, y ocupando **40 celdas** de las 100 visibles. Contraste de texto correcto, así que no es un fallo de accesibilidad: es que una fila de encabezado se anuncia con un relleno saturado de ancho completo, cuando la premisa vinculante pide jerarquía **por espaciado y tipografía, no por cromo**, y un solo acento por vista. Junto con la rejilla casi blanca de A-3, es el segundo caso medido de cromo gritando más que el contenido. **No lo cambié** porque tocar el relleno de una fila altera el render y con ello los goldens (A-1), y además ese color puede estar comprometido con la semántica de estado del módulo. **Mi recomendación, si lo autorizas:** que el capítulo se distinga por peso tipográfico y un filete superior, en vez de por un bloque de color — se gana jerarquía y se recupera la deferencia al contenido |
| **C-45** | **El botón flotante puede tapar celdas o botones de fila, y no he podido comprobarlo** | Menor, pero es una duda abierta sobre un cambio ya publicado | Al convertir el carril de concurrencia en botón flotante (`2c39fe0`), el botón pasa a **flotar sobre el área de la tabla** — es lo propio de un FAB, pero en el carril no ocurría. Medí las cuatro esquinas del botón en `/programacion-semanal` y `/programacion-intermedia` y en ambas cae sobre `wtHolder` **vacío**, sin celda ni control debajo. **El problema es que eso no prueba nada:** los proyectos disponibles tienen **1 fila** en esas vistas, así que la esquina inferior derecha está vacía por falta de datos, no por diseño. Con una tabla llena hasta abajo, ese círculo de 50 px se posa justo sobre la última fila visible — y en Programación Semanal la columna «Acciones», con sus botones de copiar y eliminar, está en el extremo derecho. **Lo que hay que comprobar cuando haya datos suficientes:** si el FAB queda encima de esos botones. Si es así, la salida habitual es reservarle un hueco al final de la tabla (`padding-bottom` en el contenedor de scroll), que es barato y no cambia comportamiento. **Lo registro en vez de adivinar** porque una tabla llena en esas vistas exige datos que no tengo en solo lectura, y prefiero decir «no medido» a decir «no pasa». **MEDIDO Y CONFIRMADO (ciclo 34): sí tapa datos.** La vista que faltaba era `/programa-general`, que sí monta el cajón y en Da Porto trae **29 filas visibles**. Con la tabla llena, el círculo se posa sobre la última fila y **oculta parcialmente el valor de la última columna** («Lib. Restr.»), confirmado en captura: el `0,0` aparece cortado por el botón. Medido con una rejilla de nueve puntos sobre el área del FAB, el resultado es `tapaCelda: true`. **No se arregla desplazando:** la tabla virtualiza y tiene su propio scroll, así que siempre hay una fila al fondo — el tapado es permanente, solo cambia a qué fila afecta. **No tapa ningún control en esta vista**, pero Programación Semanal tiene la columna «Acciones» con copiar y eliminar en el extremo derecho, y ahí no pude medirlo por falta de filas. **Por qué sigue sin aplicarse y qué decides:** las tres salidas tienen contrapartida y ninguna es obvia — (a) reservar hueco al final del área de scroll, que es lo estándar pero toca la geometría interna de Handsontable, que calcula su propio alto y es donde más fácil se rompe algo; (b) subir el botón cuando la vista tiene tabla, que resuelve el solape pero rompe la posición convencional que tú pediste; (c) aceptarlo, que es lo que hace la mayoría de apps con FAB. **Mi recomendación es (a)**, con verificación propia de que la tabla no pierde alto ni filas |
| **C-46** | **`/programa-general-actualizar` repite diez ids, y es la vista que importa cronogramas** | Medio — mismo mecanismo que C-41, en una vista de escritura | Medido en el ciclo 36 sobre una superficie que estaba en la navegación principal y **nunca se había auditado**: **10 ids duplicados** — `Max_Semana`, `baseDatos`, `permiso_canonico`, `semana`, `Semanal_Confirmada`, `Id`, `opcion`, `cuadro4`, `modal-eliminar-semana-body-texto` y `btn_listar`. Un id repetido hace que `getElementById` resuelva **siempre al primero**, y aquí el detalle que lo vuelve serio es **dónde** ocurre: esta es la vista que carga una actualización de cronograma desde Excel y la asocia contra el programa vigente, es decir, escritura masiva. Varios de esos ids son campos ocultos que llevan el contexto de la operación (`semana`, `Semanal_Confirmada`, `Max_Semana`, `baseDatos`). **No lo toqué, y esta vez la prudencia es concreta:** renombrarlos exige seguir cada lectura en el JS de importación, y equivocarse ahí no rompe un estilo — puede hacer que una importación lea la semana equivocada. Es exactamente el tipo de cambio que pediste no hacer sin decisión tuya. **Coincide con C-41** (`/control-cambios`, 12 ids) y con lo medido en `/pdc` y `/programacion-semanal`, donde `Id` y `opcion` también se repiten: el patrón es transversal y probablemente venga de un partial o de una convención antigua de formularios. **Mi recomendación:** un task que ataque el patrón entero de una vez, empezando por localizar el origen común, en vez de vista por vista. **ORIGEN LOCALIZADO (ciclo 37), así que esa recomendación ya no es vaga:** son **dos fuentes**, no una laguna dispersa. **(1) Doble emisión desde JS** — `cargarDatosGeneralesPagina2.js` inyecta campos ocultos con los ids `Max_Semana`, `Semanal_Confirmada`, `baseDatos`, `permiso_canonico` y `semana`, **los mismos que la vista PHP ya renderiza** en su bloque `.encabezado`, y `funcionesGenerales6.js` hace lo propio con `Id` y `opcion` dentro de los modales de semana. Eso explica **8 de los 10** duplicados de esta vista y explica por qué el patrón sale en cuatro vistas distintas: todas cargan esos dos JS. **(2) Copia-pega dentro de la vista** — `modalEliminarActualizacion` es copia de `modalCargarExcel` y arrastró `cuadro4` y `btn_listar`. **El (2) ya está arreglado** en `dc25242`, tras comprobar que nadie los referencia y que Bootstrap cierra por `data-dismiss`: 10 → 8 duplicados, con el cierre del modal verificado. **El (1) es lo que sigue siendo tuyo,** y ahora la pregunta es concreta y única: **¿cuál de las dos copias manda, la del PHP o la del JS?** Respondida esa, el arreglo es mecánico y sirve para las cuatro vistas a la vez. Mi recomendación es que mande el PHP y el JS deje de inyectar, porque el PHP los emite con los valores ya resueltos en servidor |
| **C-47** | **El guard del matiz de la leyenda de PG lleva fallando desde antes de esta sesión** | Medio — es una red caída, no un defecto visual | Encontrado al alinear los chips de PG con la referencia. `tests/browser/programa-general-legend-hue.mjs` falla en el punto de `actividad-futura`: exige **35 % de saturación** y el píxel real da **29 %** (`#66aa7e`, matiz 141, que sí cae en el rango verde correcto). **Lo importante es que no lo causó el cambio de hoy:** lo comprobé ejecutando el guard contra el árbol limpio, sin ninguna de mis modificaciones, y **falla con el valor idéntico**. Es decir, esa red lleva caída desde antes y nadie lo había notado — el mismo patrón que C-21, donde 34 de 35 pruebas de navegador llevaban tiempo sin proteger nada. **No toco el umbral**, que sería justo lo que me pediste no hacer: forzar el verde moviendo la vara. **Lo que hay que decidir es cuál de las dos cosas está mal:** (a) el punto perdió croma en algún cambio anterior y hay que devolvérselo —el token se deriva con `oklch(... calc(c * 1.4) ...)` y subir ese factor lo arreglaría—, o (b) el piso de 35 % se fijó demasiado alto para el verde concreto de `success-text`, y entonces lo que hay que corregir es el guard. Distinguirlo exige mirar el histórico del token, que es un task propio. **Aviso adicional, ahora que PG lleva tinte:** si se decide (a), conviene rehacer la medición con el chip ya teñido, porque el guard rasteriza el punto **sobre el fondo del chip** y ese fondo acaba de cambiar |
