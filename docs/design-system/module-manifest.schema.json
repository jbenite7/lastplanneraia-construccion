{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lastplanneraia.com/schemas/design-system/module-manifest.schema.json",
  "title": "AIA design-system module manifest",
  "type": "object",
  "required": [
    "schemaVersion",
    "designSystemVersion",
    "moduleId",
    "routes",
    "sources",
    "components",
    "vendors",
    "layouts",
    "states",
    "roles",
    "persistence",
    "exceptions",
    "tests",
    "evidence",
    "scenarios"
  ],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": {
      "const": 2
    },
    "designSystemVersion": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
    },
    "moduleId": {
      "type": "string"
    },
    "status": {
      "description": "Inventory status tracked per module in manifests/inventory.json. pilot: fully migrated, governed by a per-module manifest (routes, scenarios, goldens) validated against this schema. inventory-only: catalogued but not yet migrated; no manifest, no golden coverage. deferred-last: explicitly scheduled to migrate only after every other module; no manifest. observed-frozen: under audit observation with a frozen baseline; no manifest and no path budget; the counter may only go down.",
      "enum": [
        "pilot",
        "inventory-only",
        "deferred-last",
        "observed-frozen"
      ]
    },
    "consumerContract": {
      "enum": [
        "v1"
      ]
    },
    "routes": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "sources": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "components": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "vendors": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "layouts": {
      "type": "array",
      "items": {
        "enum": [
          "mobile",
          "tablet",
          "desktop",
          "wide"
        ]
      }
    },
    "states": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "roles": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "persistence": {
      "type": "object"
    },
    "exceptions": {
      "type": "array"
    },
    "tests": {
      "type": "array"
    },
    "evidence": {
      "type": "array"
    },
    "scenarios": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/scenario"
      }
    }
  },
  "$defs": {
    "viewport": {
      "type": "object",
      "required": [
        "width",
        "height"
      ],
      "additionalProperties": false,
      "properties": {
        "width": {
          "type": "integer",
          "minimum": 320
        },
        "height": {
          "type": "integer",
          "minimum": 568
        }
      }
    },
    "scenario": {
      "type": "object",
      "required": [
        "id",
        "family",
        "route",
        "fixture",
        "theme",
        "viewport",
        "density",
        "state",
        "role",
        "golden",
        "sha256"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string"
        },
        "family": {
          "type": [
            "string",
            "null"
          ]
        },
        "route": {
          "type": "string"
        },
        "fixture": {
          "type": "string"
        },
        "theme": {
          "enum": [
            "dark"
          ]
        },
        "viewport": {
          "$ref": "#/$defs/viewport"
        },
        "density": {
          "enum": [
            "compact",
            "touch"
          ]
        },
        "state": {
          "type": "string"
        },
        "role": {
          "type": "string"
        },
        "golden": {
          "type": "string"
        },
        "sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        }
      }
    }
  }
}
