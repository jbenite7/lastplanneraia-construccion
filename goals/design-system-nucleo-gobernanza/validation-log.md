# Trazabilidad de capacidades — Sprint 00

Este registro documenta capacidades realmente seleccionadas durante el cierre. No sustituye los gates ni convierte resultados automáticos en aprobación humana.

| Fase | Capacidad seleccionada | Motivo y acción | Evidencia | Permiso, riesgo o fallback |
| --- | --- | --- | --- | --- |
| Contratos, catálogo y coherencia | Lectura estructurada local (`rg`, `jq`, `git`) | Contrastar el contrato activo, catálogos, candidatos, inventario y estado del worktree antes de mutar el núcleo. | `goal.md`, `facts.md`, `plan.md`, `docs/design-system/{version,homologation,family-approvals,closeout-evidence}.json` y `git status --short --branch`. | Solo lectura; no limpia, mueve ni versiona trabajo ajeno. |
| Preservación Git verificable | TDD, `git` read-only y verificador Node fail-closed | Separar trabajo comprometido, staged, tracked unstaged, untracked, núcleo, piloto y cambios ajenos mediante hashes antes de cualquier aislamiento o staging. | `worktree-preservation.json` capturó HEAD `fdf34c2a130fd0c7d96b31594c1c0addae880684`, 58 commits sobre `origin/main`, staging vacío, 79 tracked unstaged y 212 untracked expandidos; `scripts/design-system-worktree-preservation.mjs` verifica repo, diffs binarios, contenido untracked, superficies ignoradas y cobertura exacta de clasificación. El RED inicial detectó verificador ausente y luego snapshot stale. Una mutación concurrente posterior en `src/Services/ControlTowerService.php` y `tests/test_bi_programa_general_activity_timeline.php` fue detectada sin revertirla. | El gate vivo permanece bloqueado mientras el worktree siga cambiando y hasta aprobar la maniobra de aislamiento. La prueba contractual usa una copia adulterada para demostrar fail-closed; no convierte un snapshot stale en PASS ni autoriza commit, staging, stash, branch, worktree, reset o restore. |
| Decisión visual compartida | Skill `frontend-design` | Evaluar la transición de tema como comportamiento de un componente canónico: chips y acción primaria no deben interpolar superficie, borde ni texto entre dark y linen. | `docs/design-system/decisions.md` (`DS-023` y `DS-025`, `candidate`), `docs/design-system/CHANGELOG.md` y contrato de Programa General. | No declara aprobación visual; las variantes permanecen candidatas hasta revisión consolidada. |
| Gates reproducibles | Workflow `superpowers:executing-plans`, TDD, Playwright, Node y pruebas PHP dentro de Docker Compose | Ejecutar contratos estáticos y convertir las fronteras de seguridad, persistencia y evidencia en gates continuos. | 2026-07-14: contratos Node 151/151 y `scripts/design-system-contracts.mjs` PASS; preflight CI 5/5 y contrato visual/CI 8/8 PASS. El workflow ahora exige contrato PG, seguridad de tablas globales y E2E UI/API/MySQL con restauración bajo `COMPOSE_PROJECT_NAME=lps-aia-design-system-ci`, consentimiento explícito y selección `construction` fail-closed. El auditor bloquea por drift mixto preservado: 7824 violaciones totales, con `unauthorized-important` 2265 > 2263, `raw-token-in-module` 747 > 737, `duplicate-canonical-primitive` 150 > 147 y `global-module-selector` 94 > 93. PHPStan global: 5 hallazgos conocidos y 0 nuevos mediante fingerprints semánticos que ignoran solo el desplazamiento de línea y rechazan errores globales. | No se elevan los budgets del auditor ni se agregan excepciones para ocultar PDC, BI, Contratos u otros cambios ajenos. La base limpia sigue requiriendo una maniobra Git aprobada. |
| Presupuestos de runtime | TDD, Playwright y subagente de auditoría solo lectura | Convertir los presupuestos exigidos por el contrato en artefacto, colector y comparador ejecutables; medir 0.3.3 sin fabricar aprobación ni usar datos locales. | 2026-07-14: el checkpoint `0.3.3` se extrajo con `git archive` a `/tmp`, sin branch ni worktree, y se ejecutó en puertos/volumen propios contra la misma fixture sanitizada de una fila que `0.3.6`. Tres muestras por versión confirmaron hashes de assets estables. Mediana 0.3.3: CSS 136933 B, JS 650886 B, 7 adapters, 3 duplicadas, flash 1, inicio 227.9 ms, filtro 280.7 ms, assets lab 0. Mediana 0.3.6: CSS 137747 B, JS 649960 B, 7 adapters, 3 duplicadas, flash 0, inicio 234.8 ms, filtro 288.6 ms, assets lab 0. El filtro es una interacción no mutante. El artefacto retrospectivo conserva las tres muestras y checksum; 17/17 contratos enfocados PASS. | El HTML con sesión se conserva solo como procedencia y no entra al hash reproducible. `runtime-baseline-0.3.3.json` mantiene métricas/tolerancias `null`; el comparador exige cero flash y cero assets lab y sigue fail-closed hasta que el usuario apruebe tolerancias. No hubo limpieza del worktree, rama, worktree Git, commit, staging ni datos productivos. |
| Bootstrap de tema | TDD + Playwright sobre Programa General | Eliminar el flash linen→dark desde la capa compartida, sin parchear el piloto ni consumidores inventariados. | El primer reporte observó `firstPaintTheme=null` y `themeFlashCount=1`; `DesignSystemHeadComponent` ahora emite `theme-bootstrap.js` versionado antes de la primera hoja de estilo. La repetición real observó `firstPaintTheme=dark`, `themeFlashCount=0`, `laboratoryAssets=0`; el editor Handsontable se abrió y canceló con `Escape`, sin guardar datos. | El bootstrap solo fija atributo y clases raíz de forma síncrona; la API interactiva permanece en `theme.js`. No modifica módulos consumidores ni datos. |
| Runtime CI y consumidores | Docker Compose, Playwright y subagente CI read-only | Validar la topología reproducible y evitar que el E2E pueda apuntar a la base local por pérdida de variables. | `lps-aia-design-system-ci` levantado el 2026-07-14 con app `18081`, MySQL `13307`, `APP_ENV=testing`, volumen propio, fixture A/R/C/V, seguridad de tablas globales PASS y smoke/laboratorio 16/16 PASS. El subagente confirmó que `dbSnapshot.mjs` selecciona compose mediante `COMPOSE_PROJECT_NAME`/`COMPOSE_FILE`; el nuevo preflight rechaza proyecto, archivos, DB, puertos, volumen, dumps o mounts fuera de allowlist antes de levantar Docker. El runner conserva axe JSON, trace, captura, video y logs Docker ante fallo. | El runtime aislado no toca la base local. Un rebuild final de la imagen y su desmontaje con volumen siguen pendientes de margen de disco y aprobación de limpieza; no se regeneraron goldens ni se migraron consumidores. |
| Revisión visual nativa | Skill `chrome:control-chrome` | Reintentar la revisión consolidada en la sesión de Chrome solicitada por el usuario. | 2026-07-14: después de que el usuario informó haber ingresado, Chrome expuso tres pestañas visibles del perfil controlable; las tres permanecían en `/login` con título `Iniciar Sesión \| Last Planner AIA`. La comprobación no encontró una pestaña autenticada del laboratorio. | No se inspeccionan cookies ni se supone que otra ventana o perfil comparte autenticación. No se afirma aprobación nativa hasta que una pestaña de ese perfil esté autenticada. |
| Homologación BI | Skill `frontend-design`, workflow `superpowers:subagent-driven-development`, subagente de revisión solo lectura y Playwright | Delimitar la corrección a `bi-primitives`: dos series inequívocas en Curva S, cuadrícula sutil, y radar centrado estructuralmente a 634 px. El revisor independiente solo inspeccionó CSS, espécimen y contratos focalizados; señaló y se corrigió la equivalencia de colores entre leyenda y series. | Aprobación visual explícita del usuario el 2026-07-13 para Curva S y radar; `node --test tests/design-system/bi-primitives.test.mjs` 5/5 PASS; `npx playwright test tests/browser/design-system-lab.mjs --grep "BI figure exposes" --workers=1` 1/1 PASS; `npm run test:a11y:lab` 1/1 PASS. | El subagente no escribió, no creó rama/worktree ni ejecutó Git mutable. El catálogo MCP efectivo solo expuso integraciones de importación/diseño externo no pertinentes al laboratorio local, por lo que no se enviaron datos ni se forzó un complemento; Playwright fue el fallback reproducible. |
| Datos del piloto | Docker Compose + E2E de Programa General | Confirmar interacción UI, API, MySQL, recarga y restauración de la fila usada en la prueba. | 2026-07-14: `node tests/test_programa_general_sprint_contract.mjs` PASS; `pg-interactions.spec.mjs` serial 3 PASS y 1 fixture opcional SKIP. Se verificaron 273 filas, persistencia UI/API/MySQL, exportación CSV, leyenda, permisos 200/403 y rechazo 403 de escrituras manipuladas; fingerprint final idéntico al snapshot. | La mutación de prueba se restauró en `finally`; no se tocaron datos de producción ni otros módulos. |
| Accesibilidad, teclado y reflow | Playwright contra el runtime local servido | Revalidar los gates automáticos consolidados sin sustituir la revisión humana. | 2026-07-14: `npm run test:a11y:lab` 1/1 PASS sobre la matriz aprobada; `npm run test:a11y:pilot` 6/6 PASS en mobile/tablet/desktop y dark/linen; `npm run test:keyboard` 2/2 PASS; `npm run test:reflow` 2/2 PASS a 320 CSS px, incluida la regla de envolver entre palabras sin fragmentarlas. | Son prechecks automáticos read-only. VoiceOver, zoom 200 %, insights de accesibilidad y aprobación visual nativa continúan bloqueantes. |
| API estable y enforcement posterior | TDD + contratos ejecutables | Enumerar la promesa SemVer sin declarar prematuramente la release y registrar el enforcement remoto posterior. | `stable-api-1.0.0.json` enumera exactamente `shell`, `page-header`, `toolbar`, `button`, `field`, `state`, `card`, `table-shell`, `overlay` y `handsontable-adapter`, con evidencia exigida en laboratorio y Programa General. `DS-GOV-001` queda registrada con owner Felipe, trigger posterior al primer push autorizado de 1.0.0 y required checks como resultado. Cinco pruebas focalizadas y la suite 151/151 PASS; el schema rechaza campos no declarados tanto en el artefacto como en cada componente. | El contrato permanece `pending-gates`; `version.json` sigue `0.3.6 / construction`. No hubo staging, commit, push, deploy ni activación de branch protection. |
| Gate externo de revisión | Plannotator | Está autorizado para el cierre del diff; se reserva para después de resolver los gates visuales y manuales bloqueantes. | `plan.md` §10 y `closeout-evidence.json`, gate `plannotator` pendiente. | Enviar un diff privado sigue requiriendo el consentimiento ya otorgado; no se ejecuta prematuramente ni se usa para eludir revisión humana. |
