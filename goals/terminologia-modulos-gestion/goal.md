# Terminologia de modulos de gestion

Cambiar la terminologia visible de `/listado-actividades/`, `/contratos/` y `/pdc/` para que el usuario entienda el flujo como Familias de obra, Paquetes de contratacion y Plan de Compras y Contrataciones. El cambio debe mantener rutas, APIs, nombres internos y valores persistidos que soportan la logica actual.

La comprension compartida esta en `facts.md`.

El plan de ejecucion aprobado esta en `plan.md`.

Done condition: los 28 hechos aceptados se reflejan en la UI, las pruebas enfocadas pasan y hay verificacion en navegador de las tres rutas principales.
